# ROADMAP — per-document status

Kept in sync with `docs/pipeline/pipeline-journal.md`'s Position block and each directory's own
`INDEX.md`. This is a flat summary for a quick glance; the indexes are authoritative on detail.

| Stage | Artifact(s) | Status |
|---|---|---|
| 00 Pipeline | Journal, backlog | ✅ Live (`docs/pipeline/`) |
| 01 Vision | MSTR-001 v1.5 (v1.1: autonomous bad-zone recovery; v1.2: single-bank/no-save reopened, §9 added; v1.3: C10 research-to-code traceability; v1.4: §9's first three research threads cycled in — genre feasibility tiered, style-evolution/song-form + emotional/energy promoted to groundable architecture candidates, cart-shape facts recorded not decided; v1.5: §9's fourth/final thread — visual evolution + audio-visual sync (R222/R223) — cycled in and promoted to the same tier; flags roadmap R9's "research not run" prerequisite note as now stale), GDS-00 (unchanged — no visual-evolution statement to reconcile), strategic assumptions register (7 assumptions; A5 research landed, adoption decision still open; A7's trigger already fired) | ✅ Amended 2026-07-31; all four §9 threads now cycled in; forward-traceability audit + cart-shape adoption decision remain owed |
| 02 Research | Per-topic encyclopedia (`docs/research/encyclopedia/`) — **49 topics**: R101-R115 (hardware, R106 substantially extended 2026-07-22 — MBC1/3/5 + PyBoy save-mechanics facts; **R104 §7-8 addendum added 2026-07-26** — visual-evolution VRAM/ROM budget, `BL-0034`), R201-R225 (design, R219-R221 added 2026-07-22; R222/R223 added 2026-07-26 — visual evolution + audio-visual sync, `BL-0034`/`BL-0035`; R211/R214/R219 addenda added 2026-07-26 — weighted random selection `BL-0037`, L-system motif recurrence `BL-0010`, Celtic/Seasonal/Holiday genres `BL-0036`; **R224 added 2026-08-08** — listening-evaluation methodology/per-parameter critique elicitation, `BL-0097`; **R225 added 2026-08-19** — harmonic coordination / shared chord context, `BL-0119`, with a **withdrawal addendum at R211 §9** striking that topic's "do not attempt chord-progression harmony" recommendation, an R212 §7 cross-link, and an **R224 §7 addendum** adding the holistic review dimension, `BL-0120`), R301-R309 (tooling, R302 §8-9 addendum added 2026-07-22); **`R217` §3a addendum added 2026-08-21** — the "song regeneration" gap closes on the record (`BL-0143`): it had been filed as needing a *menu*, and therefore a `01-vision` scope change, because it was grouped with seed entry/menu design/preset management under one premise; regeneration in shipped generative instruments is consistently a **parameterless single control**, so a button suffices and GDS-01's flat single-running-state model is untouched. Also records that the capability was two-thirds present since `IP-0007`'s 2026-07 `DIV` reseed — the real gap was "regenerating costs you your settings" — and the general lesson to **route a parked item by the interaction surface it needs, not the category it resembles** — see `docs/research/INDEX.md` | ✅ **All open research-gap backlog items closed** (`BL-0010`, `BL-0034`-`BL-0037`; `BL-0119` and `BL-0120`'s research half closed 2026-08-19) — no decisions made, facts only, ready for `03-architecture-design-synthesis`; R224 additionally grounds `09-content-review`'s still-unrun first invocation (`BL-0097`). **Open:** `BL-0120`'s `09-content-review` skill-definition half (out of every pipeline stage's write scope) |
| 03 Architecture | **GDS-02 (System Context) + GDS-04 (Domain Model) + GDS-06 (Non-functional Requirements) + GDS-08 (Presentation Architecture) + GDS-09 (Interface Specification) + GDS-10 (RTM level) authored 2026-07-26 — **the GDS ladder is now COMPLETE**, closing `BL-0001` (open since run #1)** — both written against the real shipped system rather than synthesized forward; GDS-02 names the never-tested-on-hardware gap explicitly, GDS-04 names the steering-index family/writer contract, the index-0 invariant, and the one-frame-stale bad-zone read; GDS-06 defines what a ladder-level NFR is versus an `NFR-xxxx` leaf, and records that `R101`'s cycle-tallying revisit trigger appears to have fired — **corrected 2026-07-31 (`BL-0069`): the Select-frame write-drop evidence it cited was falsified; the trigger-fired judgement survives on direct `LY` measurement showing the per-frame budget is ~exhausted on every frame (GDS-06 §2.2a/§2.3, GDS-02 §7)**. **GDS-05 is now recorded as deliberately not owed**, not merely planned. GDS-08 closes `BL-0001`'s oldest disposition, records the stateless re-render contract and the palette-swap extension strategy, places the four scattered reactive-signal questions, and addresses `BL-0016`/`BL-0021` at design altitude. GDS-09 closes the gap all six `FS-1xx` specs flagged, and found on reading the source that the **patch-point contract is vestigial** (empty dicts, discarded return values) and that `visuals.py`'s WRAM-constant duplication grew 1→11 in one package. GDS-00/01/03/07 authored (GDS-01/03 amended for autonomous recovery, `IP-0007`; GDS-03/07 reconciled 2026-07-22 against shipped code, `BL-0013`/`BL-0018`); GDS-05 deliberately not owed (rationale in GDS-06 §0). `ADS-100` (combinable generation schemes, `BL-0020`) + `ADR-0001`; `ADR-0002` — R4.5 cart-shape decision (defer MBC/SRAM adoption, evidence-based, named re-triggers), 2026-07-25. `ADS-101` — Genre-Aware Style Presets (R5), 2026-07-26 — a parallel `STYLE_TABLE` keyed by the existing `CHMIX_IDX` index, 3 concrete v1 styles named. `ADS-102` — Motif Recurrence via Weighted Variant Selection (`BL-0010`), 2026-07-26 — extends `IP-1070`'s `MOTIF_TABLE` to a small fixed set of variants selected at cycle-boundary events via a weighted lookup table, no L-system derivation engine. `ADS-103` — Song-Form & Style-Drift State Machine (R6, `BL-0010`'s song-form half), 2026-07-26 — a new, independent state machine (`SONG_STATE`/`SONG_STATE_TIMER`) cycling 4 phases, overwriting `TEMPO_IDX`/`DENSITY_IDX` per phase; stays independent of `IP-0007`'s bad-zone loop (disjoint WRAM fields); style-drift deferred past v1. **New: `ADS-104` — Settings & Control Visibility (`BL-0051`), 2026-07-26** — unifies four scattered visualizer-reactive-signal Open Questions with the user's own base-control-legend request; decides 5 new bar-height indicator tiles (tempo/octave/scale/density/channel-mix). **New: `ADS-105` — Emotional/Energy Layer (roadmap R7), 2026-07-31** — routes `R221`'s valence-arousal model; decides 2 new stored WRAM bytes (`AROUSAL`/`VALENCE`) recomputed only on the writes that can change their inputs (not every frame) — a direct response to `IP-9030`'s VBlank-budget finding, keeping this release's own per-frame CPU cost at zero; `DISSONANCE_SCORE`'s unconfirmed valence-proxy role explicitly deferred, not baked in. **New: `ADS-106` — Visual Evolution & Audio-Visual Synchronization (roadmap R9), 2026-07-31** — routes `R222`/`R223`/`R104` §7-8, unblocked at the vision layer by `MSTR-001` v1.5; decides a new `THEME_TABLE` (style-keyed, `CHMIX_IDX`-indexed, `visuals.py`-resident, independent of `STYLE_TABLE` to preserve the acyclic-import rule) replacing the flat `CALM_PALETTE` as the non-bad-zone color source, with `BAD_ZONE_FLAGS` always overriding; discrete-only for v1 (no color-cycling); mood-modulation (`IP-1120`'s `AROUSAL`/`VALENCE`, its first real consumer) explicitly deferred to a follow-on increment reusing the same mechanism. **New: `ADS-107` — Genre Blending (roadmap R8), 2026-08-07** — routes `BL-0020`'s "solo or in combination" framing and `R220`'s style-drift finding, resolving `ADS-103`'s own explicitly-deferred Open Question on whether style-drift reuses the song-form state machine (answer: no — independent WRAM, same countdown-then-overwrite shape); decides 4 new WRAM bytes (`BLEND_SRC_TEMPO`/`DENSITY`/`DUTY`, `BLEND_STEP`), `SCALE_IDX` hard-switches at blend start (categorical, cannot interpolate), 4 discrete blend steps via a compile-time shift (no SM83 division), reuses Start's existing edge (no new control, per `R217`); **explicitly supersedes `FR-1240`'s instant-apply guarantee for `TEMPO_IDX`/`DENSITY_IDX`/`DUTY_BIAS`**, flagged for `04-requirements-engineering` to amend rather than silently add beside. **New: `ADS-108` — Harmonic Coordination via a Shared Chord Context (`BL-0119`), 2026-08-19** — routes the project's largest measured quality gap (three pitched channels random-walking with no shared harmonic state; 36.1 % harsh vertical intervals), grounded in the new `R225`. Decides one stored `CHORD_IDX` WRAM byte every pitched voice reads at its own onset; a hand-authored per-scale `CHORD_TABLE` of degrees rather than runtime third-stacking (which is *incorrect* against the shipped `AND 0x07` 8-entry degree space); a 4-chord sparse tonic-biased weighted transition table; harmonic rhythm counted in **pulse-A onsets, not frames**, with the clock inside pulse A's existing onset branch so **no unconditional per-frame work is added anywhere** (`GDS-06` §2.2a / `BL-0113`'s bar); bass = root/fifth, melody = chord tone on strong onsets and a step on weak ones, harmony voice separated by an **octave, not scale degrees**; the bad-zone detector kept but made scope-aware rather than deleted or left to fight the generator; phrase/rest/cadence deferred to increment 2 with its shape reserved; and the boot default **deliberately unchanged** pending an evidence-gated flip (so increment 1 does *not* by itself answer the user's complaint — stated explicitly). **`ADR-0003`, 2026-08-19** — scheme selection moves out of `CHMIX_MASKS`'s spare bits into a parallel `SCHEME_TABLE` keyed by the same index (a third scheme needs 2 bits per channel); amends `ADR-0001`'s packing, not its principle — **⚠️ superseded 2026-08-20 before implementation**. **New: `ADS-108` §11 amendment + `ADR-0004`, 2026-08-20** — the project owner released `GDS-04` §4.1's *historical-no-regression* reading of the index-0 invariant as self-imposed (`GDS-04` §4.1 amended in step: the **fixed-point** half — index 0 equals the boot preset, so boot and Select-reset agree with each other — stands unchanged and load-bearing; only the "boot must sound like it always has" half is released). That reading was the sole reason harmonic coordination was designed as a *parallel* Scheme H beside the default rather than *as* it, and therefore the sole reason `CHMIX_MASKS` ran out of bits. **D13: chord-derived note selection replaces the default scheme's note selection in place.** No third scheme, no `SCHEME_TABLE`, no `CHMIX_MASKS` migration; `ADR-0001`'s one-bit packing reaffirmed in full; Scheme E untouched and still unharmonized; `BL-0123`'s `IP-8xx0` refactoring package obviated rather than sequenced; `ADS-108` §8 R1 (migration risk) and R7 (increment 1 changes nothing audible) both deleted with their causes. The boot sound now changes deliberately, and the audible improvement lands in increment 1. **New: `ADS-108` §12 amendment + `ADR-0005`, 2026-08-21** — the owner listened to the shipped result and reported *"constant repeated arpeggios."* `IP-1060`'s `_emit_arpeggio_tick` was never revisited when harmony arrived: it rewrites both pulse channels' frequency registers **every frame** with `[0,2,4,2]` `AND 0x07` scale-degree offsets applied *to the chord tone the harmony just chose*, stacking a second differently-rooted triad on the engine's own chord, in a 24-frame figure with no mechanism by which it could ever vary. Measured on `3e635ed`: pulse B sits on a chord tone at **100 %** of its onsets yet only **46.4 %** of sounding pulse frames are chord tones, and strong-beat harsh intervals read 12.0 % on *intent* but **25.7 % on sounding pitch** — the arpeggio undoes most of §11's delivered benefit. **D14: re-root the arpeggio onto `CHORD_TABLE`'s own tones; gate it (only a chord tone arpeggiates — pulse A's passing tones sustain); vary it (pattern drawn per onset from a weighted table whose reserved *sustain* value makes gate and variation one mechanism); and require its per-frame cost to strictly *decrease* via an onset-resolved per-channel cache** — the first chance since `R101` §8.5 declared the VBlank budget exhausted to *recover* head-room rather than spend it. Retirement was weighed as the strongest rival and rejected on evidence (with no rests or phrase structure yet, sub-note motion is the only thing happening between onsets), and kept as a one-table-edit fallback. Absorbs `CR-0006` and `BL-0125`; `ADR-0001`/`ADR-0004` untouched. Also records `BL-0128`: **`IP-1140`'s acceptance figures were measured on intent, not sounding pitch, and are overstated** (25.7 %/36.1 %/30.9 %, not 12.2 %/30.6 %/21.5 %). **New: `GDS-04` §1.4 + `GDS-07` §5a, 2026-08-21 — the pitch/output-layer writer registry.** The audit that produced the 2026-08-20 skill amendments named its root cause precisely: `GDS-04` §1.2 has registered the writers of the five steering indices since 2026-07-26, but the layer that determines what is actually *heard* — `CUR_DEGREE_*`, `NOTE_TIMER_*`, `LFSR_STATE*`, `SEMI_*`, `ARP_STATE_*`, `ARP_CACHE_*`, `DUTY_BIAS`, `CHORD_*` and the eleven PSG registers — appeared in **no writer registry anywhere**, which is how `ADS-108` could design chord-derived pitch selection without discovering `arp_tick` was already rewriting the same registers every frame. `07`'s amended four-question sweep points its question 1 at this registry, so the registry had to exist for the amendment to have teeth. Its central finding, stated at domain altitude: **the pitch layer does not follow §1.2's last-write-wins contract.** Note *selection* is single-write-site by construction; note *articulation* is layered and **the per-frame layer always wins** — a pitch written at an onset is an intention, and the pitch that sounds is whatever `arp_tick` writes next frame. Names the wave channel's asymmetry (no `arp_tick`, so its onset write *is* what sounds), and reframes `BL-0124`/`BL-0128`/`BL-0138`/`BL-0140` as four instances of one failure: not asking who writes this last. **New: `ADR-0006`, 2026-08-21 — Select becomes a reroll, not a reset.** On the project owner's direct instruction (*"The select-reset does not need to bring it back to the boot default either, just course correct from a bad zone"*), Select now reseeds each channel's LFSR and clears the bad-zone fields while **leaving the five steering indices alone**, so the listener's settings survive the press. `FR-1070` conflated three unrelated things, one actively hostile: it discarded every parameter the listener had set. The index reload was a **workaround for a constraint lifted in 2026-07** — it existed because Select was the only bad-zone escape, `IP-0007` made recovery autonomous, and both `Claude.md` and `GDS-04` §5 recorded that supersession in prose while nothing re-examined the mechanism it obsoleted. **`GDS-04` §4.1's fixed-point invariant is re-stated, not broken by omission** (second dated amendment): the table constraint and `IP-1100`'s ten-test regression evidence are untouched, boot still lands on the fixed point, and rule (a) loses only the clause "and Select-reset" — Select stops being an event about the indices at all rather than landing somewhere else. `GDS-01` step 6 and `GDS-04` §4's preset entity amended to match. **No `01-vision` change**: a button adds no menu state, so this stays inside GDS-01's flat single-running-state model. Boot path deliberately unaffected. | 🟡 Partial |
| 04 Requirements | FR-1000...FR-1120, NFR-1000...NFR-1030 (2026-07-21); extended 2026-07-22 with FR-1130...FR-1170, NFR-1040/1050 (`BL-0024`, R216 sound-design techniques); extended 2026-07-25 with FR-1180...FR-1220, NFR-1060/1070 (`BL-0020`/`ADS-100`, combinable generation schemes); extended 2026-07-26 with FR-1230...FR-1260, NFR-1080/1090 (roadmap R5/`ADS-101`, genre-aware style presets); extended again 2026-07-26 with FR-1270...FR-1300, NFR-1100/1110 (`BL-0010`/`ADS-102`, motif recurrence via weighted variant selection); extended again 2026-07-26 with FR-1310...FR-1340, NFR-1120/1130 (roadmap R6/`ADS-103`, song-form & style-drift state machine); extended once more 2026-07-26 with FR-1350...FR-1380, NFR-1140...NFR-1160 (`BL-0051`/`ADS-104`, settings & control visibility); **corrective pass 2026-07-26** — `FR-1120`'s tempo claim split (`BL-0016`, per GDS-08 §7), `NFR-1010` caveated (`BL-0060`, per GDS-06 §2.2), first Candidate Requirement `CR-0001` recorded, and the INDEX's two `⛔ Planned` rows re-labelled as deliberate deviations (`BL-0068`, per GDS-10 §3.1) | ✅ Authored 2026-07-21; extended 2026-07-22, 2026-07-25, 2026-07-26 (×4) + corrective pass |
| 05 Feature Decomposition | Feature Catalog v1 (FEAT-1000...FEAT-1050); `FEAT-1060` added 2026-07-22 (R216 sound-design techniques, `BL-0024`, shipped/VERIFIED); `FEAT-1070` added 2026-07-25 (combinable generation schemes, `BL-0020`/`ADS-100`, R4, shipped/VERIFIED); `FEAT-1080` added 2026-07-26 (genre-aware style presets, roadmap R5/`ADS-101`, shipped/VERIFIED); `FEAT-1090` added 2026-07-26 (motif recurrence via weighted variant selection, `BL-0010`/`ADS-102`, shipped/VERIFIED); `FEAT-1100` added 2026-07-26 (song-form via autonomous phase cycling, roadmap R6/`ADS-103`, `VERIFIED`, not yet part of shipped baseline); **`FEAT-1110` added 2026-07-26** (settings & control visibility, `BL-0051`/`ADS-104` — `VERIFIED` via `IP-1110`/`VR-1110`); **`FEAT-1160` added 2026-08-21** (`BL-0127`/`ADS-108` §12/`ADR-0005`) — the arpeggio re-rooted, gated and varied, bucketed **MVP-critical remediation** immediately behind `FEAT-1150` as its *completion* rather than a competing candidate | ✅ Authored 2026-07-21; extended 2026-07-22, 2026-07-25, 2026-07-26 (×4) |
| 06 Feature Specification | **`FS-100`...`FS-105` dispositioned 2026-07-26 as a deliberate deviation, not owed** (`BL-0006`/`BL-0012` closed) — the GDS ladder's completion covers every field such specs would have carried, with `docs/features/INDEX.md` now pointing at where each feature's design content actually lives; reversible, with a named re-trigger if any of `FEAT-1000`-`FEAT-1050` is materially changed rather than added to. `FS-106` authored 2026-07-22 (full 20-field spec) for `FEAT-1060`. `FS-107` authored 2026-07-25 for `FEAT-1070` (combinable generation schemes, `BL-0020`/`ADS-100`). `FS-108` authored 2026-07-26 for `FEAT-1080` (genre-aware style presets, roadmap R5/`ADS-101`). `FS-109` authored 2026-07-26 for `FEAT-1090` (motif recurrence via weighted variant selection, `BL-0010`/`ADS-102`). `FS-110` authored 2026-07-26 for `FEAT-1100` (song-form via autonomous phase cycling, roadmap R6/`ADS-103`). **`FS-111` authored 2026-07-26** for `FEAT-1110` (settings & control visibility, `BL-0051`/`ADS-104`), `VERIFIED` via `IP-1110`/`VR-1110`. **Corrective pass 2026-07-26**: all six specs' stale "GDS-09 remains ⛔ Planned" *Interfaces Used* lines updated to cite the now-authored GDS-09 (`BL-0066`), and `FS-108`'s acceptance criterion (4) reworded from an absolute same-frame guarantee to the true mechanism-level claim (`BL-0040`).; **`FS-115` authored 2026-08-21** for `FEAT-1160` — full 20-field spec whose load-bearing contribution is System Behaviour B2 + acceptance criterion A5, answering `BL-0130`: *sustaining means the frequency register keeps being written every frame*, because portamento and vibrato are produced **by** the very routine being rewritten | ✅ FS-106...FS-111 authored; FS-100...FS-105 dispositioned as a deliberate deviation |
| 07 Implementation Planning | Master Build Plan, TWBS (IP-0001...IP-0007 + remediation tranche IP-9010/IP-9020 for `BL-0019`/`BL-0017`; IP-1060/IP-1061 for `FS-106`/`BL-0024`; IP-1070 for `FS-107`/`BL-0020`; IP-1080 for `FS-108`/roadmap R5; IP-1090 for `FS-109`/`BL-0010`; IP-1100 for `FS-110`/roadmap R6; IP-1110 for `FS-111`/`BL-0051`) — 16/16 `VERIFIED` — **all 16 now in the shipped baseline** (R6/`IP-1100` + `IP-1110` GO confirmed 2026-07-31).; **`IP-1150` authored 2026-08-21** (`FS-115`/`FEAT-1160`, `BL-0127`) — `BLOCKED` on `IP-1140` being `COMPLETE`-not-`VERIFIED` with an argued disposition rather than a silent waiver; G3 granted on two cited bases. The tranche's TWBS is the **first real exercise of the widened four-question collision & obsolescence sweep**, which exists because its own question 3 was not asked when `IP-1140` was planned. `IP-1140`'s recorded acceptance figures **corrected in place** (`BL-0128`): measured on intent, not sounding pitch, and overstated | ✅ **16/16 packages VERIFIED**, 1 `READY` (`IP-9030`) |
| 08 Implementation | IP-0001-IP-0007 `VERIFIED`; IP-1060/IP-1061 `VERIFIED`; IP-9010/IP-9020 `VERIFIED`; IP-1070 (combinable generation schemes, `BL-0020`) `VERIFIED` via [VR-1070](docs/implementation/verification/VR-1070-combinable-generation-schemes.md). `IP-1080` (genre-aware style presets, roadmap R5) `VERIFIED` 2026-07-26 — 93/93 tests (T1-T15), independent live-drive confirms the DoD, via [VR-1080](docs/implementation/verification/VR-1080-genre-aware-style-presets.md). `IP-1090` (motif recurrence via weighted variant selection, `BL-0010`/`ADS-102`) `VERIFIED` 2026-07-26 — 102/102 tests (T1-T16), independent live-drive (a guaranteed exact-frame collision between a motif-cycle-boundary variant draw and an `IP-1080` style change) confirms the DoD, via [VR-1090](docs/implementation/verification/VR-1090-motif-recurrence-via-weighted-variant-selection.md). **`IP-1100` (song-form via autonomous phase cycling, roadmap R6/`ADS-103`) `VERIFIED` 2026-07-26** — 112/112 tests (T1-T17), independent live-drive (guaranteed Start-press/phase-transition collisions forced at all three cycle-internal boundaries, plus an empirical `OVERLOAD`-frequency comparison across all 4 phases) confirms the DoD, via [VR-1100](docs/implementation/verification/VR-1100-song-form-via-autonomous-phase-cycling.md). **`IP-1110` (settings & control visibility, `BL-0051`/`ADS-104`) `VERIFIED` 2026-07-26** — 122/122 tests (T1-T18), independent live-drive (a fresh 3000-frame zero-input stress run re-confirming the reverted channel-activity regression is genuinely gone, plus a frame-by-frame Select-press probe across 3 distinct pre-Select sequences reproducing what was then believed to be a self-healing display-lag finding — **that finding was falsified 2026-07-31, `BL-0069`: it was a `pb.tick()` sampling artifact, and `VR-1110` carries a correction notice**) confirms the DoD, via [VR-1110](docs/implementation/verification/VR-1110-settings-and-control-visibility.md). | ✅ **16/16 packages VERIFIED** |
| 09 Verification | All 16 packages (7 original + `IP-9010`/`IP-9020` + `IP-1060`/`IP-1061` + `IP-1070` + `IP-1080` + `IP-1090` + `IP-1100` + `IP-1110`) independently `VERIFIED`, each in a genuinely fresh session — [VR-0001](docs/implementation/verification/VR-0001-skeleton-and-single-channel-generation.md)-[VR-0007](docs/implementation/verification/VR-0007-autonomous-recovery-and-randomize.md), [VR-1060](docs/implementation/verification/VR-1060-arpeggio-and-duty-cycle.md), [VR-1061](docs/implementation/verification/VR-1061-vibrato-and-portamento.md), [VR-9010](docs/implementation/verification/VR-9010-channel-mix-gating.md), [VR-9020](docs/implementation/verification/VR-9020-overload-threshold-recalibration.md), [VR-1070](docs/implementation/verification/VR-1070-combinable-generation-schemes.md), [VR-1080](docs/implementation/verification/VR-1080-genre-aware-style-presets.md) (one Medium finding — an acceptance-criterion precision gap on bad-zone independence, not a code defect, routed to `04-requirements-engineering`), [VR-1090](docs/implementation/verification/VR-1090-motif-recurrence-via-weighted-variant-selection.md) (two Medium findings, one Low), [VR-1100](docs/implementation/verification/VR-1100-song-form-via-autonomous-phase-cycling.md) (one Low-Medium test-coverage finding, one Low descriptive-data finding, one Low doc-staleness finding, self-corrected), [VR-1110](docs/implementation/verification/VR-1110-settings-and-control-visibility.md) (one Low doc-defect — a disclosed backlog update not completed; one Low doc-staleness finding, self-corrected; one Low test-coverage finding). **`IP-1130` (Genre Blending, roadmap R8) additionally `VERIFIED` 2026-08-09** via [VR-1130](docs/implementation/verification/VR-1130-genre-blending.md) (re-verification pass, 3 non-blocking findings). | ✅ **17/17 packages independently verified** |
| **09b Content Review** | **First-ever `09-content-review` pass, 2026-08-14** (`BL-0097`) — [content-review-full-baseline.md](docs/reviews/content-review-full-baseline.md), all 17 `VERIFIED` packages driven live via `pyboy` at commit `9d58bf5` (screenshots + WRAM/register traces + the full R224 listening-evaluation question set) **Second pass 2026-08-21** — [content-review-ip-1150-arpeggio.md](docs/reviews/content-review-ip-1150-arpeggio.md), reviewing `IP-1150` against `BL-0127`'s reported complaint. **Clean on the question asked, 2 Medium findings.** First run under the skill's new mandatory four-holistic-questions-first rule and its output-boundary rule, and both earned their keep: the decisive evidence is a `pyboy` instruction hook on the two `LDH` writes to `NR13`/`NR14` inside `arp_tick_pa`, reading `A` at the instant of the write — over 1800 frames the **pre-change** build's pulse-A pitch held for **only ever 2, 4 or 6 frames, never longer, 357 runs without exception**, which *is* the owner's "constant repeated arpeggios" measured at the register; the post-change build spreads 2-60 frames, mean 5.0 → 13.7. The holistic questions then surfaced what no parameter question could: every parameter check passes and the answer to *"is there a melodic idea you could hum back?"* is still **no** — routed as **F1 (Medium)**, the engine still never rests, now the dominant defect and the cheapest third of `CR-0003`. **F2 (Medium)**: the figure varies in shape but every moving figure still moves on the same 6-frame sub-tick grid — which is precisely the trigger `BL-0129` was deferred against, so that deferral's own condition is now met by evidence. | ⚠️ **5 findings, no Critical/High** — F1 (Medium-High): default preset enters the bad-zone almost immediately at boot in 5/5 trials, undercutting the "calm default" intent; F2 (Medium): `DENSITY_IDX` does not monotonically increase onset rate, the nominal densest setting undershoots several sparser ones; F3 (Low-Medium): bad-zone entry/exit oscillates repeatedly under default no-input play; F4 (Low): octave/mode audible correctness unverifiable this pass (write-only PSG registers, pre-existing gap); F5 (Medium, not new): motif variation reachable via only one shipped preset, re-confirms `VR-1070`. Feeds the roadmap's proposed R12.5 Content & Musical Quality Pass. |
| 10 Integration Review | [Foundation bucket review](docs/reviews/integration-review-foundation-bucket.md) — re-reviewed 2026-07-25 at full 12-package scope (+`IP-1070`); re-reviewed 2026-07-26 at full 13-package scope (+`IP-1080`); re-reviewed 2026-07-26 at full 14-package scope (+`IP-1090`); re-reviewed 2026-07-26 at full 15-package scope (+`IP-1100`): song-form state machine confirmed structurally independent of bad-zone detection/recovery and Scheme-E motif-variant selection, no new finding. **Re-reviewed again 2026-07-26 at full 16-package scope** (+`IP-1110`): `visuals.py`'s complete write-target set confirmed read-only against all engine state/PSG registers (VRAM/palette/`LCDC` only) by direct code inspection; the disclosed Select-frame VRAM-write timing exception live-verified scoped to `IP-1110`'s own writes only — `IP-0006`'s channel-activity mechanism and `IP-0007`'s bad-zone-reset path both continue updating correctly on the same frame class. No new finding. `BL-0030`/`BL-0032`/`BL-0040`/`BL-0041`/`BL-0044`/`BL-0045`/`BL-0048`/`BL-0052`/`BL-0053`/`BL-0057` remain open, non-blocking. No Critical/High anywhere. All 16 `VERIFIED` packages now integration-reviewed together at least once. **New: [R7 verification tranche review](docs/reviews/integration-review-r7-verification-tranche.md), 2026-08-14** — explicit 4-package scope (`IP-9030`/`IP-8010`/`IP-8020`/`IP-1120`), independent of the Foundation-bucket review above. 2 findings: Low-Medium doc gap (`GDS-03` module table), and a **Medium-High cross-package defect** — `IP-1130` (shipped after `IP-1120`) never wires a `mood_update` recompute into its genre-blend write path, silently violating `FR-1410`; no player-visible harm yet (no consumer of `AROUSAL`/`VALENCE` exists), but flagged before roadmap R9 would be the first to hit it. Recommend remediation via `07-implementation-planning` before R9. | ⚠️ R7 tranche: 1 Medium-High finding open, recommend remediation before R9 |
| 11 Release Readiness | [Release Assessment](docs/reviews/release-assessment-r1-r2-r3.md) — consolidated R1 (Foundation) + R2 (Sound Design) + R3 (Integrity Remediation) + R4 (Combinable Generation Schemes) + R5 (Genre-Aware Style Presets) + `IP-1090`/`BL-0010` (Motif Recurrence via Weighted Variant Selection) + **R6 (Song-Form via Autonomous Phase Cycling) + `IP-1110`/`BL-0051` (Settings & Control Visibility)**. GO confirmed by the user 2026-07-25 (R1-R3), 2026-07-25 (R4), 2026-07-26 (R5), 2026-07-26 (`IP-1090`), and **2026-07-31 (R6 + `IP-1110`)**. The 2026-07-31 confirmation also withdrew a residual-risk item that had been accepted in the recommendation — `IP-1110`'s "disclosed Select-frame display lag" was falsified (`BL-0069`); it never existed, and two real items replaced it in the risk table (the ~exhausted per-frame VBlank budget, and the untested mode-3 exposure on physical hardware). | ✅ **SHIPPED — R1+R2+R3+R4+R5+IP-1090+R6+IP-1110 GO, 2026-07-31** |
| **Product Roadmap** | [`docs/roadmap/`](docs/roadmap/INDEX.md) — full planning package (product goals, capability map/dependency graph, 14-release sequence R0-R13+R4.5, milestones A-F, traceability matrix, dev strategy, exit criteria, self-review) synthesized 2026-07-22 from the existing Vision + 42-topic Research Encyclopedia, no new research | ✅ Authored 2026-07-22 — planning input for `03`/`04`/`05`/`06` as each release is picked up, not itself an implementation authorization |

See `docs/pipeline/backlog.md` for the live list of what each 🟡/🔴/⛔ actually needs next.
