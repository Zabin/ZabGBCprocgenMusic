# Verification Reports — Index

Owned by `09-package-verification` — the only skill that writes `VERIFIED`.

[↑ Implementation index](../00-master-build-plan.md)

| VR | Package | Date | Result | Headline |
|---|---|---|---|---|
| [VR-0001](VR-0001-skeleton-and-single-channel-generation.md) | IP-0001 | 2026-07-21 | ✅ VERIFIED | Skeleton build chain + pulse-A generation + full input mapping + scoped reset; 32/32 tests, non-default tempo extremes independently re-driven. Same-session verification, user-accepted exception (not a standing waiver). |
| [VR-0002](VR-0002-pulse-b-and-wave-channel.md) | IP-0002 | 2026-07-21 | ✅ VERIFIED | Pulse B + wave channel generation (parameterized `_emit_channel_gen`, `CHANNELS` table); 60/60 tests, octave-floor edge case (`OCTAVE_IDX=0`) + half-rate reload independently re-driven live. Genuinely fresh session. |
| [VR-0003](VR-0003-noise-channel-and-density.md) | IP-0003 | 2026-07-21 | ✅ VERIFIED | Noise channel + Euclidean density wiring; 60/60 tests, mid-range density (`DENSITY_IDX=3`, k=5) independently driven live confirming monotonic scaling between (not just at) the suite's tested extremes. Genuinely fresh session. |
| [VR-0004](VR-0004-bad-zone-detection.md) | IP-0004 | 2026-07-21 | ✅ VERIFIED | Bad-zone detection (dissonance/stuck/overload/combined); 60/60 tests, non-default scale (`SCALE_IDX=3`, pentatonic) independently driven live. One new doc-coherence finding: `FR-1090` still names an unbuilt `HIST_PA/PB/WV` ring buffer — extends `BL-0013`'s existing scope up to the requirements layer. |
| [VR-0005](VR-0005-full-reset-scope.md) | IP-0005 | 2026-07-21 | ✅ VERIFIED | Full reset-to-preset (audit-only package, no new code); 60/60 tests, pulse B/wave/noise Select-reset coverage independently driven live, closing the `BL-0014` gap the package doc itself flagged. |
| [VR-0006](VR-0006-minimal-visualizer.md) | IP-0006 | 2026-07-21 | ✅ VERIFIED | Minimal visualizer (channel-activity tiles + bad-zone palette); 60/60 tests, palette/bad-zone reaction independently confirmed live via rendered pixels (one-frame VBlank lag investigated and correctly explained, not a defect). New finding `BL-0016`: `FR-1120` still claims tempo representation the shipped visualizer doesn't build. |
| [VR-0007](VR-0007-autonomous-recovery-and-randomize.md) | IP-0007 | 2026-07-21 | ✅ VERIFIED | Autonomous bad-zone recovery + Select randomize; 60/60 tests, dissonance pull-toward-tonic independently confirmed (51/51 deltas = -1 following a DISSONANT onset). **New Medium-High finding `BL-0017`**: `OVERLOAD_THRESHOLD=20` is mathematically unreachable (ceiling ~8.8 onsets/window even at max tempo+density) — `FR-1100`/the overload-recovery doubling are code-complete but permanently dead under current constants. Completes independent verification of the full 7-package tranche. |
| [VR-1060](VR-1060-arpeggio-and-duty-cycle.md) | IP-1060 | 2026-07-25 | ✅ VERIFIED | Arpeggio + duty-cycle variation; 65/65 tests, non-default `OCTAVE_IDX=3`/`SCALE_IDX=2` independently re-driven live, 8200-frame stress run clean. Low-Medium doc-coherence finding: package doc's own text still describes a stale 3-entry/mod-3 arpeggio table; shipped code and GDS-07 both correctly document the actual 4-entry/mod-4 design. |
| [VR-1061](VR-1061-vibrato-and-portamento.md) | IP-1061 | 2026-07-25 | ✅ VERIFIED | Vibrato + portamento; 65/65 tests, non-default `TEMPO_IDX=7` (max)/`OCTAVE_IDX=3` independently re-driven live, 8200-frame stress run clean. Two Low-Medium findings: (1) package doc's stale `VIBRATO_OFFSETS`-table/glide-counter design text vs. the shipped inline-±1/zero-WRAM design; (2) portamento (`FR-1150`) has no dynamic register-level test (write-only PSG registers, verified by code review only) and sits outside `NFR-1020`'s literal FR-range coverage — a real, disclosed testability gap, not a functional defect. |
| [VR-9010](VR-9010-channel-mix-gating.md) | IP-9010 | 2026-07-25 | ✅ VERIFIED | Channel-mix gating (`BL-0019`); 77/77 tests, independent non-default live drive at preset 5 (`0b1100`, wave+noise only — different from the suite's own preset-3 fixture) confirmed pulse A/B silenced, wave/noise active, and full re-inclusion after wrapping back to preset 0, plus an 8200-frame stress run clean. Two Low/Low-Medium findings: (1) an undeclared but beneficial one-line `docs/architecture/07-data-model.md` doc-scope addition; (2) a shipped code comment claims a dissonance-scoring-vs-mute design decision was "flagged to the backlog" but no matching backlog entry actually exists. |
| [VR-9020](VR-9020-overload-threshold-recalibration.md) | IP-9020 | 2026-07-25 | ✅ VERIFIED | Overload threshold recalibration (`BL-0017`); 77/77 tests, independent non-default live drive at `TEMPO_IDX=5`/`DENSITY_IDX=6` (a distinct realistic-high, non-maximal combination from the suite's own `TEMPO_IDX=6`/`DENSITY_IDX=5` fixture) confirmed OVERLOAD reachable (peak onset count 9) and the default preset non-spurious (peak onset count 6, matching the package's own documented empirical measurement exactly), plus an 8200-frame stress run clean. One Low finding: an undeclared but beneficial `Claude.md` test-count/doc-scope addition. |
| [VR-1070](VR-1070-combinable-generation-schemes.md) | IP-1070 | 2026-07-25 | ✅ VERIFIED | Combinable generation schemes (`BL-0020`); 85/85 tests, independent non-default live drive at `DENSITY_IDX=5`/`TEMPO_IDX=6`/preset 6 (wave on Scheme E) — a combination T14's own fixture never varies — confirmed onset-rate matches the Euclidean prediction (497/995 ≈ 0.499 vs. k/n=0.500), plus a separate mid-note `CHMIX_IDX` switch scenario independently confirming `FR-1190`'s "next onset, not mid-note" edge, plus bad-zone entry/recovery at this combination. **New Medium finding**: `CHMIX_MASKS` assigns Scheme E to exactly one preset (wave only) — pulse A/B's Scheme-E code path is fully wired and correct but unreachable via any shipped preset, so `BL-0020`'s "solo or in combination" ask is only partially realized in shipped data. One Low finding: a self-inflicted, self-corrected test-methodology bug in this run's own drive script (recorded for future verifiers). |
| [VR-1080](VR-1080-genre-aware-style-presets.md) | IP-1080 | 2026-07-26 | ✅ VERIFIED | Genre-aware style presets (roadmap R5); 93/93 tests, independent live drive: exact-press-frame style application at non-sequential preset order (3→1→2, distinct from T15's own sequential/settled-read walk), `DUTY_BIAS`'s add-then-wrap composition confirmed directly in the real `NR11` duty register, a rapid double-Start edge case, a manual-D-pad-steering-then-overwrite scenario (FS-108 workflow #2), and a 6000-frame randomized-input stress run (712 style changes, no hang). ROM budget independently re-measured (3497 used/29271 free) matching the package's own claim exactly. **New Medium finding**: FS-108's bad-zone-independence acceptance criterion states an absolute invariant ("a style change does not alter bad-zone WRAM") that the code only guarantees narrowly (the style-change write itself never touches it) — a broader randomized sweep found bad-zone fields DO coincidentally change on ≈16% of style-change frames, due to unrelated channels' independent onset timing, not a code defect. One Low finding: informational ROM-budget re-confirmation (no discrepancy). |
| [VR-1090](VR-1090-motif-recurrence-via-weighted-variant-selection.md) | IP-1090 | 2026-07-26 | ✅ VERIFIED | Motif recurrence via weighted variant selection (`BL-0010`/`ADS-102`); 102/102 tests (T1-T16), independent live drive: reconstructed the exact motif-cycle-boundary frame list live (a clean, no-Start-press pass) and used it to force a *guaranteed* exact-frame collision with an `IP-1080` style change (S1) — stronger than the shipped `T16.8`'s own 47-frame interval, which this run found never actually produces a same-frame collision within its own 4000-frame window; also independently swept tight 2-12-frame periodic Start-press intervals (S2) and drove an extended 60,000-frame sample of the retention/switch weighting (19.7% switch rate, consistent with the designed 25%). ROM budget independently re-measured (3679 used/29089 free, -182 bytes) matching the package's own claim exactly. **Two Medium findings**: (1) the shipped `T16.6`'s retention-bias assertion uses only an 11-event sample and is nearly powerless to detect a wrong weighting table; (2) the shipped `T16.8`'s own chosen interval never actually produces the same-frame collision its name/DoD claim to cover, though the underlying engine behavior is correct (confirmed independently by this run's S1/S2). One Low-Medium finding: the committed `Driftune.gbc` binary was not refreshed by `IP-1090`'s own implementing commit (unlike every prior package). One Low finding: a stale, unrelated `docs/features/INDEX.md` status row still describing `FEAT-1090` as not yet built. |
| [VR-1100](VR-1100-song-form-via-autonomous-phase-cycling.md) | IP-1100 | 2026-07-26 | ✅ VERIFIED | Song-form via autonomous phase cycling (roadmap R6/`ADS-103`); 112/112 tests (T1-T17), independent live drive: independently re-derived the 4-phase transition-frame sequence from a clean boot (not trusting `T17.6`'s own frame number), then forced a *guaranteed* same-frame Start-press/phase-transition collision at all three cycle-internal boundaries (INTRO→BUILD, BUILD→PEAK, PEAK→BREAKDOWN) — the shipped `T17.6` exercises only the first; song-form's write won cleanly at every boundary. Also empirically compared `OVERLOAD` frequency across all 4 phases over 40,000 frames: BUILD (tempo=4/density=4) showed 0.000% `OVERLOAD`, statistically indistinguishable from the default/INTRO preset's own 0.000% — the package's own named BUILD/`OVERLOAD` risk did not materialize — while PEAK (tempo=6/density=6) showed a materially higher 21.0% `OVERLOAD` rate, a previously-unmeasured data point for `09-content-review`'s musical judgment. ROM budget independently re-measured (3791 used/28977 free, -112 bytes) matching the package's own claim exactly. One Low-Medium finding: `T17.6` only tests the first boundary collision, not all three (though the underlying routine is boundary-agnostic by construction, confirmed independently). One Low finding: the empirical `OVERLOAD` data above, filed for `09-content-review`. One Low finding: a stale `docs/features/INDEX.md` `FEAT-1100` row, corrected in-place by this VR. |
| [VR-1110](VR-1110-settings-and-control-visibility.md) | IP-1110 | 2026-07-26 | ✅ VERIFIED | Settings & control visibility (`BL-0051`/`ADS-104`); 122/122 tests (T1-T18). Independently re-derived both disclosed findings from scratch rather than trusting the shipped suite alone: (1) a fresh 3000-frame, zero-button-input stress run confirmed `CHANNEL_CELLS` tracks `NR52` with **0 mismatches**, directly re-confirming the reverted early-call-site regression is genuinely absent from the shipped (last-position) code; (2) a frame-by-frame Select-press probe across **3 distinct pre-Select button sequences** confirmed the narrower, self-healing display-lag finding exactly as disclosed (WRAM resets on the exact Select frame, the settings-cell display is provably stale that same frame, and corrects itself exactly one frame later) — reproduced reliably in all 3. Also independently drove a non-default scenario beyond `T18`'s own single-tap fixture: 6 consecutive Start taps stepping `CHMIX_IDX` through presets 1-6 and 10 consecutive D-pad Up taps exercising `TEMPO_IDX`'s full 7→0 wraparound, every intermediate indicator value correct. ROM budget independently re-measured (4240 used/28528 free, +449 bytes) matching the package's own claim exactly. Three Low findings: (1) the package's own `Documentation Updates` field named `docs/pipeline/backlog.md`'s `BL-0051` row as needing an update on completion — not actually done by the implementing commit; (2) stale `docs/features/INDEX.md`/`01-feature-catalog.md` `FEAT-1110` rows, corrected in-place by this VR; (3) the shipped Select-frame-lag test sequence (`T18.8`-`T18.10`) exercises only one pre-Select button combination, though this run's own independent drive confirms the behavior holds across others. |
| [VR-1130](VR-1130-genre-blending.md) | IP-1130 | 2026-08-09 | ✅ VERIFIED | Genre Blending (roadmap R8/`FS-113`); re-verification pass against remediation commit `f6fd243`. 154/154 tests green, ROM builds correctly, ROM budget independently re-measured (4477/28291). Independently re-derived the F1 remediation's core claims rather than trusting the commit: hand-derived a genuine mid-blend value at `BLEND_STEP=3`/preset 1→2 (a value+pair `T21.3b` itself doesn't cover) matching the formula exactly; confirmed `BLEND_STEP`'s boot/Select init fix against a side-by-side rebuild of the pre-remediation commit, which shows the previously-undiagnosed boot-time spurious mini-blend genuinely existed; re-confirmed the mid-blend-restart contract (`FR-1490`) via a 4th independent construction. **Three non-blocking findings, none a functional defect**: (1) Medium — `hook_register` instruction tracing shows the remediation's disclosed "press-frame-specific" timing effect is actually a general, ~30-frame-periodic engine-wide characteristic present even with zero blend activity — `FR-1480`'s landing guarantee is unaffected, but the causal attribution in the commit/docs is inaccurate; (2) Low-Medium — the `T17.6` test-change's stated justification (a call-order change) is factually wrong, independently confirmed by rebuilding the pre-remediation commit — the test's *result* is correct, only its reasoning is misattributed; (3) Low — `NFR-1220`'s body text still says 4 new WRAM bytes where the RTM note beside it correctly says 7. Carried forward, still open: the package doc's Risks field still names the stale `N=16` (original F2), untouched by this remediation. |
