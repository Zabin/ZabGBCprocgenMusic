# Pipeline Backlog

| ID | Filed | Type | Summary | Sev/Pri | Entry stage | Disposition | Status |
|---|---|---|---|---|---|---|---|
| BL-0001 | 2026-07-21, run #1 | research-gap | GDS-02 (System Context), GDS-04 (Domain Model), GDS-05/06 (FR/NFR at the GDS-ladder level — superseded in ordering by the direct `04-requirements-engineering` pass already done, but the ladder levels themselves are still unauthored), GDS-08 (Presentation Architecture/visualizer), GDS-09 (Interface Spec), GDS-10 (RTM level) remain `⛔ Planned`. | Medium | 03 | `DONE` 2026-07-26 (run #97) — **the GDS ladder is complete.** Drained level by level across runs #92-#97: GDS-02 (System Context), GDS-04 (Domain Model), GDS-06 (Non-functional Requirements), GDS-08 (Presentation Architecture), GDS-09 (Interface Specification), GDS-10 (RTM level), each with its merge gate closed. **GDS-05 is recorded as deliberately not owed** rather than outstanding (rationale in [GDS-06 §0](../architecture/06-non-functional-requirements.md) — the leaf `FR` baseline already covers the capability set). This was the pipeline's oldest backlog entry, open since run #1. GDS-08 (visualizer) is now especially ripe since `IP-0006`+`IP-1110` have both shipped; GDS-09's absence has been flagged by every `FS-1xx` authored so far. | DONE |
| BL-0004 | 2026-07-21, run #1 | gate | `IP-0001` was built and self-tested in the same session/agent that authored it, not verified independently. Per this project's own adopted convention (mirroring the reference project's "verification ideally in a fresh session" practice), it is `COMPLETE` but not `VERIFIED`. | Medium | 09 | `DONE` — run #5: the user explicitly authorized "accept single session limitations just this once" to reach an MVP. `09-package-verification` re-ran on `IP-0001` this session with that exception recorded ([VR-0001](../implementation/verification/VR-0001-skeleton-and-single-channel-generation.md)) — VERIFIED. This is a one-time exception, not a standing waiver of the independence rule for future packages. | DONE |
| BL-0005 | 2026-07-21, run #1 | design-question | Preset table values (tempo BPM steps, octave root Hz values, scale semitone sets, Euclidean density k/n pairs — the last not yet implemented) and bad-zone thresholds (dissonance/stale/overload) are first-guess placeholders (GDS-03 SS3/SS4 say so explicitly), not yet tuned by ear. **Update, run #3:** the dissonance weight table specifically is now literature-grounded (Helmholtz roughness ordering), not just plausible-by-taste — see [R204 SS5](../research/encyclopedia/R204-bad-zone-detection-heuristics.md); the *tempo/octave/scale/density* preset values are still untuned placeholders. | Medium | 08 (content-authoring) / 09 (content-review) | `DEFERRED` — revisit trigger: once `IP-0002`/`IP-0003` land (pulse B/wave/noise all present) and there's enough sound surface for a real listening pass to be meaningful; tuning single-channel pulse A alone isn't representative. R204's grounded table is the starting point for that pass, not a substitute for it. | NEW |
| BL-0006 | 2026-07-21, run #1 | feature | No FS-xxx (Feature Specification, stage 06) has been authored yet for any of FEAT-1000...1050 — the pipeline moved from the feature catalog straight to implementation planning for `IP-0001`'s narrow slice rather than writing full 20-field specs first. | Medium | 06 | `DONE` 2026-07-26 (run #99) — dispositioned as a **deliberate deviation, not owed work**, jointly with `BL-0012`. Full reasoning in `docs/features/INDEX.md`'s Backfill decision: the GDS ladder's completion (runs #92-#97) now covers every field an `FS-100`-`FS-105` would carry, field by field with a maintained owner for each; six new documents would restate that content and then need syncing with all of it. Reversible, with a named re-trigger. Original: **run #5 update**: the user authorized proceeding straight to an MVP this session; abbreviated (not full 20-field) FS notes were written per-package instead of formal FS-1xx documents, tracked as `BL-0012`. Full FS-1xx authoring is deferred, not abandoned — see `BL-0012`. | DONE |
| BL-0007 | 2026-07-21, run #1 | design-question | Working title "Driftune" is explicitly open to revision (MSTR-001 SS1 flags it as such) — no user confirmation has been sought yet. | Low | 01 | `DEFERRED` — revisit trigger: surface it to the user the next time `01-vision` is naturally revisited (e.g. alongside BL-0001's GDS-08 visualizer work, where a name might matter more concretely), rather than interrupting this session's flow for a naming call alone. **RESOLVED 2026-08-07 — the user was asked directly and chose to keep "Driftune" as the final product name.** No rename needed anywhere; the ROM header, build output and the whole docs tree already use it. | DONE |
| BL-0008 | 2026-07-21, run #3 | finding | [R207](../research/encyclopedia/R207-gb-chiptune-channel-idioms.md) (GB-era chiptune channel-usage idioms) found that GDS-03's current plan would give the wave channel the same melodic scale-walk treatment as pulse B, undifferentiated — real GB chiptune convention (LSDJ/GB Studio) treats the wave channel as a distinct bass/timbre voice, not a second identical lead. | Medium | 06 (feature specification, `IP-0002`'s eventual FS) | `DONE` — run #5: `IP-0002` implemented the wave channel with a distinct octave anchor (one index lower, floored at 0) and half note-rate, per R207 SS5's concrete recommendation. Confirmed working via `test_rom.py` T6 (independent CUR_DEGREE_WV walk). | DONE |
| BL-0009 | 2026-07-21, run #3 | doc-defect | Run #1's research pass authored three monolithic per-tier files (`R100`/`R200`/`R300-*.md` directly under `docs/research/`) before reconciling against the adapted `02-research-*` skills' own expected structure (`docs/research/encyclopedia/R1xx`/`R2xx`/`R3xx`, one topic per file, 7-section shape, inline citations). | Medium | 02 | `DONE` — run #3 split the load-bearing content into 10 new encyclopedia topics (R107-R110, R201-R207 except R203 was new not split, R301, R305) with real citations (Helmholtz/psychoacoustic sources, Toussaint's Euclidean rhythms paper, GB chiptune convention sources, PyBoy's own docs, generative-music history), superseded the three original files in place (retained, not deleted), and updated `docs/research/INDEX.md` to the new structure. | DONE |
| BL-0010 | 2026-07-21, run #4 | research-gap | [R211](../research/encyclopedia/R211-melodic-harmonic-generation-techniques.md) SS3 and [R212](../research/encyclopedia/R212-form-tension-and-macro-musical-parameters.md) both independently confirm the same real gap from different angles: no SM83-tractable technique was surfaced this pass for genuine phrase/motif development or composed song-structure/cadence/key-modulation — Driftune currently generates texture and variation, not composed musical form. R214's L-system idea (SS5) is the most promising *specific* candidate, not yet evaluated in depth. **2026-07-22 update:** [R220](../research/encyclopedia/R220-style-evolution-and-song-form-structure.md) (MSTR-001 §9 thread) found a cheap, concrete partial answer for the *song-form-structure* half of this gap — horizontal-resequencing/vertical-layering technique, a state machine driving Driftune's existing tempo/density/scale parameters through envelopes, no new composition needed. The *motif-recurrence* half (L-systems, R214 SS5) remains open, deliberately not solved by R220 (a materially harder problem) — this entry now tracks only that narrower remainder. **2026-07-26 update:** deep-evaluated — [R214](../research/encyclopedia/R214-grammar-and-cellular-generation-survey.md) §8 finds probabilistic L-systems (McCormack) are the concretely-cited approach, confirms real compositional precedent for exactly this application (Kyburz's "Cells," L-system-rewrite-driven motif *selection*), and surfaces a load-bearing constraint (Worth & Stepney: melody quality degrades at longer derivations, "short... still interesting" is the target shape) — the correct design is a small, fixed-depth, weighted-rule table re-applied per recurrence event, not a growing derivation string; structurally close to `IP-1070`'s already-shipped `MOTIF_TABLE`. | Medium | 02→03→04→05→06→07→08→09, all complete | `DONE` — **2026-07-26 update:** `IP-1090` independently `VERIFIED` via [VR-1090](../implementation/verification/VR-1090-motif-recurrence-via-weighted-variant-selection.md) (fresh-session `Agent`, isolated worktree, merged). `MOTIF_TABLE` extended to 4 pre-composed variants selected via a weighted lookup at cycle boundaries, no L-system derivation engine — satisfies R214 §8's bounded-depth constraint end to end, research through independent verification. This entry's motif-recurrence half is fully closed; `BL-0044`/`BL-0045` (test-strength findings from `VR-1090`) and `BL-0043` (deferred scope questions) are separate, non-blocking follow-ons, not part of this entry's own remaining scope. | DONE |
| BL-0011 | 2026-07-21, run #4 | feature | Four concrete, cited generation-technique upgrade candidates surfaced by the 115-topic research pass: (1) arpeggio-as-polyphony on pulse channels ([R216](../research/encyclopedia/R216-sound-design-techniques.md) SS5); (2) `DIV`-register-seeded boot LFSR instead of a fixed seed, for a genuinely different sequence each power-on ([R213](../research/encyclopedia/R213-prng-seed-management.md) SS5); (3) session-length-adaptive drift, Spore/"The Shuffler" precedent ([R218](../research/encyclopedia/R218-inspiration-and-history-survey.md) SS5); (4) bytebeat-style procedurally-generated Wave RAM tables instead of only picking among precomputed shapes ([R218](../research/encyclopedia/R218-inspiration-and-history-survey.md) SS5). | Low-Medium (post-v1 polish, none blocking the Foundation bucket) | 00 (each should be individually triaged via `00-intake` when picked up, not bulk-authorized here) | **Run #6 update:** item (2) `DONE` — `IP-0007` implemented `DIV`-based reseeding on boot/Select (driven by the user's own reset/randomize directive, not a separate intake). **2026-07-22 update:** item (1) (arpeggio) superseded by `BL-0024` (user-directed R216 implementation push, broader scope covering arpeggio + vibrato + portamento + duty-cycle) — tracked there now, not duplicated here. Items (3), (4) remain `DEFERRED` — revisit trigger: a post-`BL-0024` intake pass. | NEW |
| BL-0012 | 2026-07-21, run #5 | doc-defect | The user directed "iterate the pipeline through to an MVP ROM, accept single-session limitations just this once" — `07-implementation-planning`'s packages for `IP-0002`-`IP-0006` were authored with abbreviated (not full 20-field) FS-equivalent notes inline on each `IP-xxxx` doc, rather than separate formal `06-feature-specification` `FS-1xx` documents, to move at the pace the user's directive required. | Medium | 06 | `DONE` 2026-07-26 (run #99) — dispositioned as a **deliberate deviation** jointly with `BL-0006`; see that entry and `docs/features/INDEX.md`'s Backfill decision for the field-by-field reasoning. The INDEX rows now point at where each feature's design content actually lives rather than reading as six outstanding documents. Original: **run #6 update**: `IP-0007` (autonomous recovery) continued under the same abbreviated-notes convention. A future session should author the proper `FS-100`...`FS-105` documents retroactively from the shipped `IP-0002`-`IP-0007` packages (an as-built-style backfill, same pattern the reference project used for its own bootstrap), closing this alongside `BL-0006`. | DONE |
| BL-0013 | 2026-07-21, run #5 (scope extended run #9, VR-0004) | doc-defect | `IP-0004`'s implementation deviates from two of GDS-03/R204's original proposals: (1) stale/repetition detection is period-1 only, not the period-1-or-2 GDS-03 SS4b described, and doesn't use GDS-07's reserved ring-buffer fields; (2) dissonance scoring uses a 7-entry interval-class table (inversions folded) rather than R204 SS5's literal 12-entry proposal. Both are documented, deliberate MVP simplifications, not oversights. Run #9 (`VR-0004`) extended this to `FR-1090`, which also still names the unbuilt ring buffer. **Run #19 update (`03-architecture-design-synthesis`):** the architecture half is done — GDS-03 §4a/§4b and GDS-07 §3/§4 now both carry dated reconciliation notes describing the shipped design and explicitly marking the fuller versions as a named v2 upgrade path with no fired trigger. `R204` SS5 (`02-research-game-design`) and `FR-1090`'s text (`04-requirements-engineering`) remain outstanding — out of this skill's write-scope, named for their owners rather than touched directly. | Low-Medium (no functional defect — the simplifications are sound and tested; this is a doc-coherence gap) | 03 (architecture, done) / 02 (research, still owed) / 04 (requirements, still owed) | `SCHEDULED` — R204 SS5 and `FR-1090`'s wording still need the same reconciliation GDS-03/GDS-07 just received; ride either owning skill's next natural touch of those documents. | IN PIPELINE |
| BL-0014 | 2026-07-21, run #5 | doc-defect | `IP-0005`'s own package doc flags a light verification gap: pulse B/wave/noise's Select-reset coverage is implicitly exercised (via T6/T7's drift patterns) but not independently asserted post-Select the way T5/T8 do for pulse A and bad-zone state. No evidence of an actual defect — `init_engine`'s code was read and audited as covering every relevant field. | Low | 09 (a future verification pass, or a small `test_rom.py` addition) | `DONE` — run #10 (`VR-0005`): independently drifted pulse B/wave/noise state, pressed Select, confirmed `CUR_DEGREE_PB`/`CUR_DEGREE_WV`/`NOISE_STEP_IDX`/timers all reset correctly on the exact reset frame (the per-channel `STALE_COUNT_*` variance observed is explained by `IP-0007`'s independent randomized reseeding, not a defect). | DONE |
| BL-0015 | 2026-07-21, run #6 | finding | A rare (~0.1% of frames), self-healing one-frame transient was found where `BAD_ZONE_FLAGS` bit3 (COMBINED) can read one frame behind bits0-2 during `IP-0007`'s testing — e.g. bit0 set while bit3 momentarily still clear, corrected the very next frame. Static review of `_emit_badzone_tick` (`music_engine.py`) shows the combined-bit computation is logically correct and unconditional every call; the exact root cause (plausibly a PyBoy `tick()`-boundary/CPU-execution-granularity artifact rather than a logic bug) was not chased down further given the severity — it affects one frame of the visualizer's palette choice only; the autonomous recovery logic itself reads bits0-2 directly, not bit3, so it is unaffected. | Low (cosmetic, self-healing, no behavioral impact found) | 09 (a future verification/investigation pass) | **Revisit trigger FIRED and largely resolved, 2026-07-31 (run #102).** The entry's own hypothesis — "plausibly a PyBoy `tick()`-boundary/CPU-execution-granularity artifact rather than a logic bug" — is now **confirmed**, by the deeper investigation the trigger anticipated. `tick(1)` advances exactly one ROM frame but returns at a point *inside* that frame's work, after `apply_input` and before `update_visuals` completes, so any read comparing a WRAM field against its derived value samples two different moments. Recorded in `R305` §3 as a standing test-design hazard and in `GDS-02` §7. Remaining open: the interrupt-*latency* half, which this investigation did not measure. `DEFERRED` on that narrower question only. | DEFERRED |
| BL-0016 | 2026-07-21, run #11 (`VR-0006`) | doc-defect | `FR-1120` requires the visualizer to "represent tempo, per-channel activity, and bad-zone status" — the shipped `visuals.py` (`IP-0006`) implements only per-channel activity and bad-zone status; `TEMPO_IDX`/tempo is never read anywhere in the file (confirmed by grep, zero matches). The package doc's own "Explicit non-scope" section already names "tempo-synced motion" as an unbuilt, unscheduled follow-up, so this is a knowingly-scoped-down MVP simplification, not an oversight — but `FR-1120` itself still describes the full three-signal version. Same pattern as `BL-0013`/`FR-1090` (doc says more than the shipped simplification), one layer up at the requirements text this time for a different feature. No functional defect. | Low-Medium (doc-coherence only, no functional impact) | 04 (requirements) / 03 (architecture, GDS-08 still pending per `BL-0001`) | `DONE` 2026-07-26 (run #98) — `04-requirements-engineering` implemented GDS-08 §7's resolution: `FR-1120` reworded to cover only per-channel activity and bad-zone status, and the tempo-synced-motion claim split out as **`CR-0001`**, this project's first Candidate Requirement. Original: **run #95 update ([GDS-08 §7](../architecture/08-presentation-architecture.md)) resolved this at design altitude, three parts**: (1) `TEMPO_IDX` *is* now represented — `IP-1110`'s first indicator bar — so "tempo is absent from the display" is out of date; (2) tempo-**synced motion** (a visual element whose timing follows the beat, which is how `FR-1120`'s wording reads naturally alongside "per-channel activity" and in `R205`/`R223`'s sync context, and which `IP-0006`'s own package doc named as its explicit non-scope) does **not** exist and remains unbuilt/unscheduled; (3) `FR-1120` is therefore **satisfied on a narrow reading and overstated on the natural one**. Recommended fix: split the wording — keep the setting-display claim (now true, traced to `FR-1350`), move the synced-motion claim to an explicitly-unbuilt candidate. Still owed to `04-requirements-engineering`; a ladder level does not edit the FR baseline. Original disposition: reword `FR-1120` the next time `04-requirements-engineering` touches the FR document, alongside `03-architecture-design-synthesis`'s still-pending GDS-08 authoring (`BL-0001`). | DONE |
| BL-0017 | 2026-07-21, run #12 (`VR-0007`) | bug | `OVERLOAD_THRESHOLD=20` (`music_engine.py`, `ONSET_WINDOW_FRAMES=32`) is mathematically unreachable: independently computed the engine's own onset-rate ceiling at the *maximum* tempo (`TEMPO_TABLE[7]=20`) and *maximum* density (`DENSITY_K[7]=12`) simultaneously — `≈8.8` onsets per 32-frame window, well under the threshold of `20`. Empirically confirmed: `BAD_ZONE_FLAGS` bit2 (OVERLOAD) has never been observed set anywhere across this session's testing (`VR-0004`'s live drive, T8's 2000-frame runs, T10's 8000-frame stress evidence, and a dedicated 6000-frame max-density drive in `VR-0007`). This means `FR-1100` (OVERLOAD detection) is code-complete but permanently unreachable, and `IP-0007`'s own overload-driven reload-doubling recovery logic is consequently dead code that can never execute under any valid tempo/density combination — not merely untuned at the default (which is all `BL-0005` currently says), but structurally unreachable everywhere. No test currently asserts OVERLOAD triggering, so this was undetected until `VR-0007`'s non-default-parameter live drive. The dissonant/stuck recovery paths (the ones most likely to matter perceptually) are independently confirmed working and unaffected. | **Medium-High** (a documented requirement/feature is silently non-functional under all reachable engine states — real functional gap, not cosmetic; no crash/data-corruption risk) | 07 (implementation planning — needs an actual remediation package, e.g. `OVERLOAD_THRESHOLD` recalibrated to roughly `5`-`6` given the computed `~8.8` ceiling, or `ONSET_WINDOW_FRAMES` widened) / 08 (code-implementation, to build the recalibration) | `SCHEDULED` — author a small remediation Implementation Package (via `07-implementation-planning`, entering as a code fix, not a doc-only change like `BL-0013`/`BL-0016`) to recalibrate the overload threshold/window so bit2 becomes reachable under realistic play, then re-verify via `09-package-verification`. Should be weighed before or alongside `10-integration-review`, since it's a real functional gap in a feature the release plan already counts as delivered. **Run #33 update:** G3 granted, `IP-9020` built this session. VR-0007's own average-rate ceiling formula (~3.17 at default, ~8.8 at max) turned out to *understate* the real peak a window-aligned counter sees — empirically measured actual peaks instead (default preset peaks at 6, not ~3; a realistic-high tempo=6/density=5 combination peaks at 8; absolute max peaks at 11) and recalibrated `OVERLOAD_THRESHOLD` to `7` (triggers at count>7) on that empirical basis, not the analytical formula alone — a refinement worth noting for any future threshold work. New `test_rom.py` T13 suite (2 checks: reachable at realistic-high, not spurious at default) — 77/77 full suite, 8200-frame default-preset stress run confirms zero spurious triggers. `COMPLETE`, not yet `VERIFIED` — independent verification needs a future fresh session. **`09-package-verification` update (`VR-9020`, fresh session, 2026-07-25):** independently re-verified — 77/77 full suite (T1-T13); independently drove the built ROM live to `TEMPO_IDX=5`/`DENSITY_IDX=6` (a distinct realistic-high, non-maximal combination from the suite's own `TEMPO_IDX=6`/`DENSITY_IDX=5` fixture) and confirmed OVERLOAD reachable (peak onset count 9 within 2000 frames), plus independently re-confirmed the default/sparse preset does not spuriously trigger it over an independent 2000-frame run (peak onset count 6, matching the package's own documented empirical measurement exactly); 8200-frame stress run clean. `VERIFIED` (`docs/implementation/verification/VR-9020-overload-threshold-recalibration.md`). One Low finding filed in the VR (an undeclared but beneficial `Claude.md` test-count/doc-scope addition — filed as `BL-0028`). `BL-0017` flipped to `DONE` by the pipeline manager, run #38. | DONE |
| BL-0018 | 2026-07-21, run #13 (`10-integration-review`, Foundation bucket) | doc-defect | Six WRAM addresses live in the shipped ROM — `LFSR_STATE_PB`/`LFSR_STATE_WV` (`0xC017`/`0xC018`, added by `IP-0002`), `NOISE_STEP_IDX` (`0xC019`, `IP-0003`), `SEMI_PA`/`SEMI_PB`/`SEMI_WV` (`0xC01A`-`0xC01C`, `IP-0004`) — appear nowhere in `docs/architecture/07-data-model.md` (GDS-07), added incrementally across three packages, none of which updated the architecture doc. No collision with GDS-07's own reserved-but-unused `HIST_HEAD_*`/`HIST_*` range (`0xC013`-`0xC015`, `0xC020`-`0xC037`, already tracked by `BL-0013` from the opposite direction — documented-but-unused, not undocumented-but-used). `memory.md`'s own WRAM quick-reference already lists all six, so the gap is specific to the formal architecture document. No functional defect — this is a single-package-verification blind spot: no individual VR checks GDS-07 completeness against the whole tree, only integration review has that vantage point. | Low-Medium (doc-coherence only, no functional impact) | 03 (architecture) | `DONE` — run #19 (`03-architecture-design-synthesis`): all six addresses now documented in GDS-07 §3, each tagged with the package that added it and this entry's ID. | DONE |
| BL-0019 | 2026-07-21, run #13 (`10-integration-review`, Foundation bucket) | bug | `CHMIX_IDX` ("channel-mix," the Start-button-controlled parameter, `0xC004`) is correctly stepped by input and correctly reset to its preset — but has **no consumer anywhere in the shipped code**. Grepped the entire tree: no channel's generation or PSG register writes are ever gated/muted/conditioned on `CHMIX_IDX`'s value, and no channel-mix preset table (analogous to `TEMPO_TABLE`/`DENSITY_K`) exists at all. All four channels always play regardless of what Start does. `IP-0001`'s own package doc explicitly scoped this as a promise ("`DENSITY_IDX`/`CHMIX_IDX` are wired... but have no consumer until `IP-0002`/`0003`") — `IP-0003` kept the `DENSITY_IDX` half of that promise but **no package from `IP-0002` through `IP-0007` ever wired `CHMIX_IDX`**, and none of the seven independent per-package `VR-000x` verifications caught the gap (each audited only its own package's scope). `FR-1000`/`FR-1010` (traced by `FEAT-1000`, shown fully delivered on the Master Build Plan/Feature Catalog) both presume this gate functions. Already informally flagged in `Claude.md`/`memory.md` ("channel-mix wired but not yet consumed") but never formally routed through the pipeline — zero prior backlog entries reference it, despite `Claude.md`'s own "Known Issues" section claiming the backlog is the live, non-stale list. | **High** (a fully-specified, requirements-traced, player-facing control is completely non-functional, silently dropped across five packages' worth of development; no crash/corruption risk, but a real, user-visible gap between documented and actual behavior) | 07 (implementation planning — author a remediation package wiring `CHMIX_IDX` to a real channel-activity-mask table and gating each channel's generation/register writes on it, per GDS-03's original design intent) / 08 (code-implementation) / 09 (re-verification) | `SCHEDULED` — should be remediated and re-verified before `11-release-readiness` GO is considered for this bucket, per the integration review's own recommendation; needs a fresh `07`→`08`→`09` loop like any other code-scoped finding, with `08`'s own G3 authorization gate applying when reached (not yet attempted). **Run #33 update:** G3 granted, `IP-9010` built this session — `CHMIX_MASKS` (8-entry, bit0=pa/bit1=pb/bit2=wv/bit3=nz) now consumed by `_emit_channel_gen`/`_emit_noise_gen`; excluded channels' DAC/envelope forced off (real `NR52` inactivity within one note-cycle, not just stopped retriggering); new `test_rom.py` T12 suite (5 checks) confirms exclusion + re-inclusion; 73/73 full suite; 8200-frame stress run clean. `COMPLETE`, not yet `VERIFIED` — independent verification needs a future fresh session (this run's own G3-authorized implementation work). **`09-package-verification` update (`VR-9010`, fresh session, 2026-07-25):** independently re-verified — 77/77 full suite (T1-T13); independently drove the built ROM live to preset 5 (`0b1100`, wave+noise only, a different non-default combination than the suite's own preset-3 fixture) and confirmed pulse A/B silenced and wave/noise active in `NR52` over 400 frames, plus full re-inclusion after wrapping back to preset 0; 8200-frame stress run clean. `VERIFIED` (`docs/implementation/verification/VR-9010-channel-mix-gating.md`). Two Low/Low-Medium findings filed in the VR (an undeclared but beneficial doc-scope addition to `docs/architecture/07-data-model.md`; a shipped code comment claims a dissonance-scoring-vs-mute decision was "flagged to the backlog" but no such entry actually exists — recommend a follow-up `00-intake` filing — filed as `BL-0029`). `BL-0019` flipped to `DONE` by the pipeline manager, run #39. | DONE |
| BL-0020 | 2026-07-22, intake (user-filed) | feature | User's ask, as stated: "implement multiple procgen music schemes which can be used either solo or in combinations according to research guidance." Distinct from `BL-0010` (the phrase/song-form composition gap) and `BL-0011` (a bundle of specific *technique* upgrades — arpeggio, adaptive drift, bytebeat wave tables) — this is a request for a **meta-capability**: more than one selectable generation *scheme* (algorithm/approach to producing notes, not just a preset-table value), usable alone or blended/layered in combination, grounded in the research already gathered across `docs/research/encyclopedia/R2xx-*` (e.g. R211 melodic/harmonic generation techniques, R212 form/tension/macro-musical parameters, R214 L-systems, R216 sound design, R202 Euclidean rhythms, R218 inspiration/history survey). The current engine has exactly one generation scheme (LFSR-driven scale-constrained walk per channel, `_emit_channel_gen`) with no concept of alternate/selectable/combinable schemes anywhere in the requirements baseline, architecture, or code — this has no home in the approved FR/GDS baseline today. | Medium (user-stated feature request, no urgency given) | 03 (architecture-design-synthesis) — routed one level upstream of a plain requirements addition because "solo vs. combination" implies real design decisions (how schemes coexist per-channel or per-mix, what a "combination" mixing rule even means on 4 fixed PSG channels, WRAM/preset-space impact) that should be resolved architecturally before `04-requirements-engineering` drafts FRs against them, per intake's own "pick the more upstream stage when unsure" rule | **Run #19 update (`03-architecture-design-synthesis`):** architecture design complete — [ADS-100](../architecture/ADS-100-combinable-generation-schemes.md) proposes one concrete design (a second "Scheme E" — Euclidean-onset + fixed-motif — selectable per pitched channel alongside the existing "Scheme W" walk, riding the not-yet-shipped `CHMIX_IDX` preset space rather than a new control, per [ADR-0001](../architecture/adr/ADR-0001-scheme-selection-rides-chmix-preset-space.md)). Contingent on `IP-9010` shipping first (the design reuses its planned mask-byte layout). | **Run #45 update (`04-requirements-engineering`):** formalized `ADS-100` §5/§6's candidates as `FR-1180`-`FR-1220`/`NFR-1060`/`1070` — `IP-9010`'s contingency confirmed satisfied (shipped bit layout matches `ADR-0001`'s assumption). Delta review found no conflicts. **Run #46 update (`05-feature-decomposition`):** added `FEAT-1070` to the Feature Catalog, no conflicts, right-sized for one implementation package. **Run #47 update (`06-feature-specification`):** authored `FS-107` (full 20-field spec), four Open Questions recorded (data-decisions deferred to planning; a possible dedicated timing NFR; a possible future visualizer scheme-signal, not requested; GDS-09's standing absence). **Run #48 update (`07-implementation-planning`):** authored `IP-1070` (single package, not split), recorded **NOT authorized** — `BL-0020`'s own filing request never carried explicit build-and-ship language, unlike `BL-0024`. **Run #49 update:** user explicitly granted G3 via `AskUserQuestion` ("Yes, authorize and build it"). **`08-code-implementation` built `IP-1070`**: `CHMIX_MASKS` bits4-6 now carry per-channel scheme-select; `_emit_channel_gen`'s note-selection step branches on it (Scheme W unchanged; Scheme E reuses `_euclidean_pattern`/`noise_pattern_table` for onset timing and a new shared `MOTIF_TABLE` for pitch selection); new packed `MOTIF_STEP_PA`/`PB`/`WV` WRAM (`0xC038`-`0xC03A`). New `test_rom.py` T14 suite (7 checks) — 85/85 full suite, 8200-frame stress run clean; independently confirmed live (wave channel's Euclidean step cycles 0-15, motif step cycles 0-7, bad-zone entry/recovery both still work with a Scheme-E channel active). `COMPLETE`, not yet `VERIFIED` — independent verification needs a future fresh session. **`09-package-verification` update (`VR-1070`, fresh session, 2026-07-25):** independently re-verified — 85/85 full suite (T1-T14); independently drove the built ROM live at `DENSITY_IDX=5`/`TEMPO_IDX=6`/`CHMIX_IDX=6` (wave on Scheme E, a combination T14's own fixture never varies) confirming the wave channel's Euclidean step/motif step both cycle through their full ranges and the actual onset rate (497/995 ≈ 0.499) matches the Euclidean-pattern prediction (k/n=0.500) at this non-default density; independently drove a second scenario (a live mid-note `CHMIX_IDX` switch from preset 5 to preset 6) confirming `FR-1190`'s "next onset, not mid-note" edge holds; independently re-confirmed bad-zone entry/recovery with Scheme E active at this combination. `VERIFIED` (`docs/implementation/verification/VR-1070-combinable-generation-schemes.md`). Two findings filed in the VR: (1) **Medium** — `CHMIX_MASKS` assigns Scheme E to exactly one preset (wave only); pulse A/B's Scheme-E code path is fully wired and correct (confirmed by regression checks and code reading) but unreachable via any shipped preset, so this feature's "solo or in combination" ask is only partially realized in shipped data — recommend a follow-up `08-content-authoring` package adding preset(s) that exercise pulse A/B Scheme E and/or multi-channel combinations; (2) Low — a self-inflicted, self-corrected test-methodology bug in this verification run's own independent drive script (recorded for future verifiers, not an engine defect). | `SCHEDULED` — `09-package-verification` on `IP-1070`, fresh session. **Completed 2026-07-25**: see `VR-1070` update above; `IP-1070` flipped `COMPLETE`→`VERIFIED`. `BL-0020` flipped to `DONE` by the pipeline manager, run #50 — new finding filed separately as `BL-0032`. | DONE |
| BL-0021 | 2026-07-22, run #16 (`R208`) | finding | `R208` (palette/color design research) computed approximate perceived luminance for `visuals.py`'s two "on"-tile colors: `CALM_PALETTE` idx3 ≈ 21.7, `BAD_PALETTE` idx3 ≈ 15.9 (0-31 scale) — a real but narrow (~27%) luminance gap. The shipped calm(blue-green)/bad(red) semantic correctly follows the near-universal warning-color convention (R208 §3, cited), and isn't relying on hue alone — but red-green color vision deficiency (affecting a meaningful fraction of viewers, R208 §3 cited) means the gap should be wider to read clearly at a glance, or a second non-color signal (e.g. a distinct tile shape, not just a palette swap) should distinguish the states. Nothing in R205, GDS-03, or the `IP-0006` package doc considered accessibility when the visualizer shipped; `VR-0006` verified hue correctness, not luminance-only legibility. No functional defect — a design-quality gap. | Low-Medium (accessibility/design-quality, no functional impact; real but not urgent) | **run #95 update ([GDS-08 §4.3](../architecture/08-presentation-architecture.md)) addressed this at design altitude**: adopted the principle that **a state distinction should never be carried by colour alone**, and observed that widening the luminance gap (the obvious fix) still leaves colour as the sole channel — so the structurally better answer is a **second, non-colour signal** (a distinct bad-zone *tile shape*), which the tile budget makes affordable (10 of 256 slots used, `R104` §7) and which fits the shape-vocabulary the display already uses for magnitude. Not closed: the actual shape is an `08-content-authoring` judgement and the legibility outcome needs `09-content-review`'s eye. Original routing: 08-content-authoring (a small palette-tuning package, or add a shape-based secondary signal) — grounded, doesn't need further architecture work first | `SCHEDULED` — fold into whatever next touches `visuals.py`'s palette (a future visualizer package, or GDS-08's eventual authoring per `BL-0001`); not urgent enough to warrant its own dedicated package ahead of higher-priority work (`BL-0019`, `BL-0017`). | NEW |
| BL-0022 | 2026-07-22, run #17 (`R304`) | doc-defect | `test_rom.py` T1 asserts the header checksum (`0x14D`, the boot-gating one) but never the global checksum (`0x14E`-`0x14F`) — a latent test-coverage gap, not a functional risk, since the global checksum has no boot-time consequence on real hardware/emulators (`R304` §3, cited). A future regression in `set_header`'s emission logic that broke only the global checksum would go undetected. | Low (test-coverage completeness only; zero functional/boot-time risk) | 08-code-implementation (a one-line `test_rom.py` addition, T1.6) | `DEFERRED` — revisit trigger: the next time `test_rom.py`'s T1 suite is touched for another reason; not worth a dedicated package given the near-zero risk. | NEW |
| BL-0023 | 2026-07-22, run #17 (`R306`) | doc-defect | No `requirements.txt`/`pyproject.toml` exists anywhere in the repo to document the `pyboy` dependency `test_rom.py` requires. Confirmed as a real, recurring friction point, not a theoretical gap: every one of this session's fresh-session `09-package-verification` runs needed an unplanned `pip install pyboy` before the suite could run at all (first hit in run #7, `ModuleNotFoundError: No module named 'pyboy'`). `R306` recommends a minimal manifest pinning the verified version (`pyboy==2.7.0`, per `R301`). | Low-Medium (pure friction, no functional risk — every run has successfully self-recovered by installing on the fly) | 08-refactoring (or a small direct doc/tooling addition — not a behavior change) | `SCHEDULED` — add `requirements.txt` (`pyboy==2.7.0`) the next time any package touches the build/test toolchain, or as a standalone near-zero-risk addition whenever convenient; not urgent enough to interrupt higher-priority work. | NEW |
| BL-0024 | 2026-07-22, intake (user-directed) | feature | User's ask, as stated: "Iterating the pipeline skill run through to implantation the concepts in R216 — Sound Design Techniques: Timbre, Arpeggio, Vibrato, Portamento & Percussion Synthesis. Iterate until they are all in a committed and pushed ROM." Four concrete techniques from [R216](../research/encyclopedia/R216-sound-design-techniques.md), none yet implemented: (1) arpeggio-as-polyphony (cycle a pulse channel's frequency rapidly among 2-3 notes of an implied chord — R216's own "single highest-value technique to add"); (2) vibrato (periodic small pitch modulation, a phase counter + small table); (3) portamento (per-frame frequency glide from old note to new note over N frames, replacing the current instant-onset model); (4) duty-cycle variation (`NR11`/`NR21` currently fixed at 50%, R216 recommends steerable or degree-linked). R216's 5th technique, percussion synthesis, is effectively already satisfied by the shipped `IP-0003` noise implementation (`NR42=0xF2`, fast-decay envelope matching R216's own recommendation) — to be confirmed, not re-implemented, during requirements/spec work. **Folds in and supersedes `BL-0011` item (1)** (arpeggio, open since run #4) — that item is superseded by this broader, explicitly-scoped entry, not duplicated. | High (explicit user directive: "iterate until... committed and pushed") | 04 (requirements-engineering) — no home in the current FR/GDS baseline (the engine's current model is instant-onset, single-note-per-channel; arpeggio/vibrato/portamento are all new per-note-modulation behavior), routed one level upstream of feature-spec per intake's own "no home in the baseline" rule `IN PIPELINE` — manager weighed the filing request itself as G3 authorization (per intake's own note) and carried the item straight through: `04-requirements-engineering` (FR-1130-FR-1170, NFR-1040/1050), `05-feature-decomposition` (FEAT-1060), `06-feature-specification` (FS-106), `07-implementation-planning` (IP-1060/IP-1061, both recorded G3-authorized on this basis), `08-code-implementation` (both built, tested, committed — IP-1060 arpeggio+duty-cycle, IP-1061 vibrato+portamento, 65/65 `test_rom.py` checks, two clean stress runs). Percussion synthesis (R216's 5th technique) confirmed already satisfied by shipped `IP-0003`, no new work needed. Both packages `COMPLETE`, not yet `VERIFIED` — `09-package-verification`'s standing fresh-session-independence rule applies, no waiver given this run. **Run #33 update:** both `IP-1060` ([VR-1060](../implementation/verification/VR-1060-arpeggio-and-duty-cycle.md)) and `IP-1061` ([VR-1061](../implementation/verification/VR-1061-vibrato-and-portamento.md)) independently verified this run — `VERIFIED`, 65/65 tests each, non-default presets re-driven live. `BL-0024` is now fully delivered end-to-end (requirements through independent verification). | DONE |
| BL-0025 | 2026-07-25, run #33 (`VR-1060`) | doc-defect | `IP-1060`'s own package doc (Files to Create/Modify, Implementation Tasks rows) still describes a 3-entry `ARPEGGIO_OFFSETS` table (`e.g. [0, 2, 4]`) cycled mod 3. The shipped implementation uses a 4-entry period-4 up/down table (`[0, 2, 4, 2]`), step index packed as `mod 4` (`AND 0x30`) — a deliberate design choice, correctly documented in GDS-07 §3 and the pipeline journal (run #22), but never folded back into the package doc itself. No functional defect: `FR-1130`'s "2 or 3 notes" requirement is still satisfied (3 distinct pitches: root/+2/+4). | Low-Medium (doc-coherence only, same pattern as `BL-0013`/`BL-0016`/`BL-0018`) | 07 (implementation planning, or whichever stage-08 peer next opens this package doc) | `SCHEDULED` — fold into `IP-1060`'s package doc the next time it's opened for any reason; not urgent enough to warrant its own dedicated touch. | NEW |
| BL-0026 | 2026-07-25, run #33 (`VR-1061`) | doc-defect | `IP-1061`'s own package doc specifies a `VIBRATO_OFFSETS` ROM table and a portamento glide-remaining-counter WRAM field; the shipped implementation instead computes vibrato inline (phase-conditional ±1, no table — SM83 has no ADC/SBC for safe multi-byte arithmetic) and reuses `IP-1060`'s `arp_tick`/`gen_tick` ordering for portamento at zero extra WRAM. Both deviations are correctly documented in GDS-07 §3 and the pipeline journal (run #23) but never folded back into the package doc itself — same pattern as `BL-0025`. No functional defect, confirmed by this run's code-review trace of the actual emitted instruction sequence. | Low-Medium (doc-coherence only) | 07 (implementation planning) | `SCHEDULED` — fold into `IP-1061`'s package doc alongside `BL-0025`'s `IP-1060` fix, same natural touch. | NEW |
| BL-0027 | 2026-07-25, run #33 (`VR-1061`) | finding | Portamento (`FR-1150`) has no dynamic, automated test — `NR13`/`NR14` are write-only (established project-wide limitation since `IP-0001`/`VR-0001`), so its glide behavior can only be confirmed by static code review (traced and confirmed correct this run: old-degree onset retrigger + `arp_tick`-before-`gen_tick` carry-forward). `NFR-1020` ("every shipped behavior has at least one headless PyBoy test") was scoped to `FR-1000`-`FR-1120` and was never extended to cover `FR-1130`-`FR-1170` when those were added by `BL-0024`'s requirements pass — so `FR-1150` sits in a real, previously-unnoticed gap: outside `NFR-1020`'s literal coverage, and not actually testable by any assertion without a new WRAM mirror of the held frequency. Not a functional defect — the underlying logic was independently verified correct — but a real requirements-scoping/test-coverage gap. | Low-Medium (test-coverage/requirements-scoping, no functional risk) | 04 (requirements-engineering — clarify `NFR-1020`'s scope or record an explicit, honestly-named exception, same pattern as `MSTR-001` C10's own exception convention) | `SCHEDULED` — fold into the next `04-requirements-engineering` touch of `NFR-1020`, or accept as a named standing exception; not urgent (no functional risk, logic already independently traced). | NEW |
| BL-0028 | 2026-07-25, run #38/#39 (`VR-9010`, `VR-9020`) | doc-defect | Both `IP-9010` and `IP-9020` made small, accurate, beneficial doc updates (`docs/architecture/07-data-model.md`'s `CHMIX_IDX` row; `Claude.md`'s test-count line and Known Issues bullet) that weren't named in either package's own `Documentation Updates` field — a recurring pattern, not isolated to one package: a package that adds a `test_rom.py` suite will always need `Claude.md`'s test-count line touched, and a package that changes a WRAM table's real behavior will always need GDS-07's row description corrected, whether or not `07-implementation-planning` thought to name them explicitly. No functional or coherence defect in either case — both edits were correct and needed. | Low (process/planning-quality only — every instance so far has been beneficial, not harmful) | 07 (implementation-planning — routinely name `Claude.md`'s test-count line and any GDS-07 row a package's own WRAM/data-table changes touch, in every future package's `Documentation Updates` field) | `SCHEDULED` — fold into `07-implementation-planning`'s own template/checklist the next time it's touched; not urgent, no functional risk. | NEW |
| BL-0029 | 2026-07-25, run #38 (`VR-9010`) | doc-defect | `IP-9010`'s shipped code comment (`music_engine.py`'s `_emit_channel_gen` docstring) states the dissonance-scoring-includes-muted-channels design decision was "flagged to the backlog, not silently decided" — but no matching `BL-xxxx` entry actually exists for it (searched "dissonance"/"chmix"/"muted channel" — no match prior to this entry). The design decision itself is sound and clearly documented in code (each channel's melodic walk keeps evolving while muted, so dissonance scoring tracking that evolution trades a small bounded inaccuracy for musical continuity on re-inclusion), but the traceability claim in the comment was false until now. This entry itself is the fix — recording the decision the code comment already claimed existed. | Low-Medium (a documentation/traceability claim in shipped code didn't match the actual backlog state — same "implementation-outpaced-doc" pattern as `BL-0025`/`BL-0026`) | 00 (intake — this entry closes the gap the code comment named) | `DONE` — this entry is the backlog record `IP-9010`'s code comment referred to; no further action needed unless a future package revisits the dissonance-scoring-vs-channel-mix interaction. | DONE |
| BL-0030 | 2026-07-25, run #40 (`10-integration-review`, 9-package re-review) | finding | A muted pitched channel (pulse A/B/wave, excluded via `CHMIX_MASKS`) still increments `ONSET_WINDOW_COUNT` every note cycle — identical to when included — because `_emit_channel_gen`'s onset-window increment runs *before* `IP-9010`'s channel-mix gate check. `_emit_noise_gen`'s equivalent code deliberately excludes muted-channel phantom onsets (explicit code-comment intent: "no phantom onset counted for a channel that made no sound"), so the same "excluded from the mix" concept produces opposite onset-counting behavior depending on channel type — an internally inconsistent, undocumented asymmetry within `IP-9010` itself. Empirically confirmed (not just read from the asm): peak `ONSET_WINDOW_COUNT` over 4000 frames at default tempo/density is identical (6) whether pulse B/wave are muted (`CHMIX_IDX=3`) or all channels are active (`CHMIX_IDX=0`); muting the noise channel instead (`CHMIX_IDX=6`) measurably drops the peak to 5. Neither `IP-9010`'s package doc (which flagged the analogous *dissonance*-scoring-vs-mute question, resolved as `BL-0029`) nor `IP-9020`'s empirical threshold calibration (performed exclusively at `CHMIX_IDX=0`) considered this interaction — invisible to both single-package verifications by design, only visible at the integration-review vantage point. | Medium (a real, undocumented behavioral inconsistency — surprising, not release-blocking; both packages individually satisfy their own DoD; no crash/data-corruption risk) | 07 (implementation planning — author a small remediation package that either (a) makes pitched-channel exclusion also skip the onset-window increment, matching noise's behavior, or (b) documents the current behavior as a deliberate choice — e.g. "the overload metric tracks total generation work, not audible output" — either resolution acceptable, but it must be a stated decision) | `SCHEDULED` — fold into a future `07-implementation-planning` pass when `CAP-09`/scheme-selection work next touches `CHMIX_MASKS`-adjacent code, or address standalone if convenient; not urgent (no functional risk, `10-integration-review`'s own verdict recommends it not block `11-release-readiness`). | NEW |
| BL-0031 | 2026-07-25, run #41 (`11-release-readiness`, R1+R2+R3 assessment) | gate | `FEAT-1060` (`IP-1060` arpeggio+duty-cycle, `IP-1061` vibrato+portamento) has never been covered by any `10-integration-review` pass — neither the original 2026-07-21 review nor the 2026-07-25 re-review ever named them, despite `IP-1060`/`IP-1061` sharing the exact `_emit_channel_gen`/`arp_tick` code paths that `IP-9010`'s channel-mix gating and `IP-9020`'s overload counting both modify. Per `11-release-readiness`'s own rule ("a missing report is a NO-GO input, not a gap to fill in-pass"), this blocked a GO recommendation for the consolidated R1+R2+R3 release — see [release-assessment-r1-r2-r3.md](../reviews/release-assessment-r1-r2-r3.md). | High (blocked the release assessment from reaching GO — not a functional defect, a coverage gap in the evidence chain a consolidated release explicitly requires) | 10 (integration-review — re-run at the full 11-package scope, superseding both prior reviews in one pass) | `DONE` — run #42: `10-integration-review` re-ran at the full 11-package scope, exercised the actual arpeggio/vibrato-vs-channel-mix seam live (confirmed clean, no leakage/flicker), closed. | DONE |
| BL-0032 | 2026-07-25, run #50 (`VR-1070`) | finding | `CHMIX_MASKS` assigns a Scheme-E bit to exactly one preset (preset 6, wave channel only) — pulse A/B's Scheme-E code path is fully wired and correct in `_emit_channel_gen` (confirmed by regression checks and code reading in `VR-1070`) but is unreachable through any shipped `CHMIX_IDX` preset. `BL-0020`'s original ask ("multiple procgen music schemes which can be used either solo or in combinations") is therefore only partially realized in the actual shipped data: the mechanism supports per-channel scheme combination, but no preset actually exercises a pulse-channel-on-Scheme-E or a genuine multi-channel-scheme combination beyond the single wave-only example. **Re-confirmed independently 2026-08-14** by the project's first `09-content-review` pass ([content-review-full-baseline.md](../reviews/content-review-full-baseline.md) F5), where `R224`'s motif manipulation-check question surfaced it again from the listener's side rather than the code-reading side — recorded here rather than filed as a duplicate entry. No severity change; the re-confirmation is corroboration, not escalation. | Medium (a real gap between the feature's stated ask and what's reachable in practice — not a functional defect, the mechanism itself is correct and tested) | 08 (content-authoring — a small follow-up package adding `CHMIX_MASKS` preset data that exercises pulse A/B Scheme E and/or a genuine multi-channel combination; pure data, no new mechanism) | `SCHEDULED` — fold into a future preset-tuning pass (natural companion to `BL-0005`'s standing "presets are untuned placeholders" disposition) or address standalone; not urgent, no functional risk. | NEW |
| BL-0033 | 2026-07-25, run #51 (`10-integration-review`, 12-package re-review) | finding | A pitched channel that is both `CHMIX`-muted (`IP-9010`) and assigned Scheme E (`IP-1070`) is provably safe by code inspection — the mute gate runs after both generation schemes converge at the shared `gt_delta_ready_{suffix}` label, so it is scheme-agnostic by construction — but the combination is exercised by **no shipped preset and no test**: `CHMIX_MASKS`'s only scheme-assigning preset (6) keeps its Scheme-E channel (wave) active, never muted. Confirmed this combination cannot be constructed live without a data/code change: attempted to poke the emulated ROM's `chmix_masks_table` byte directly in a running PyBoy instance to force the combination for a live test; the write was silently rejected (readback unchanged), consistent with real Game Boy cart ROM being read-only to the CPU — a hard emulator/hardware constraint, not a bug. Related to but distinct from `BL-0032` (which is about Scheme E being unreachable for pulse A/B specifically, not about the mute interaction). | Low (reasoned-safe by construction, zero functional risk identified; a test-coverage gap, not a defect) | 04 (requirements/feature-decomposition, whenever `BL-0032`'s own follow-up preset-data package is picked up) / 07 (implementation-planning — the actual preset-data package) | `SCHEDULED` — fold into `BL-0032`'s eventual follow-up preset-data package: include at least one preset that combines a mute with a Scheme-E assignment, closing both gaps in one pass; not urgent, no functional risk, `10-integration-review`'s own verdict recommends it not block `11-release-readiness`. | NEW |
| BL-0034 | 2026-07-26, intake (user-directed status review follow-up) | research-gap | Visual Evolution — theme/palette/animation evolution, environmental/time-of-day/seasonal themes, and music-reactive/mood-reactive/tempo-reactive/style-reactive visuals (user's original 22-section vision-expansion list, §16). Routed by `01-vision`'s run #25 to `02-research-game-design`/`02-research-gbc-hardware` alongside the MBC/SRAM and musical-identity/style-evolution threads — those two closed (`R106` extended, `R219`-`R221` authored); this one was never picked up. Confirmed via grep: no `docs/research/encyclopedia/` topic addresses palette/theme/animation evolution or environmental/seasonal visual themes. Already named as a prerequisite gap from two directions: `docs/roadmap/02-capability-map.md`'s CAP-14 (Visual Engine) row ("evolution... planned, needs a not-yet-run research pass") and `docs/roadmap/04-release-roadmap.md`'s **R9 — Visual Evolution & Audio-Visual Synchronization** ("prerequisite not yet satisfied"). Future code implementation this feeds: CAP-14's evolution tier and release R9, both currently blocked on this research. | Medium (a named, standing roadmap prerequisite — not urgent, no release currently depends on it; R9 sits after R5-R8 in sequence) | 02-research-game-design (visualizer-reactive-design half: palette/theme/animation-evolution conventions) + 02-research-gbc-hardware (VRAM/palette/tile-budget half: what a richer reactive palette/tile set actually costs) — mirrors the exact dual-routing run #25 used for the MBC/SRAM thread | **2026-07-26 update:** design half authored — [R222](../research/encyclopedia/R222-visual-evolution-conventions.md) grounds palette-swap-only theming (Pokemon Gold/Silver hardware precedent) as the concrete, cheap mechanism; recommends extending the existing 2-entry palette table rather than a new rendering path. **Hardware half closed same day** — [R104](../research/encyclopedia/R104-cgb-palette-system.md) §7-8 addendum confirms palette-only theming costs ~8 bytes/theme against 29349 free ROM bytes (measured via `ADR-0002`'s own instrumentation method), no new mechanism needed; only *simultaneous* multi-palette tile variety (not R222's swap-only design) would need real `VBK`/tilemap-attribute work. `DONE` — both halves closed. | DONE |
| BL-0035 | 2026-07-26, intake (user-directed status review follow-up) | research-gap | Audio-Visual Relationship/Synchronization — beat/melody/rhythm/harmonic/motion/color synchronization, and intensity/emotional/structural mapping between the engine's generative state and the visualizer (user's original 22-section list, §17). Never researched — confirmed via grep, no dedicated topic exists; `R205` (generative visualizer conventions) only lightly touches tempo-reactive visuals in passing, not a dedicated sync-mapping treatment. Named by the same R9 prerequisite note as `BL-0034` (bundled into the same roadmap release, "Visual Evolution & Audio-Visual Synchronization"). Future code implementation this feeds: CAP-14 (Visual Engine)'s evolution tier and release R9, alongside `BL-0034`. | Medium (same standing-prerequisite disposition as `BL-0034`; not urgent, no release currently depends on it) | 02-research-game-design | **2026-07-26 update:** authored — [R223](../research/encyclopedia/R223-audio-visual-synchronization.md) grounds pitch as the highest-confidence mapping dimension, amplitude/timbre-to-intensity mapping, green-channel-preferential brightness encoding for CGB's RGB15 palette format, and confirms `VR-0006`'s prior one-frame-lag finding matches general perceptual-tolerance literature. `DONE` — fully addressed at the design layer; feeds `CAP-14`/R9 alongside `R222`. | DONE |
| BL-0036 | 2026-07-26, intake (user-directed status review follow-up) | research-gap | `R219` (genre feasibility on 4-channel GBC PSG) doesn't mention three genre/style references from the user's original list (§4): **Celtic, Seasonal, and Holiday** — confirmed via grep of the topic's own text, zero matches for any of the three. All other ~26 of the user's genre references are explicitly tiered in `R219`'s three-confidence-bucket breakdown; these three were silently not carried forward when the topic was authored. Not a new research topic — an addendum to the existing `R219` topic, same "extend, don't duplicate" discipline `R106`/`R302` already used for their own MSTR-001 §9 addenda. Future code implementation this feeds: `R219` itself (completing its own stated scope) and downstream **R5 — Genre-Aware Style Presets** (the roadmap's current next release, in progress) — Celtic/Folk-adjacent and Seasonal/Holiday-themed presets would be additional candidate style regions if R219's addendum finds them hardware-feasible; also a candidate input to **R6 — Song-Form & Style-Drift Engine** if any of the three carry distinct structural/seasonal-form implications worth carrying forward. | Low-Medium (a genuine scope gap in an already-authored topic, but narrow — 3 of ~29 genre references, none flagged high-priority by the user) | 02-research-game-design — a small addendum to `R219`, not a new topic | **2026-07-26 update:** addended — `R219` §8 finds Celtic high-confidence (mode-driven, joins the Folk/World tier, real GB-chiptune-scene precedent cited), Holiday high-confidence and uniquely cheap (major key/4-4-time/moderate tempo/stepwise melody/bell timbre all map directly onto existing engine parameters — arguably the single cheapest preset in the whole genre list), and Seasonal reclassified as a preset-rotation concern (not a genre or technique), routed to R220 instead. `DONE`. | DONE |
| BL-0037 | 2026-07-26, intake (user-directed status review follow-up, 115-topic wishlist audit) | research-gap | Audited the user's original 115-topic/10-phase research wishlist (first supplied run #4, 2026-07-21) against the full 42-topic encyclopedia, item by item via the `docs/research/INDEX.md` phase-citations plus targeted greps for the three items with no explicit citation. **114 of 115 items are covered** (either by an explicitly-cited topic, e.g. Phase 1 items 1-15 across `R101`/`R108`/`R110`-`R115`, or by an uncited-but-clearly-in-scope topic, e.g. item 25 "music theory for algorithmic composition" distributed across `R201`/`R211`/`R212`). **One genuine gap: item 45, "weighted random selection."** Confirmed via grep — zero mentions of "weighted"/"probability distribution"/"biased random" anywhere in the encyclopedia. Notable: this technique is **already shipped in code** without ever being researched/grounded — `music_engine.py`'s `DELTA_TABLE = [0xFF, 0x00, 0x00, 0x01]` (-1, 0, 0, +1, indexed by 2 LFSR bits) makes "no pitch change" twice as likely as either directional step, a working weighted-random design that predates any citation for it — the same "implementation outpaced its own documentation" pattern as `BL-0025`/`BL-0026`/`BL-0029`. Future code/documentation this feeds: grounding `DELTA_TABLE`'s existing design (`_emit_channel_gen`, shipped since `IP-0001`/`IP-0002`) retroactively, and informing any future generation-scheme work (`BL-0020`'s Scheme W/E precedent) that wants a different weighting shape. Also directly relevant to the still-open **C10 forward-traceability audit** (MSTR-001 §3, journal run #26) — this is exactly the class of gap that audit exists to catch project-wide; this entry closes one instance of it, not the full audit. | Low (the underlying technique is already shipped, tested, and working — this is a retroactive grounding/documentation gap, not a missing capability) | 02-research-game-design — a small topic (or a addendum to `R201`/`R211`, both of which already touch weighted/probabilistic note selection adjacently) | **2026-07-26 update:** addended — `R211` §8 grounds `DELTA_TABLE`'s existing bias via a patent-literature-described lookup-table-weighting mechanism and named "drunk"/biased-random-walk melody-generation prior art; recommends any future differently-weighted generation scheme reuse the same table-not-arithmetic pattern. `DONE`. | DONE |
| BL-0038 | 2026-07-26, run #55 (`ADS-101`) | design-question | `ADS-101`'s own Open Question: should the new `DUTY_BIAS` (per-style timbre offset) add to or override `IP-1060`'s existing per-degree `DUTY_BY_DEGREE` duty-cycle selection? Deliberately left undecided at the architecture layer — a concrete combination rule needs prototyping/listening, not a design-tension resolvable on paper. | Low (an implementation-level detail, not a design tension blocking anything upstream) | 07 (implementation-planning, once `04`/`05`/`06` carry `ADS-101` forward to a package) | **2026-07-26 update:** resolved by `07`/`08` — `DUTY_BIAS` adds to the existing `(cur_degree & 0x03)` duty-table index, then re-masks `AND 0x03` (wrap, not clamp), reusing the exact idiom already used at that write site. Implemented and tested (`IP-1080` T15.1-T15.3 confirm each style's `DUTY_BIAS` composes correctly). `DONE` — independent verification still owed alongside `IP-1080`'s own. | DONE |
| BL-0039 | 2026-07-26, run #55 (`ADS-101`) | feature | `ADS-101`'s own Open Question: Celtic (R219 §8's Folk/World-tier addition, needs a `SCALE_SEMITONES` Mixolydian/Dorian/Aeolian extension) and Holiday's own stepwise-motion-bias finding (R219 §8) were both deliberately deferred past `ADS-101`'s 3-style v1 scope (Techno/Chiptune-Driving, Ambient/Lo-Fi, Holiday) — real, grounded follow-on candidates, not silently dropped. Celtic needs new scale-table content (not just a `STYLE_TABLE` row); Holiday's stepwise-motion bias needs either a per-style `DELTA_TABLE` variant or reuse of `ADS-100`'s Scheme-E motif mechanism. | Low-Medium (post-v1 polish, none blocking R5's own 3-style completion criteria) | 05 (feature-decomposition, once R5's v1 styles ship and their actual listening character is known, per `ADS-101` §9's own reasoning) | `DEFERRED` — revisit trigger: once R5's 3 v1 styles are `VERIFIED` and content-reviewed, a natural `00-intake`/`05-feature-decomposition` follow-up for a 4th style (Celtic) and/or Holiday's motion-bias refinement. | NEW |
| BL-0040 | 2026-07-26, run #61 (`VR-1080`) | finding | `FS-108`'s acceptance criterion (4) and `IP-1080`'s DoD both state, in absolute terms, that "a style change does not alter `DISSONANCE_SCORE`/`BAD_ZONE_FLAGS`/`STALE_COUNT_*`/`ONSET_WINDOW_COUNT`." True narrowly (`_emit_apply_style`'s own writes never touch those addresses, confirmed by full-body code read) but not in the broader sense a literal reading implies: `engine_tick` runs unconditionally every frame including a Start-press frame, so a coincidental onset from an *unrelated* channel can still update those fields on the same frame `test_rom.py`'s own T15.5 comment already documents this exact caveat for `DISSONANCE_SCORE` but doesn't extend it to the other 3 fields. `VR-1080`'s own broader randomized-stress sweep (6000 frames, 712 real style-change frames) measured a ≈16% same-frame-collision rate under adversarial play — root-caused to independent, unrelated channel-onset timing, not a code defect this package introduced. | Medium (a requirements/acceptance-criterion precision gap — the underlying engine behavior is correct and intentional per `ADS-101`'s "bad-zone bookkeeping stays scheme/style-agnostic and keeps ticking every frame" design, but the claim as written could mislead a future reader into assuming a stronger same-frame guarantee than the code actually provides; no crash/correctness risk) | 04 (requirements-engineering — reword the acceptance-criterion/FR-1240 companion wording to state the invariant precisely, same pattern `VR-1070`'s own test-methodology-precision finding used) | `SCHEDULED` — `FS-108` half fixed run #99; the remaining `IP-1080` DoD half rides `IP-9030` v2 (re-confirmed 2026-07-31, run #102). | SCHEDULED |
| BL-0041 | 2026-07-26, run #62 (`10-integration-review`, 13-package re-review) | finding | No shipped `CHMIX_IDX` preset combines a named (non-default) style (`STYLE_TABLE` indices 1-3, `IP-1080`) with Scheme E (`CHMIX_MASKS`'s only scheme-assigning preset, 6, `IP-1070`) — both mechanisms independently confirmed correct by this review's own live drive (preset 3: Holiday style + pulse-A/noise-only mute, confirmed correct; preset 6: default style + Scheme E, confirmed correct), but the specific combination of a *named* style with Scheme E is untested and unreachable via any shipped preset. Same "mechanism supports combination, data doesn't yet exercise it" pattern already found by `BL-0032`/`BL-0033`. | Low (both mechanisms individually correct and verified; a data/test-coverage gap, not a functional defect) | 05/07 (fold into `BL-0032`'s eventual follow-up preset-data package — or a new, related entry — so one future pass assigns a preset that combines a named style, a channel mute, and a Scheme-E assignment, closing all three coverage gaps at once) | `SCHEDULED` — fold into `BL-0032`'s eventual follow-up preset-data package; not urgent, no functional risk, `10-integration-review`'s own verdict recommends it not block `11-release-readiness`. | NEW |

**Triage note (run #13, 2026-07-21):** `10-integration-review` ran on the full 7-package Foundation
tranche (the standing gate — all packages independently `VERIFIED` — was satisfied). Two new
entries filed: `BL-0019` (High — `CHMIX_IDX` has no consumer anywhere, a cross-package dead-end no
single-package VR could see) and `BL-0018` (Low-Medium — six WRAM addresses undocumented in
GDS-07). Neither `DEFERRED` — both `SCHEDULED` with a concrete next step, consistent with
severity-honesty for `BL-0019`'s High rating. `BL-0017` re-surfaced by the review, not duplicated.
Per the review's own verdict, `11-release-readiness` is not being sought for this bucket until
`BL-0019` is remediated and re-verified.

**Triage note (run #12, 2026-07-21):** Same session as runs #7-#11 (still independent).
`09-package-verification` ran on `IP-0007` — `VERIFIED`
([VR-0007](../implementation/verification/VR-0007-autonomous-recovery-and-randomize.md)) —
**completing independent verification of the full 7-package tranche.** New entry `BL-0017` filed
(Medium-High: `OVERLOAD_THRESHOLD` mathematically unreachable, a real functional gap not just
doc-coherence) — dispositioned `SCHEDULED` (a remediation package, not `DEFERRED`), consistent
with severity-honesty: a Medium-High finding is addressed via a concrete next step, not shelved.
Not raised as `NEEDS-USER` — the remediation path is clear and will naturally hit its own G3 gate
when `08-code-implementation` is reached, so no decision is needed from the user *now*.

**Triage note (run #11, 2026-07-21):** Same session as runs #7-#10 (still independent).
`09-package-verification` ran on `IP-0006` — `VERIFIED`
([VR-0006](../implementation/verification/VR-0006-minimal-visualizer.md)). New entry `BL-0016`
filed (`FR-1120` overstates shipped tempo-representation scope) — dispositioned `SCHEDULED`
alongside `BL-0013`/`BL-0001`'s existing GDS-08/reconciliation work, not `NEEDS-USER` (it's a
routine doc-coherence fix, not a decision).

**Triage note (run #10, 2026-07-21):** Same session as runs #7-#9 (still independent).
`09-package-verification` ran on `IP-0005` — `VERIFIED`
([VR-0005](../implementation/verification/VR-0005-full-reset-scope.md)). `BL-0014` closed `DONE`
this run — the exact gap it named (pulse B/wave/noise Select-reset never independently asserted)
was closed by this VR's own live drive.

**Triage note (run #9, 2026-07-21):** Same session as runs #7-#8 (still independent).
`09-package-verification` ran on `IP-0004` — `VERIFIED`
([VR-0004](../implementation/verification/VR-0004-bad-zone-detection.md)). One new finding
(`FR-1090` still names the unbuilt ring buffer) folded into the existing `BL-0013` entry rather
than filed as a new ID, since it's the same underlying doc-coherence gap one layer higher.

**Triage note (run #8, 2026-07-21):** Same session as run #7 (still independent — no
implementation work done in this session). `09-package-verification` ran on `IP-0003` —
`VERIFIED` ([VR-0003](../implementation/verification/VR-0003-noise-channel-and-density.md)); no
new findings. No backlog disposition changes required.

**Triage note (run #7, 2026-07-21):** Fresh session. `09-package-verification` ran on `IP-0002` —
`VERIFIED` ([VR-0002](../implementation/verification/VR-0002-pulse-b-and-wave-channel.md)); no new
findings surfaced (`BL-0008`, already `DONE`, independently re-confirmed). No backlog entries
required disposition changes this run — all standing entries' triggers remain unfired.

**Triage note (run #6, 2026-07-21):** User directed: "If you are already able to detect bad zones,
avoid them or navigate naturally out of them. The Select button is only for the user to
reset/randomize if they want to." Implemented `IP-0007` (autonomous avoidance/recovery + Select
reset-and-randomize via `DIV`-seeded LFSRs), amending MSTR-001 C5 to v1.1 and GDS-01/GDS-03 to
match. Fixed two `JR`-out-of-range assembler errors (converted to `JP_NZ`) that surfaced once the
recovery logic lengthened the per-channel routines. Revised several `test_rom.py` assertions
(T5.5, T8.1/2/4/6/7/7c) that had implicitly assumed the now-superseded fixed-seed behavior,
documenting why in each case; added new suite T10 confirming autonomous entry into *and* recovery
from a bad zone with no Select. 60/60 checks pass; an 8000+ frame stress run confirmed both.
`BL-0011` item 2 (`DIV`-seeding) closed as a direct consequence. One new low-severity finding
filed (`BL-0015`, a self-healing display artifact, investigated but not fully root-caused given
its severity). `BL-0012`/`BL-0014` updated to include `IP-0007` in their existing scope.

**Triage note (run #5, 2026-07-21):** User authorized "accept single session limitations just this
once" and directed iterating the pipeline through to an MVP ROM. `BL-0004` (verification gate)
resolved `DONE` this run — `IP-0001` VERIFIED. Implemented and self-tested `IP-0002`-`IP-0006`
(pulse B/wave, noise/density, bad-zone detection, full reset, minimal visualizer) — 56/56
`test_rom.py` checks, 6000+ frame stress run clean. `BL-0006` updated to reflect abbreviated FS
notes (`BL-0012`, scheduled backfill). `BL-0008` closed `DONE` (wave bass role implemented,
confirmed by test). Two new doc-coherence gaps filed (`BL-0013`, `BL-0014`), both low-severity,
neither blocking. This is a deliberate, user-authorized pace exception for this run only — not a
precedent for skipping `06-feature-specification`/independent verification on future increments.

**Triage note (run #4, 2026-07-21):** User supplied a 115-topic, 10-phase research wishlist;
authored 17 new encyclopedia topics (R111, R113-R115, R209-R218, R307-R309) covering the
load-bearing majority of it, deduped against R107-R110/R201-R207/R301/R305 already authored, and
explicitly mapped the remainder (SM83 instruction set, PPU/palette/OAM topics, tile encoding,
toolchain portability, ROM validation split, palette design) to existing `⛔ Planned` rows with
the same "no gap yet" reasoning as before — nothing was silently dropped, see
`docs/research/INDEX.md`. Surfaced `BL-0010` (research-gap, phrase/form generation) and `BL-0011`
(four `feature`-type upgrade candidates) this run, both `DEFERRED` with named triggers, neither
blocking. `BL-0008`'s ride (`IP-0002`'s FS) is unaffected.

**Triage note (run #2, 2026-07-21):** `BL-0002`/`BL-0003` were `DONE` and relocated verbatim to
`docs/pipeline/backlog-archive.md`. `BL-0004` is due this run (rides `09-package-verification` on
`IP-0001`) — flipped to `IN PIPELINE`. `BL-0005`/`BL-0007`'s deferral triggers have not fired yet
(no `IP-0002`/`IP-0003`, no `01-vision` revisit in progress) — left `DEFERRED`. `BL-0001`/`BL-0006`
remain `SCHEDULED` for their named rides, not yet due. Still no `NEEDS-USER` entries.

**Triage note (run #1):** every entry above got an explicit disposition at filing time (no
separate later triage pass needed yet, since this is the project's first run). None are
| BL-0042 | 2026-07-26, run #65 (`ADS-102`) | design-question | `ADS-102`'s own Open Question: the concrete `N_VARIANTS` count (proposed 4) and `MOTIF_VARIANT_SELECTOR` weighting curve are not fixed by the architecture pass — R214 §8 grounds the *shape* (bounded, weighted, retention-biased) but not exact numbers. Same class of decision `BL-0005`'s standing "presets are untuned placeholders" disposition already tracks. | Medium | 07 (implementation-planning, proposing concrete values) / 08-content-authoring (tuning by ear) | **2026-07-26 update:** `IP-1090` named concrete first-guess values — `N_VARIANTS=4`, `MOTIF_VARIANT_SELECTOR=[0x00,0x00,0x00,0x01]` (3-in-4 retain). `SCHEDULED` — still not tuned by ear (same `BL-0005` disposition); a future listening pass, not blocking. | NEW |
| BL-0043 | 2026-07-26, run #65 (`ADS-102`) | design-question | `ADS-102`'s remaining Open Questions, all deliberately deferred past its v1 scope: (1) whether `MOTIF_VARIANT_IDX` needs to be per-channel once a future preset assigns Scheme E to more than one channel simultaneously (not yet shipped, so v1 uses a single shared byte); (2) whether variant rows should be authored via an actual offline L-system rewrite step or simply hand-composed (either satisfies R214 §8, left to content-authoring judgment); (3) whether motif-variant selection should eventually be biased by R220/R6's song-form state machine once R6 is built. | Low (none block v1's own scope; all are real, named follow-on integration points) | 05/07 (question 1, once a multi-channel-Scheme-E preset is proposed) / 08-content-authoring (question 2) / 03 or 07 (question 3, once R6 is picked up) | `DEFERRED` — revisit triggers named per-question above; none blocks `ADS-102`'s own v1 motif-recurrence design from proceeding to `04-requirements-engineering`. | NEW |

| BL-0044 | 2026-07-26, run #71 (`VR-1090`) | finding | `T16.6`'s retention-bias assertion (`IP-1090`) samples only 11 boundary events over its own 6000-frame run — `changes < boundaries` is nearly powerless at that sample size (would pass even at a true 50% switch rate). `VR-1090`'s own independent 60,000-frame run (117 events, ~19.7% switch rate) is the real statistical confirmation `FR-1290` needs, not the shipped test. | Medium (test-strength gap, not a functional defect — the underlying weighting is confirmed correct by the independent run) | 08-code-implementation (strengthen `T16.6`'s sample size, e.g. extend the driven run length) | `SCHEDULED` — fold into the next touch of `IP-1090`'s test suite; not urgent, `VR-1090`'s own verdict recommends it not block `VERIFIED`. | NEW |
| BL-0045 | 2026-07-26, run #71 (`VR-1090`) | finding | `T16.8`'s shipped 47-frame Start-press interval, as tested, never actually produces an exact-frame collision with a real motif-cycle boundary within its own 4000-frame window (nearest miss: 6 frames) — the test's claimed same-frame-collision coverage doesn't occur in its own fixture, even though `VR-1090`'s own independently-constructed guaranteed-collision test confirms the underlying engine behavior is correct. | Medium (test-coverage gap — the test's own docstring claim overstates what its fixture exercises; no functional defect) | 08-code-implementation (retime `T16.8` to derive a real motif-cycle-boundary frame and force the Start press onto it, per `VR-1090`'s own methodology) | `SCHEDULED` — fold into the next touch of `IP-1090`'s test suite; not urgent, `VR-1090`'s own verdict recommends it not block `VERIFIED`. | NEW |
| BL-0046 | 2026-07-26, run #71 (`VR-1090`) | doc-defect | `IP-1090`'s implementing commit (`c67a105`) did not refresh the committed `Driftune.gbc` binary, unlike every prior package's own commit convention — caught and fixed twice more this session (once before dispatching the verification agent, once after merging its result) by rebuilding from source. | Low-Medium (the tracked binary was stale between commits, not a source-code defect; already remediated in the live tree) | 08-code-implementation (rebuild-and-commit `Driftune.gbc` as the final step of every future package commit, per the project's own standing convention) | `DONE` — remediated live (commits after `c67a105` restored the correct binary); no further action needed. | DONE |
| BL-0047 | 2026-07-26, run #71 (`VR-1090`) | doc-defect | `docs/features/INDEX.md`'s `FEAT-1090` row still read "not yet built" after `IP-1090` reached `VERIFIED`. | Low (routine drift, same pattern several prior runs have caught and fixed) | 00-pipeline-manager (fix on next touch) | `DONE` — fixed in this same run. | DONE |

| BL-0048 | 2026-07-26, run #72 (`10-integration-review`, 14-package re-review) | finding | No shipped `CHMIX_IDX` preset exercises `IP-1090`'s motif-variant selection together with a muted Scheme-E channel or a named (non-default) style — the only shipped Scheme-E-assigning preset (6) has that channel active and carrying the default style. Extends the same "mechanism supports the combination, data doesn't yet exercise it" pattern already tracked by `BL-0032`/`BL-0033`/`BL-0041`. | Low (all mechanisms individually correct and verified; a data/test-coverage gap, not a functional defect) | 05/07 (fold into `BL-0032`'s eventual follow-up preset-data package — one future pass assigning a preset combining a mute, a named style, and a Scheme-E assignment with an active motif-variant channel would close all four related gaps at once) | `SCHEDULED` — same eventual follow-up package as `BL-0032`/`BL-0033`/`BL-0041`; not urgent, no functional risk, `10-integration-review`'s own verdict recommends it not block `11-release-readiness`. | NEW |
| BL-0049 | 2026-07-26, run #72 (`10-integration-review`, 14-package re-review) | doc-defect | `Claude.md`'s "Known Good Behavior" heading parenthetical still says `IP-1090` is "not yet independently verified" — stale since `VR-1090`. The separate "not yet GO'd" half remains accurate. | Low (doc-coherence only, no functional impact) | 08-code-implementation / 00-pipeline-manager (a one-line heading edit) | `DONE` — fixed as part of `11-release-readiness`'s own G4 baseline-flip pass (run #74). | DONE |
| BL-0050 | 2026-07-26, run #72 (`10-integration-review`, 14-package re-review) | doc-defect | `docs/feature-planning/01-feature-catalog.md`'s release-bucket header still says `FEAT-1090` is "not yet built" — stale since `IP-1090` reached `VERIFIED`. The separate "not part of the shipped baseline" claim (pending `11-release-readiness` GO) remains accurate. | Low (doc-coherence only, no functional impact) | 05-feature-decomposition (a one-line header edit) | `DONE` — fixed as part of `11-release-readiness`'s own G4 baseline-flip pass (run #74). | DONE |

| BL-0051 | 2026-07-26, intake: user asked "Add backlog items for visuals that indicate the different settings in place and what the buttons influence (eg octave and tempo)" | feature | The shipped visualizer (`IP-0006`/`FEAT-1040`) shows only per-channel-activity tiles + a calm/bad-zone palette swap (`BAD_ZONE_FLAGS`-driven) — it has never displayed any of the engine's tracked parameter *values* (`TEMPO_IDX`/`OCTAVE_IDX`/`SCALE_IDX`/`DENSITY_IDX`/`CHMIX_IDX`, nor the newer `STYLE_TABLE`/`SONG_STATE` dimensions `IP-1080`/`IP-1100` added) or any legend for which of the 6 input controls (D-pad ×4, A, B, Start, Select) edits which parameter — a listener/player currently has no way to see what's currently set or what any button does beyond trial and error. This request **unifies four previously-scattered Open Questions** already independently raised and left unaddressed across `FS-107` (scheme-reactive signal), `FS-108` (style-reactive signal), `FS-109` (motif-variant-reactive signal), and `FS-110` (song-form-phase-reactive signal) — each explicitly named "a future intake request has a clear place to land" rather than solved piecemeal; this is that request, and it is broader than any one of the four (it also covers the original 6 base controls/parameters, which have never had any visual indication at all, not just the newer mechanisms). | Medium (a real, long-standing usability gap — not a regression, no functional defect; user-initiated feature request, no stated urgency) | 03 (`03-architecture-design-synthesis` — extends GDS-08's still-`⛔ Planned` presentation-architecture level, per `BL-0001`; genuine design tension exists — which parameters to surface and how, given the tile/palette budget the existing channel-activity/bad-zone display already uses — warranting an ADS-xxx rather than jumping straight to a Feature Spec) | `SCHEDULED` — `03-architecture-design-synthesis` authored [`ADS-104`](../architecture/ADS-104-settings-and-control-visibility.md) (5 bar-height indicator tiles, reusing the existing BG palette, no new font/text; scheme/style/motif-variant/song-phase indicators deferred to the same reusable mechanism); `04-requirements-engineering` formalized `FR-1350`-`FR-1380`/`NFR-1140`-`NFR-1160` from it (delta review clean, no conflicts); `05-feature-decomposition` added the [`FEAT-1110`](../feature-planning/01-feature-catalog.md) catalog row (depends on `FEAT-1040`, explicitly no dependency on `FEAT-1080`/`FEAT-1090`/`FEAT-1100`); `06-feature-specification` authored [`FS-111`](../features/fs-111-settings-and-control-visibility.md) (full 20-field spec); `07-implementation-planning` converted it into [`IP-1110`](../implementation/packages/IP-1110-settings-and-control-visibility.md), `READY`, G3-authorized on the standing basis; `08-code-implementation` built it, `COMPLETE` (122/122 tests, one disclosed self-healing Select-frame display-lag finding); `09-package-verification` independently confirmed it, `VERIFIED` via [VR-1110](../implementation/verification/VR-1110-settings-and-control-visibility.md) (both disclosed findings independently reproduced from multiple distinct scenarios; 3 further findings harvested as `BL-0055`-`BL-0057`). **Base-control half of this request is now closed** — the four newer per-feature reactive signals (scheme/style/motif-variant/song-form-phase) remain explicitly deferred to a v1.1+ per `ADS-104` §9, a separate future intake item, not this entry's remaining scope. | DONE |
| BL-0052 | 2026-07-26, `VR-1100` (`IP-1100` verification) | finding | `test_rom.py`'s `T17.6` (forced Start-press/phase-transition collision) only exercises the first (INTRO→BUILD) boundary — it does not repeat the same forced collision at the `BUILD→PEAK` or `PEAK→BREAKDOWN` boundaries, so the "song-form always wins the last-write race" contract is confirmed at one boundary and only independently re-derived (not forced-collision-tested) at the other two. | Low-Medium (the mechanism is verified correct at all 3 boundaries by non-collision derivation; only the forced-collision *test* is narrower than the full transition set) | 08-code-implementation (extend `T17.6` to loop over all 3 transition boundaries, same empirically-derived-frame methodology) | Rode `IP-9030` v2 as scheduled — confirmed 2026-07-31 (run #114 reconciliation): `test_rom.py`'s `T17.6` now loops over all 3 phase-internal boundaries (`test_rom.py:904`, "BL-0052: exercise all three phase-internal boundaries"). | DONE |
| BL-0053 | 2026-07-26, `VR-1100` (`IP-1100` verification, 40,000-frame OVERLOAD comparison) | finding | Descriptive-only data point for `FS-110`'s Open Question 2: the BUILD phase's `OVERLOAD` incidence over a 40,000-frame run measured identical to the shipped default (0.000% vs 0.000%), while PEAK measured materially higher (21.0%) — i.e. BUILD's tempo/density target doesn't currently distinguish it from default-preset overload behavior, though PEAK does as intended. Not a defect (no requirement states BUILD must raise `OVERLOAD` incidence), but a content-tuning data point `09-content-review`/`05-feature-decomposition` should have on hand if `SONG_TABLE`'s BUILD row is revisited. | Low (descriptive data, no functional or requirements violation) | 09-content-review (fold into any future `SONG_TABLE` content-tuning pass) | `DEFERRED` — no trigger beyond "if `SONG_TABLE` values are ever revisited for content reasons." | NEW |
| BL-0054 | 2026-07-26, `VR-1100` (`IP-1100` verification) | doc-defect | `docs/features/INDEX.md`'s `FEAT-1100`/`FS-110` row was still stale (pre-`VERIFIED` wording) at verification time. | Low (doc-coherence only, no functional impact) | 09-package-verification | `DONE` — corrected in-place by `VR-1100` itself as part of its own verification pass (same self-correcting pattern `BL-0047` established). | DONE |
| BL-0055 | 2026-07-26, `VR-1110` (`IP-1110` verification) | doc-defect | `IP-1110`'s own `Documentation Updates` field named `docs/pipeline/backlog.md` ("`BL-0051` closes its base-control half on this package's completion") as a required update, but the implementing commit never touched that file — `BL-0051`'s row still read `IN PIPELINE` with no mention of its base-control half closing. | Low (doc-coherence only, no functional impact; the underlying feature is correctly built and tested) | 00-pipeline-manager | `DONE` — `BL-0051`'s own row updated in this same pass to reflect the base-control half closing on `IP-1110` reaching `VERIFIED`. | DONE |
| BL-0056 | 2026-07-26, `VR-1110` (`IP-1110` verification) | doc-defect | `docs/features/INDEX.md`'s `FEAT-1110`/`FS-111` row and `docs/feature-planning/01-feature-catalog.md`'s bucket header both still read "not yet built" against the actual shipped/`VERIFIED` state. | Low (doc-coherence only, no functional impact) | 09-package-verification | `DONE` — corrected in-place by `VR-1110` itself as part of its own verification pass (same self-correcting pattern `BL-0047`/`BL-0054` established). | DONE |
| BL-0057 | 2026-07-26, `VR-1110` (`IP-1110` verification) | finding | `test_rom.py`'s `T18.8`-`T18.10` (Select-frame display-lag sequence) only exercises one pre-Select button combination — `VR-1110`'s own independent drive confirmed the same lag/self-heal shape holds across two additional, distinct pre-Select sequences, but the shipped suite itself only demonstrates it from one. | Low (test-coverage gap, not a functional defect — the underlying timing interaction is a single code path, not sequence-dependent, and this was independently confirmed to hold across sequences) | 08-code-implementation (optionally extend `T18.8`-`T18.10` to iterate over a second distinct pre-Select sequence, same rigor upgrade `VR-1100`'s own `BL-0052` recommended for `T17.6`) | Rode `IP-9030` v2 as scheduled — confirmed 2026-07-31 (run #114 reconciliation): `test_rom.py`'s `T18.8`-`T18.10` now iterate over 2 distinct pre-Select sequences (`test_rom.py:1001`, "BL-0057: exercise at least 2 distinct pre-Select button sequences"), and `T18.10`'s name/comment correctly attributes the lag to the render-vs-reset-timing artifact, not the falsified Select-frame drop. | DONE |
| BL-0058 | 2026-07-26, run #92 (`03-architecture-design-synthesis`, GDS-02) | research-gap | GDS-02 §7 names, for the first time anywhere in the tree, that **Driftune has never been run on physical GBC hardware** — every assertion the project makes is an assertion about PyBoy 2.7.0's behavior. `R111` (the one APU-erratum topic researched in depth) covers a Channel-3 wave-RAM erratum that is confirmed **DMG-only and absent on CGB**, so it explicitly does *not* address this exposure. No topic characterizes where PyBoy's APU emulation might diverge from CGB silicon in ways this engine's per-frame register writes would notice. | Low-Medium (no evidence of an actual defect; an unquantified validation gap, not a known problem. The hardware test itself is outside every automated stage's reach — it needs a flash cart and a physical device.) | 02-research-gbc-hardware (author a PyBoy-vs-CGB-silicon APU-divergence topic, so an eventual hardware test knows what to look for rather than being a general smoke test) | **Escalated in specificity 2026-07-31 (run #102) — still `NEEDS-USER`, not yet ripe.** This gap now carries one concrete, named question rather than a general fidelity concern: PyBoy models no PPU-mode gating on VRAM writes (`R301` §3), real silicon enforces mode 3 (`R102` §3), and this ROM's `update_visuals` finishes on VBlank's *last* scanline — so whether that margin holds on hardware is unanswerable here by construction (`GDS-06` §2.3, `GDS-02` §7, `R102` §3c). **A cheaper partial substitute now exists and should be offered first:** cross-checking against a mode-accurate emulator (SameBoy/BGB, `R309`). Decision the user owns: is hardware (or emulator cross-check) validation wanted, and at what priority? **ANSWERED 2026-08-07: ship v1.0 as *emulator-validated*, stated plainly in the release notes, and add a mode-accurate-emulator cross-check (SameBoy/BGB, `R309`) at R13.** That targets the specific divergence PyBoy cannot model — no PPU-mode gating on VRAM writes, against this ROM's `update_visuals` finishing on VBlank's last scanline — without requiring hardware. Real-silicon validation becomes a v1.1 goal, not a v1.0 blocker. | SCHEDULED |
| BL-0059 | 2026-07-26, run #93 (`03-architecture-design-synthesis`, GDS-04) | doc-defect | `ROADMAP.md`'s stage-06 row still lists only `FS-106`-`FS-110` and calls the stage "🟡 FS-106/FS-107/FS-108/FS-109/FS-110 authored" — `FS-111` (settings & control visibility, authored 2026-07-26 for `FEAT-1110`, now `VERIFIED` via `IP-1110`) is missing from both the detail cell and the status cell. Noticed while a stage-03 run was updating the adjacent stage-03 row; not fixed in-pass since the row belongs to stage 06. | Low (doc-coherence only, no functional impact) | 06-feature-specification (a one-line row update the next time it touches `ROADMAP.md`) | `SCHEDULED` — rides `06-feature-specification`'s next run, which is already queued this session for the `FS-100`-`FS-105` backfill (`BL-0012`/`BL-0006`). | Confirmed resolved 2026-07-31 (run #114 reconciliation): `ROADMAP.md`'s stage-06 row now names `FS-111` explicitly (settings & control visibility, `BL-0051`/`ADS-104`, `VERIFIED` via `IP-1110`/`VR-1110`) in both the detail cell and status cell. | DONE |
| BL-0060 | 2026-07-26, run #94 (`03-architecture-design-synthesis`, GDS-06 §2.2) | research-gap | **`R101`'s own stated cycle-tallying revisit trigger appears to have fired.** `R101` §5 says to revisit per-instruction cycle tallying "if a future package pushes per-frame work close to the VBlank budget." `IP-1110` shipped exactly that evidence: on the frame Select is pressed, `apply_input`'s full `init_engine` reset costs enough extra CPU that `update_visuals`'s VRAM writes for that frame are silently dropped — reproduced by `VR-1110` across 3 distinct button sequences. The project has **never measured its actual per-frame cycle headroom**; the whole evidence base is "no frame drops observed in stress runs," and stress runs without a Select press cannot exercise the frame class now known to be tight. | Medium (no requirement is violated and the symptom is cosmetic and self-healing — but the *posture* that the empirical proxy is adequate is now contradicted by evidence, and no one knows how much headroom remains) | 02-research-tooling-and-testing (re-derive `R101`/`R308`'s "cycle counting is over-engineering" conclusion against this new evidence), then `07-implementation-planning` if the recommendation stands | `SCHEDULED` — **run #98 partial:** `04-requirements-engineering` added the caveat to `NFR-1010` recording that the extended-run method is no longer sufficient alone for packages touching the Select-reset frame or `update_visuals`. **Research half `DONE` 2026-07-26 (run #100)** — `R308` §8 re-derived it and **reversed the conclusion**, with a local experiment as evidence. Key finding beyond the original: `NFR-1010`'s stress-run proxy is *structurally blind* to this class of problem (a dropped VRAM write causes no hang, no slowdown, no frame drop — the only three things it watches). Revised priority: a VRAM-write-integrity check first (`BL-0069`), cycle tallying second (**`R101` §8 `DONE` run #101** — reversed its own conclusion, quantified the budget against `R102`'s ~1140 M-cycle VBlank window, honestly costed the ~150-emitter tooling, and named a cheaper first diagnostic: an `LY`/`STAT` probe around `update_visuals` to measure the actual overrun before committing to the full package), architecture change only after quantification. Not urgent (nothing is broken), but genuinely triggered rather than hypothetical, so it should not sit as `DEFERRED` with no trigger. | DONE |
| BL-0061 | 2026-07-26, run #94 (`03-architecture-design-synthesis`, GDS-06 §6 Q2) | finding | Nobody has checked whether the Select frame is the **only** tight frame class. The same reasoning applies to any frame combining heavy `apply_input` work with visualizer writes: a Start press applies a style row on the same frame the visualizer renders, and a song-form phase transition writes two indices on a frame the visualizer then renders. Neither has been probed for dropped VRAM writes. The Select case was found only *because* `IP-1110` gave the visualizer a value that visibly changes across a reset. | Low-Medium (speculative but cheap to answer, and the one case anybody looked at did turn up a real effect) | 09-package-verification or 10-integration-review (a targeted frame-by-frame probe of the Start-press and phase-transition frame classes, same method `VR-1110` used for Select) | **ANSWERED 2026-07-31 (run #102) — and the question was malformed.** There is no Select-frame drop and no frame class is distinctly tight: every frame, idle included, finishes `update_visuals` on VBlank's last scanline. Start and Select are a few instructions further along the same nearly-spent budget, not a separate category. `GDS-06` §6 OQ2 closed as answered rather than deferred; the live replacement question (*how much head-room, in cycles, and what guards it?*) is carried by `GDS-06` §6 OQ1 and by `IP-9030` v2. | DONE |
| BL-0062 | 2026-07-26, run #94 (`03-architecture-design-synthesis`, GDS-06 §5/§6 Q3) | recommendation | GDS-06 §5 names a recurring pattern as a discipline-level observation: **this project's shipped tests tend to demonstrate a mechanism works once, where the claim being made is that it works generally** — `BL-0044` (statistically weak sample), `BL-0045` (a collision test whose fixture never collides), `BL-0052` (1 of 3 boundaries), `BL-0057` (1 of several sequences). Independent verification has constructed the general case and caught it every time, so the system works — but it works because stage 09 is diligent, not because anything requires it. Whether to state the generality expectation explicitly (as an `NFR`, or as a stage-08 authoring convention) is open. | Low (a process-quality question, not a defect; the existing safety net has a 4-for-4 record) | 04-requirements-engineering (if it becomes an `NFR`) or the `08-code-implementation` skill's own conventions | `DEFERRED` — revisit trigger: if a narrow-test gap ever reaches `10-integration-review` *without* stage 09 having caught it first, i.e. the moment the safety net's 4-for-4 record breaks. | NEW |
| BL-0063 | 2026-07-26, run #95 (`03-architecture-design-synthesis`, GDS-08 §1.1) | doc-defect | GDS-08 records that the visualizer uses **no sprites, no OAM, and no DMA at all** — every pixel is background tilemap. `R105` (OAM/sprites/DMA) therefore has **no forward trace into shipped code**, which under `MSTR-001` C10 must be either routed forward or recorded as an honestly-named exception. GDS-08 §1.1 names it as an exception in passing, but `R105` itself carries no such note, and no forward-trace audit artifact exists anywhere (C10 was added in `MSTR-001` v1.3 and its own changelog entry predicted "several orientation/history topics... may currently have no forward trace"). | Low (traceability-bookkeeping only; the underlying design choice is deliberate and correct) | 02-research-gbc-hardware (add the C10 exception note to `R105` itself), and separately `01-vision`/`10-integration-review` for the full forward-trace audit `MSTR-001` v1.3 predicted would be owed across all 44 topics | `DONE` 2026-07-26 (run #101) — `R105` now carries an explicit C10 exception recording that no sprites/OAM/DMA are used anywhere by design ([GDS-08 §1.1](../architecture/08-presentation-architecture.md)), plus a note that the capability retains standby value with its budget entirely unspent. The **full C10 forward-trace audit** across all 44 topics is a larger, separate piece of work that has been owed since v1.3 and has never been scheduled — flagged here as a standing gap, not folded into this entry. | DONE |
| BL-0064 | 2026-07-26, run #96 (`03-architecture-design-synthesis`, GDS-09 §3) | refactor | **The patch-point contract is vestigial.** `build_engine_asm(rom)` and `build_input_asm(rom)` are both typed `-> dict` and both construct a `patches = {}`, but neither ever assigns a key into it, and `build_rom.py` calls both as bare statements, discarding the return values. The dicts are constructed, returned, and dropped on every build. This is scaffolding inherited from the reference project (where patch points were a real mechanism) and never needed here, because `gbc_lib.py`'s own label/fixup resolution pass already does the job. **Observable cost**: three documents describe it as a live interface that carries no information — GDS-03's "patch-point contract", the GDS-09 ladder-table row, and the *Interfaces Used* field of six `FS-1xx` specs that cite "patch-point dict keys" as an interface they consume. Anyone reading those looks for a mechanism that isn't there. | Low (no functional defect — the code is correct and the ROM is correct; a documentation-truth and dead-scaffolding cost) | 07-implementation-planning (an `IP-8xx0` refactoring package: drop the unused dicts and the `-> dict` annotations), then `08-refactoring`. The three document families describing it need correcting either way — GDS-03 is `03`'s own, the FS fields are `06`'s. | **DONE 2026-07-31** — `IP-8010` `COMPLETE`: `build_engine_asm`/`build_input_asm`'s dead `patches = {}`/`return patches`/`-> dict` removed. Equivalence proven: byte-identical ROM (SHA-256 `a646a651af...`), 129/129 identical check-name set. Awaits `09-package-verification` (fresh session). | IN PIPELINE |
| BL-0065 | 2026-07-26, run #96 (`03-architecture-design-synthesis`, GDS-09 §6) | refactor | `visuals.py` deliberately does not import `music_engine.py` (keeping the import graph acyclic and the visualizer a genuinely read-only consumer, GDS-03 §1), declaring the WRAM addresses it needs as plain integers instead. **That duplication grew from 1 constant to 11 in a single package**: `IP-0006` duplicated only `BAD_ZONE_FLAGS`; `IP-1110` added five WRAM index addresses (`TEMPO_IDX`/`OCTAVE_IDX`/`SCALE_IDX`/`DENSITY_IDX`/`CHMIX_IDX`) and five `PRESET_*` values. Nothing has drifted, but nothing prevents drift: moving an address in `music_engine.py` leaves the build green and the visualizer silently reading the wrong byte, and the five `PRESET_*` duplicates are *values* rather than addresses, so a preset change would leave boot-time indicator initialization stale with no test failing. | Low-Medium (no current defect; a real and newly-steepened drift risk — the trend 1→11 in one package matters more than the present state) | 07-implementation-planning (an `IP-8xx0` refactoring package: a shared constants module both `music_engine.py` and `visuals.py` import, which preserves the acyclic rule since it would depend on nothing), then `08-refactoring` | **DONE 2026-07-31** — `IP-8020` `COMPLETE`: new `wram_constants.py` (dependency-free) is now the canonical source of the 11 constants; `music_engine.py`/`visuals.py`/`input_map.py` all import from it, `visuals.py` no longer duplicates any of them. Equivalence proven: byte-identical ROM, 129/129 identical check-name set. `LY`/`VIS_ENTRY_LY` confirmed not instances of this pattern, left untouched. Awaits `09-package-verification` (fresh session). | IN PIPELINE |
| BL-0066 | 2026-07-26, run #96 (`03-architecture-design-synthesis`, GDS-09 merge decision) | doc-defect | Two document families now carry statements GDS-09 contradicts or supersedes: (1) the **six `FS-1xx` *Interfaces Used* fields** each state "GDS-09 (Interface Specification) remains `⛔ Planned`, same standing gap every prior FS has flagged" — stale the moment GDS-09 was authored; several also cite "patch-point dict keys" as a consumed interface, which `BL-0064` shows is empty. (2) **GDS-03**'s own "patch-point contract" description, same issue. | Low (doc-coherence only) | `06-feature-specification` for the six FS fields; `03-architecture-design-synthesis` for GDS-03's own text (its own to amend on a future ladder pass) | `DONE` (FS half) 2026-07-26 run #99 — all six specs' *Interfaces Used* and *Open Questions* lines updated to cite the real GDS-09 sections each consumes; no FS turned out to cite the vestigial patches dict, so nothing needed correcting there. **GDS-03's own patch-point text remains open** and rides the next `03` pass. Original: GDS-03's amendment rides the next `03` pass that touches it. Deliberately not corrected in-pass: a ladder level does not edit six downstream specs. | DONE |
| BL-0067 | 2026-07-26, run #97 (`03-architecture-design-synthesis`, GDS-10 §4) | recommendation | **`MSTR-001` C10's research→code forward-trace audit has never been run, and no forward-trace artifact of any kind exists.** C10 (added v1.3) requires every authored research topic to trace forward to a design feature actually implemented in code, or carry an explicitly recorded exception. Its own changelog predicted the audit would be owed across (then) 39 topics; there are now **44**, and nothing anywhere would notice a topic that never landed. Unlike the missing `03-rtm.md` (which GDS-10 §3.1 judges a reasonable deviation because independent verification already does that job better), **this gap has no alternative mechanism**. Two instances already found incidentally rather than by audit: `R105` (OAM/sprites/DMA — the visualizer uses no sprites at all, `BL-0063`) and `R308`/`R101`'s cycle-budgeting guidance (grounded a decision *not* to act, a legitimate C10 exception shape but recorded nowhere as one — and GDS-06 §2.2 has since partly overturned that decision). | Medium (a standing vision-level commitment the tree has no mechanism to check itself against; no functional risk) | `02-research-*` skills for a lightweight **per-topic forward-trace field** (one header line naming the FR/FEAT/IP the topic fed, or its recorded exception — maintained where the topic lives, matching this project's existing conventions rather than centralising into a matrix); the sweep across all 44 is a `10-integration-review` traceability-dimension pass or a dedicated package | **DONE 2026-07-31** — the C10 forward-trace convention is established and applied across the whole encyclopedia. All 47 topics carry a `6b. Forward trace` section (`5b` in `R221`, whose numbering differs): 33 traced, 14 honest exceptions or recorded partials. `docs/research/INDEX.md` carries the categorised tally. | DONE |
| BL-0068 | 2026-07-26, run #97 (`03-architecture-design-synthesis`, GDS-10 §3.1) | doc-defect | `docs/requirements/INDEX.md` lists `02-requirements-review.md` and `03-rtm.md` as `⛔ Planned`, which reads as outstanding work. GDS-10 §3.1 takes the position that **neither is owed**: review findings are carried inline as dated *Delta Review* sections in the single requirements file (all seven exist), and the RTM is a reasonable deviation whose information is distributed across artifacts that are already maintained. The rows should be re-labelled from `⛔ Planned` to a deliberate, reasoned deviation so they stop reading as debt. | Low (doc-coherence; the underlying single-file convention is working and already acknowledged in the requirements file's own "Known pre-existing gap" note) | 04-requirements-engineering (its own index rows to re-label; GDS-10 §5 explicitly declines to edit them) | `DONE` 2026-07-26 (run #98) — `04-requirements-engineering` agreed with GDS-10 §3.1's reasoning and re-labelled both rows as deliberate, reasoned deviations citing it. | DONE |
| BL-0069 | 2026-07-26, run #100 (`02-research-tooling-and-testing`, `R308` §8.4) | finding | **The visualizer's VRAM writes are not reliably VBlank-gated, on any frame doing more than minimum work.** `R308` §8's local experiment confirmed drops on Start-press, Select-press, and autonomous song-form-transition frames, with the song-form case dropping *partially* (cell 0 landed, cell 3 did not) — proving the frame's work overruns the safe window and cuts the write sequence off mid-block, rather than any single operation blocking a write. Corroborated by `IP-1110`'s own history (moving the block to the front made the *channel-activity* writes drop instead — whichever block runs last is exposed) and by a static measurement showing `init_engine` is only 176 bytes (~6% of the per-frame region), so the margin was already ≈zero. **Impact is cosmetic and self-healing** (the stateless re-render contract, `GDS-08` §3) — audio is unaffected, since the APU writes are not VRAM. **But `NFR-1010`'s stress-run method is structurally blind to it**: a dropped VRAM write produces no hang, no slowdown and no frame drop. | Medium (no correctness defect and no audio impact; a real and previously-unquantified gap between what `GDS-06` §2.1 claims about VBlank gating and what the implementation achieves) | **RE-DERIVED 2026-07-31 (run #102). The finding as filed is FALSIFIED and withdrawn.** `IP-9030` built the diagnostic this entry asked for and the measurement disproved the entry's own premise: PyBoy 2.7.0 applies no PPU-mode gating to VRAM writes (`mb.py:502-511`), so no write is dropped and none could ever have been observed; the symptom was a `pb.tick()` mid-frame sampling artifact, uniform across every frame class including idle. **The replacement finding, which is real and worse:** `HALT` wakes at `LY` 144, `read_joypad`+`apply_input`+`engine_tick` consume ~9 of VBlank's 10 scanlines, and `update_visuals` finishes at `LY` 153 — head-room of a handful of instructions, spent on every frame, guarded by nothing. Corrections landed in `R308` §8.5, `R101` §8.5, `R102` §3b/§3c, `R110` §3/§5, `R301` §3/§5, `R305` §3/§5, `GDS-06` §2.1/§2.2a/§2.3, `GDS-02` §7. `SCHEDULED` — rides `IP-9030` v2 (VBlank budget assertion), which awaits G3 re-confirmation. | IN PIPELINE |
| BL-0070 | 2026-07-26, run #100 (`02-research-tooling-and-testing`, `R308` §8.3) | doc-defect | [GDS-06 §2.1](../architecture/06-non-functional-requirements.md) states that VBlank gating is "well-founded", "solid", and "structural — there is exactly one place visualizer code runs, and it is inside the gated region by construction." `R308` §8.3's evidence contradicts the *achieved* property: the loop **starts** in VBlank but the frame's work outlasts it, so later writes land in arbitrary PPU modes and are discarded in mode 3. The claim describes the design's intent accurately and its outcome inaccurately. | Low-Medium (doc-accuracy; the underlying finding is tracked as `BL-0069`, this entry is only about the ladder text overstating confidence) | 03-architecture-design-synthesis (amend GDS-06 §2.1 — its own §2.2 already records the frame-budget concern, so this is a consistency fix between two adjacent subsections of the same document) | **DONE 2026-07-31 (run #102)** — and the entry was right for a reason it did not know. `GDS-06` §2.1's "solid/structural" claim was indeed overstated, but not because writes are dropped (they are not). Corrected at source: §2.1's Standing rewritten to "sound in mechanism, marginal in budget", drawing the distinction the original text elided — the construction guarantees the VBlank window is *entered*, not that the frame's work *fits* in it. New §2.3 records what the harness structurally cannot verify. | DONE |
| BL-0071 | 2026-07-26, run #100 (`02-research-tooling-and-testing`, `BL-0067` half) | recommendation | The `MSTR-001` C10 forward-trace convention is now **established and applied to the R300 tier** (`R301`-`R309` each carry a `## 6b. Forward trace` section naming the shipped code they fed or an explicit exception; three are honestly recorded as *not* clean traces). **The R100 and R200 tiers — 35 topics — remain owed**, to `02-research-gbc-hardware` and `02-research-game-design` respectively. `BL-0063`'s `R105` exception is one known instance already waiting. | Low-Medium (the convention exists and the pattern is proven; this is applying it to two more tiers — mechanical, but it is where the actual audit value is, since R1xx/R2xx are where orientation/history topics with no forward trace are most likely) | `02-research-gbc-hardware` (R1xx, 15 topics) and `02-research-game-design` (R2xx, 23 topics) | **DONE 2026-07-31** — R2xx (23 topics) completed the audit, after R3xx (run #100) and R1xx (run #101). Final tally 24→**47 of 47 traced**. R2xx produced the highest exception rate of the three tiers (5 clean exceptions, 4 partials), which is the expected shape for a survey-heavy tier. Two entries are findings rather than bookkeeping: `R209` is the encyclopedia's weakest-traced topic (cited by nothing anywhere in the tree), and `R214` is the strongest argument for the discipline — its §8 talked the project out of an L-system derivation engine and into the weighted-variant table that shipped as `IP-1090`. | DONE |
| BL-0072 | 2026-07-31, run #102 (`08-code-implementation`, `IP-9030` Blocking Report) | gate | **`IP-9030` v2's G3 authorization needs re-confirmation.** The 2026-07-26 standing-basis grant was correctly cited but was given for a package whose objective was to detect and quantify a dropped-VRAM-write behaviour that has since been shown not to exist. v2 addresses a different (real) problem, ships a **permanent per-frame ROM diagnostic** rather than removable instrumentation, and additionally corrects four documents carrying the falsified claim. `07`'s position: the user's underlying intent is better served by v2 than by v1 and nothing is irreversible — but a grant for X is not a grant for Y. | Medium (blocks the package; nothing else waits on it) | 07 → 08 | **RESOLVED 2026-07-31** — the user's explicit words this turn, "Continue to iterate on assuming pre authorization for everything this session," directly answer this re-confirmation request. `IP-9030` v2 built and `COMPLETE` (129/129); awaits `09-package-verification` in a fresh session. | DONE |
| BL-0073 | 2026-07-31, run #102 (`02-research-tooling-and-testing`, `R305` §5) | finding | **A methodological failure worth a standing guard, not just a one-off correction.** The `R308` §8 dropped-write finding was carefully derived, internally consistent, cited to real hardware documentation, **independently reproduced by `VR-1110`**, and wrong — because it inferred a *hardware* mechanism from an *emulator* observation without checking whether the emulator models that mechanism. It propagated into five documents and one planned package before being caught. `R305` §3 now carries the check-the-emulator-source rule and `R305` §5 a can/cannot-establish table. **What is not yet addressed: independent verification reproduced the error rather than catching it**, because it repeated the same method. Whether `09-package-verification`'s own conventions should require method-independence (not merely session-independence) when confirming a finding is an open question this entry exists to keep alive. | Medium (process, not product) | 09 (its own conventions) — possibly `04` if it becomes an `NFR` | `DEFERRED` — revisit trigger: the next `09-package-verification` run that confirms a *finding* (as opposed to verifying a package's DoD), or any future run that adjudicates a disputed measurement. | DEFERRED |
| BL-0074 | 2026-07-31, run #102 (`07-implementation-planning`, `IP-9030` v2 TWBS) | recommendation | **No package owns the *fix* verb for the per-frame budget.** `IP-9030` v2 is a guard, not a remediation — deliberately, since both `R101` §8.5 and `GDS-06` §6 OQ1 hold that a regression guard should exist before remediation is attempted. The two candidate remediations are named but unowned: (a) static per-routine cycle tallying (~150 `gbc_lib.py` opcode emitters, costed in `R101` §8.3 — a *pricer* that attributes cost before a routine ships, which no runtime guard can do); (b) an architecture change moving visualizer writes into a dedicated VBlank ISR (`GDS-06` §6 OQ1, `GDS-08`/`GDS-09` describe the current arrangement). Recorded so the deferral is explicit rather than silent. | Medium | 07 (after `IP-9030` v2 ships and reports real head-room) | `DEFERRED` — revisit trigger: `IP-9030` v2 `VERIFIED` and its `T19` head-room numbers on record, **or** any package whose planning shows it would add material per-frame work. | DEFERRED |
| BL-0075 | 2026-07-31, run #102 (documentation review) | doc-defect | **`docs/INDEX.md` pointed at a `pipeline/BOOTSTRAP.md` run-book that has never existed in the tree** — two links, one of them the `pipeline/` row's primary target. Corrected in place to point at the journal, with a note that the pipeline's actual run-book is `.claude/skills/README.md` plus each skill's `SKILL.md`. Filed rather than silently fixed because the row described the document as *"The run-book"*, i.e. someone intended a top-level pipeline run-book to exist; whether one should be authored is a real question, not a typo. | Low | 00-pipeline-manager (decide whether a `docs/pipeline/` run-book is genuinely wanted, or whether the skills' own README is the run-book and the row was simply wrong) | `DEFERRED` — revisit trigger: the next time a new contributor or fresh session needs orienting to the pipeline and the skills' README proves insufficient. | DEFERRED |
| BL-0076 | 2026-07-31, run #102 (documentation review) | doc-defect | **`.claude/skills/08-refactoring/SKILL.md` cites `docs/research/encyclopedia/R307-refactoring-practices.md` as its grounding topic, and that file does not exist.** The `R307` ID is taken by an unrelated topic (`R307-realtime-audio-engine-architecture.md`), so this is not a rename — the refactoring-practices grounding the skill claims to rest on was never authored, and `07-implementation-planning`'s own conventions also cite "grounding: `R307`" for refactoring equivalence contracts. Two possible resolutions and they are materially different: author the missing topic under a free R3xx ID and fix both skills' citations, or accept that refactoring practice needs no encyclopedia topic and remove the citations. **Not fixed here** — skill files are outside every pipeline stage's write scope, and the choice is a real one. | Medium (two skills rest a stated convention on a non-existent document; `IP-8xx0` packages are required to carry an equivalence contract "grounding: R307") | 02-research-tooling-and-testing (if authored) — the citation fix belongs with whoever owns the skill definitions | **DONE 2026-07-31** — resolved directly rather than left NEEDS-USER: authoring the missing topic was within `02-research-tooling-and-testing`'s own tier and the constructive resolution. `R310` authored, both dangling `R307` citations (`08-refactoring`/`07-implementation-planning` SKILL.md) corrected to `R310`. | DONE |
| BL-0077 | 2026-07-31, run #102 (documentation review) | doc-defect | **27 broken internal links across 6 documents**, all fixed this run: `docs/pipeline/backlog.md` (14 — `encyclopedia/Rxxx` missing the `../research/` prefix), `docs/reviews/release-assessment-r1-r2-r3.md` (7 — missing `../` prefix, plus one `FS-106` vs. the real lowercase `fs-106-` filename), `docs/INDEX.md` (2 — see `BL-0075`), and one each in `00-master-build-plan.md`, `strategic-assumptions-register.md` and `ADR-0002` (which cited `R302-codegen-patterns.md`; the real file is `R302-python-assembler-codegen-patterns.md`). None changed any document's meaning. **The real finding is that nothing checks this** — every one of these has been dead since it was written, across many runs and two integration reviews. A link check is a few lines and would run in the same place the ROM/test gates do. | Low individually, Medium as a class | 07 → 08 (a link-check step, plausibly folded into a future doc-scoped package) or `10-integration-review`'s own checklist | `DEFERRED` — revisit trigger: the next `10-integration-review` run, or any doc-scoped `IP-8xx0` package (it would be cheap to fold in there). | DEFERRED |
| BL-0078 | 2026-07-31, run #102 (documentation review) | doc-defect | **The encyclopedia topic count was stated as "44" in live documents while the real count is 47** (R1xx 15 + R2xx 23 + R3xx 9). The figure was quoted in `ROADMAP.md`'s stage-02 row, `docs/architecture/INDEX.md`'s GDS-10 row, and `BL-0071`'s own C10 coverage fraction ("24 of 44"), so the forward-trace audit's stated coverage was overstating itself. Corrected in all three; historical journal rows left as written, since they record what was believed at the time. | Low | — (corrected in this run) | `DONE` 2026-07-31 — corrected in the three live locations; the C10 fraction is now honestly 24 of 47, with R2xx's 23 outstanding (`BL-0071`). | DONE |
| BL-0079 | 2026-07-31, run #109 (`03-architecture-design-synthesis`, `ADS-105` OQ2) | research-gap | **`R221`'s dissonance-as-valence-proxy claim remains uncited**: "dissonance/consonance is intuitively adjacent to (un)pleasantness... but no source in this pass directly validated dissonance-score-as-valence-proxy specifically." `ADS-105` (routing roadmap R7) deliberately excludes `DISSONANCE_SCORE` from v1's `VALENCE` formula on this basis — `SCALE_IDX` alone is sufficient and citation-confirmed. If a future pass wants to fold `DISSONANCE_SCORE` in (e.g. to distinguish "major scale, currently dissonant" from "major scale, consonant" as different valence states), this citation gap should close first. | Low (v1 ships without needing it; a real gap only if a future pass wants the richer signal) | `02-research-game-design` (fetch-verification pass on `R221`, or an honest note that the claim remains unconfirmed if no supporting source is found) | `DEFERRED` — revisit trigger: a future requirements/architecture pass proposing to add `DISSONANCE_SCORE` to the `VALENCE` formula. | DEFERRED |
| BL-0080 | 2026-07-31, run #109 (`03-architecture-design-synthesis`, `ADS-105`) | design-question | **Should roadmap R7's `AROUSAL` formula include `CHMIX_IDX`-derived active-channel count?** `R221` notes active-channel count is a direct arousal-axis lever in the literature; `ADS-105` leaves this open rather than deciding, since the architectural cost of adding it later is low (the trigger-site plumbing for a `CHMIX_IDX` change is already covered by the same recompute sites `AROUSAL`/`VALENCE` need for other reasons). | Low (a genuine but low-stakes design choice, not blocking) | `04-requirements-engineering` (decide when drafting R7's FR-candidates into baseline `FR`s) | **DONE 2026-07-31** — resolved by `04-requirements-engineering`: excluded from v1 baseline, recorded as `CR-0002` (not baselined). Reversible later per `ADS-105` §2's own reasoning. | DONE |
| BL-0081 | 2026-07-31, run #109 (`03-architecture-design-synthesis`, `ADS-105`) | recommendation | **`ADS-105`'s exact `AROUSAL`/`VALENCE` derivation formulas are a recommendation, not a decision** — a lookup-table-based lint from `TEMPO_IDX`/`DENSITY_IDX` (arousal) and a direct `SCALE_IDX`-keyed lookup (valence), avoiding on-device division (SM83 has no direct support). This crosses into testable-acceptance-criterion territory `03`'s own scope boundary routes downstream. | Low (design guidance already given; a normal planning step, not a gap) | `04-requirements-engineering` | **DONE 2026-07-31** — resolved by `04-requirements-engineering`: acceptance criteria kept at the behavioral level (monotonicity, fixed mapping), not literal lookup-table values, per the standing no-byte-level-detail-in-requirements rule. Exact tables routed to `07-implementation-planning`. | DONE |
| BL-0082 | 2026-07-31, run #114 (`00-pipeline-manager` reconciliation) | doc-defect | `ROADMAP.md`'s stage-07 row reads "Master Build Plan, TWBS (...16 packages...) — 16/16 `VERIFIED` — **all 16 now in the shipped baseline**" with a status cell of "✅ **16/16 packages VERIFIED**, 1 `READY` (`IP-9030`)". Both are stale: `IP-9030` is no longer `READY` (it is `COMPLETE`, v2, 2026-07-31), and `IP-1120` (`FS-112`/roadmap R7) now also exists as a 17th package, `COMPLETE`. Four packages (`IP-9030`, `IP-8010`, `IP-8020`, `IP-1120`) are `COMPLETE` awaiting fresh-session `09-package-verification`, not `VERIFIED`. | Low (doc-coherence only, no functional impact — `docs/implementation/00-master-build-plan.md` and `packages/INDEX.md` are both already correct; only this flat-summary row lags) | 07-implementation-planning (a one-row update the next time it touches `ROADMAP.md`, or folds into the next `09-package-verification`/`10-integration-review` pass that already has to read all four packages' current state) | `SCHEDULED` — rides the next `07`/`09` invocation that touches this row. | SCHEDULED |
| BL-0083 | 2026-07-31, run #114 (`01-vision`, MSTR-001 v1.5) | doc-defect | `docs/roadmap/04-release-roadmap.md`'s R9 entry ("Visual Evolution & Audio-Visual Synchronization") states "**Prerequisite not yet satisfied:** the visual-evolution research thread (`MSTR-001` §9) has not been run — this release cannot be architected until that research lands." This is now false: `R222` (visual-evolution conventions) and `R223` (audio-visual-synchronization conventions), both authored 2026-07-26, satisfy it, and `MSTR-001` v1.5 (this session) formally cycled both topics' findings into §9, promoting the thread to the same "concretely groundable" tier as R5/R6/R7's own threads. `R104` §7-8's VRAM/palette-budget addendum (also already authored) supplies the one piece of quantification R222 itself named as still owed. All three of R9's grounding prerequisites are therefore satisfied; only the roadmap's own prerequisite note has not caught up. | Low-Medium (the roadmap document itself is a read-only cross-cutting input no pipeline stage owns to edit per its own convention — but leaving it stale risks a future run skipping R9 on the mistaken belief its research is still outstanding) | Whichever skill next touches R9 — most naturally `03-architecture-design-synthesis` when it picks up R9's own `ADS-xxx`, correcting the note as part of that pass, since the roadmap is read-only to every other stage | **Confirmed still stale 2026-07-31 (run #114) — `ADS-106` (`03-architecture-design-synthesis`) picked up R9 this same run and, correctly per its own write scope, did NOT edit `docs/roadmap/04-release-roadmap.md` itself (a read-only cross-cutting input to every stage). Re-flagged for the pipeline manager's own reconciliation step, or `05-feature-decomposition`/`07-implementation-planning` the next time either touches R9's roadmap row.** | SCHEDULED |
| BL-0084 | 2026-07-31, run #114 (`03-architecture-design-synthesis`, `ADS-106` §9 OQ1) | recommendation | `ADS-106`'s `THEME_TABLE` mechanism is fully specified, but its 3 non-default rows' exact color values (Techno/Chiptune-Driving, Ambient/Lo-Fi, Holiday themes) are left to content authoring, same convention `STYLE_TABLE`'s own untuned values already follow (`BL-0005`-class deferral). Not blocking implementation planning — the mechanism doesn't need the values to be fully specified. | Low | `08-content-authoring` (or `09-content-review`, once a package exists to review against) | `SCHEDULED` — rides whichever future package implements `RM-9001`. | SCHEDULED |
| BL-0085 | 2026-07-31, run #114 (`03-architecture-design-synthesis`, `ADS-106` §9 OQ2/OQ3) | design-question | Two sizing/sequencing calls `ADS-106` explicitly left open rather than deciding: (1) `RM-9002`'s exact mood-modulation rule (how `AROUSAL`/`VALENCE` combine with the now-designed style theme) is deferred to a future `03`/`06` pass once `RM-9001` ships and the mechanism has a proven first application; (2) whether `RM-9001`/`RM-9002` should be one feature/package or two is a `05-feature-decomposition`/`07-implementation-planning` sizing call — `ADS-106`'s own position is two, since `RM-9002` depends on a WRAM signal (`IP-1120`'s `AROUSAL`/`VALENCE`) the v1 mechanism doesn't yet consume. | Low (ordinary forward planning, not a gap) | `05-feature-decomposition` (when R9 reaches feature-catalog decomposition) | `SCHEDULED` — rides `05-feature-decomposition`'s next pass over roadmap R9. | SCHEDULED |
| BL-0086 | 2026-07-31, run #117 (self-audit of this session's own output) | doc-defect | **`ADS-106` mischaracterizes the mechanism it extends, and `NFR-1190` baselines that mischaracterization as a requirement.** `visuals.py:91-95`'s `_emit_write_palette(rom, colors)` takes a Python list at *assembly* time and emits 36 bytes of **inline immediates** (1 `BCPS` setup + 8 `LD A,n`/`LDH (n),A` pairs). **No ROM-resident palette table exists anywhere**; the calm/bad choice is a branch between two fully-emitted immediate blocks (`visuals.py:154-161`). `ADS-106` §2 describes the change as swapping "one constant reference" for "an indexed table read", and `NFR-1190` requires "zero unconditional per-frame CPU cost beyond one indexed table read replacing one constant reference" — both false. A real indexed implementation additionally needs: a new ROM data table (none today), a **new WRAM read of `CHMIX_IDX` in the palette path** (confirmed absent — `CHMIX_IDX` is read only *after* `uv_palette_done`, in the settings block, with `A`/`HL` clobbered between), ×8 index scaling (SM83 has no scaled addressing), and 8 indexed reads replacing 8 immediates. Net: a per-frame **increase** on the exact path `IP-9030` measured at a handful of instructions of VBlank margin. **Design not considered because of the error:** keeping the immediate-write shape and branching to one of N emitted blocks trades ~288 bytes of ROM (against 29349 free, `R104` §7) for per-frame cost *identical to today* — plausibly the correct answer on this project's real constraint profile, but choosing it is a corrected `03` pass, not an audit's call. | **High** (nothing shipped and no code is wrong — the ROM is correct; but a baselined requirement is factually wrong about the system it constrains. If `07` plans and `08` builds from it, the cost criterion is unmeetable as written, forcing a silent violation or a mid-build redesign — a stage-08 Blocking Report) | `03-architecture-design-synthesis` (correct `ADS-106` §2/§6 and its Decision Log against the real mechanism, and re-decide the selection rule with the branch-to-N-blocks option on the table), then `04-requirements-engineering` (re-word `NFR-1190` to the corrected cost model) | `NEEDS-USER` — a High finding may not be quietly deferred. Decision required: correct `ADS-106`+`NFR-1190` now (before R9 reaches `05`/`07`), or accept the risk and correct at planning time. Recommend correcting now — it is two document edits today versus a blocked package later. **DE-ESCALATED 2026-08-07 by the v1.0 scope decision:** the user scoped v1.0 as R0-R8 + R12 + R12.5 + R13, putting **R9 in v1.1**. This therefore no longer blocks the release — it is v1.1 cleanup. Severity stands as filed (a baselined requirement is still factually wrong about the system it constrains) but it is no longer urgent and no longer needs a user decision: **default is to correct `ADS-106` §2/§6 + Decision Log and re-word `NFR-1190` as part of the next doc-coherence pass**, alongside `BL-0087`/`BL-0091`/`BL-0092`/`BL-0093`, which land in the same documents. | SCHEDULED |
| BL-0087 | 2026-07-31, run #117 (self-audit) | doc-defect | **`GDS-08` misdirects its own readers, and `ADS-106`/`NFR-1190` inherited the error.** `GDS-08` §0 (line 20) states "the stateless re-render contract (§5)", but §5 is *Presentation is strictly downstream — the read-only invariant*; the contract is stated verbatim in **§3** (*The sync scheme — and why it is stateless*): "Contract: every frame, read current engine state and re-render every cell from scratch." `BL-0069`'s own text cites §3 correctly, so the tree is inconsistent with itself. `ADS-106` §6 (line 132) and `NFR-1190` both cite §5 — wrong, and wrong specifically because `GDS-08`'s own summary was trusted rather than checked. | Low-Medium (doc-coherence; no functional impact, but it is a load-bearing contract cited by a baselined NFR, and the misdirection actively propagates) | `03-architecture-design-synthesis` for `GDS-08` §0's own cross-reference (its document); the `ADS-106`/`NFR-1190` citations ride `BL-0086`'s correction pass | `SCHEDULED` — rides `BL-0086`'s correction pass for the two inherited citations; `GDS-08` §0's own fix rides the next `03` pass that touches it. | SCHEDULED |
| BL-0088 | 2026-07-31, run #117 (self-audit) | finding | **`T20` carries dead code and two inconsistent expected-value helpers.** (1) `test_rom.py`'s `non_decreasing = [...]` (immediately before `T20.2`) is assigned and never used. (2) Two helpers compute the same thing: `expected_arousal(pb)` reads *both* indices from memory (robust; used by `T20.6`-`T20.12`), while module-level `expected_arousal_from(tempo, density)` is called by `T20.1`/`T20.3` with **hardcoded** `PRESET_DENSITY_IDX`/`PRESET_TEMPO_IDX`. The hardcoded form passes only because the first song-form transition lands at frame ~1764 while those traces run ~24 frames — an unstated dependency that would fail confusingly if `BOOT_FRAMES` or `SONG_TABLE[0]`'s duration ever changed. The robust helper makes the fragile one unnecessary. (3) Related, not new: `T20.2`/`T20.4` are near-tautological given `T20.1`/`T20.3` — an instance of the pattern `BL-0062` already tracks, not a separate defect. | Low (test-code quality; all 142 checks pass and do test the right properties — this is latent fragility plus dead code, not a wrong assertion) | `08-code-implementation` (fold into whichever package next touches `test_rom.py`; deliberately **not** fixed in-pass, per stage-08's own no-"while I'm here"-fixes rule) | `SCHEDULED` — rides the next `08-code-implementation` run touching `test_rom.py`, alongside `IP-1120`'s own `09-package-verification`. | SCHEDULED |
| BL-0089 | 2026-08-07, run #118 (full-repo audit) | doc-defect | **`08-content-authoring`'s entire declared write scope is files that do not exist.** The skill defines its scope as "the data halves of `tiles.py`, `patterns.py`, `music_data.py`"; none of the three is in the tree (root holds only `build_rom.py`, `gbc_lib.py`, `input_map.py`, `music_engine.py`, `test_rom.py`, `visuals.py`, `wram_constants.py`). `GDS-09` §2 already records this honestly at the *interface* level ("**Three of those five do not exist here** … no `tiles.py`, no `patterns.py`, and no `music_data.py` at all — `visuals.py` owns its tile bytes"), but the skill definitions were never reconciled: **7 skill files** still name them (`08-content-authoring`, `08-code-implementation`, `08-refactoring`, `09-content-review`, `02-research-game-design`, `run-driftune`, `README`). No work has actually been blocked — no `IP-xxxx` names `08-content-authoring` as executor and it has never been invoked — but the skill cannot write anything as specified, and `07-implementation-planning` would route a content package to it. | Medium (a stage-08 peer is unusable as written; latent, not yet hit) | The skill-definition owner (`.claude/skills/` is outside every pipeline stage's write scope). Precedent: `BL-0076` resolved an identical class of defect by correcting the skill files directly. Options: repoint the scope at where content actually lives (`visuals.py`'s tile/palette data, `music_engine.py`'s tables), or record the three modules as a deliberate never-built deviation and narrow the skill. | **ANSWERED 2026-08-07: create the three modules.** The user chose the most architecture-faithful option over repointing or retiring — extract the data out of `visuals.py` and `music_engine.py` into real `tiles.py` / `patterns.py` / `music_data.py`, restoring the module decomposition `GDS-03`/`GDS-09` always described. **Consequence, recorded honestly:** this is a genuine `IP-8xx0` **refactoring package** (executor `08-refactoring`) carrying an equivalence contract — byte-identical ROM, identical full-suite check-name set — touching the two largest modules, and it becomes a **prerequisite on the critical path to R12.5**, since the tuning pass needs a working content surface. It also requires its own G3 authorization (refactoring packages are never pre-authorized). Next: `07-implementation-planning` authors the package; `GDS-09` §2's honestly-recorded "three of those five do not exist" note is superseded once it lands. | SCHEDULED |
| BL-0090 | 2026-08-07, run #118 (full-repo audit) | doc-defect | **Research-topic tallies say 47; there are 48.** `docs/research/INDEX.md` line 86 ("all 47 topics carry a forward-trace section") and line 91 ("Whole-encyclopedia tally: 47 topics — 33 traced, 14 …"), and `ROADMAP.md`'s stage-02 row ("**47 topics**"), all predate `R310` (refactoring practices, authored 2026-07-31 to close `BL-0076`). Audit confirmed `R310` **is** listed in the index and **does** carry its `## 6b. Forward trace` section, and that **all 48 topics carry one** — so the `MSTR-001` C10 coverage claim is still true, only the counts are stale. | Low (bookkeeping; the substantive C10 claim survives) | `02-research-tooling-and-testing` (owns `R310`'s tier) or whichever `02-*` skill next touches the index; `ROADMAP.md`'s row rides the same pass | `SCHEDULED` — rides the next `02-research-*` run, or any pass that touches `docs/research/INDEX.md`. | SCHEDULED |
| BL-0091 | 2026-08-07, run #118 (full-repo audit) | doc-defect | **`GDS-08` §5's structural invariant is falsified in its literal form by `IP-9030`.** §5 states: "every write in the module targets VRAM, the palette ports, or `LCDC` — **never a `0xC0xx` engine field**, never a PSG register," and cites the 16-package integration review as having confirmed it structurally. `IP-9030` subsequently added `rom.LD_nn_A(VIS_ENTRY_LY)` at `visuals.py:139`, with `VIS_ENTRY_LY = 0xC061` (`visuals.py:61`) — a `0xC0xx` write from inside `visuals.py`. **The invariant's substance holds**: nothing in the engine reads `VIS_ENTRY_LY` (only `test_rom.py` does), so presentation still cannot cause a musical behavior, which is what §5 is actually protecting. But §5's stated *proof criterion* is now false, and §5 leans on that criterion as its evidence. A future reviewer re-running the check finds a `0xC0xx` write and must either conclude the invariant broke or discover the carve-out unaided. Audit separately confirmed the rest holds: `input_map.py` contains **0** `NRxx` references; `visuals.py` writes no PSG register (its only `NR` use is an `LDH_A_n` **read** of `NR52`). | Low-Medium (doc-accuracy on a load-bearing architectural invariant; no functional risk) | `03-architecture-design-synthesis` (restate §5's criterion as "never a field any engine routine reads," or carve out the write-only diagnostic explicitly) | `SCHEDULED` — rides the next `03` pass touching `GDS-08` (`BL-0086`/`BL-0087`'s correction pass is the natural ride, since it already reopens `GDS-08`). | SCHEDULED |
| BL-0092 | 2026-08-07, run #118 (full-repo audit) | finding | **The ROM-budget re-measurement discipline `ADR-0002`/`R104` §8 established has not run for 8 packages, and 8 documents carry the stale figure.** `ADR-0002` fixed the method (`rom.pos` read after `build_rom.build()`) and its result — 3419/32768 used (10.4%), **29349 free** — measured after the 12 R1-R4 packages. `R104` §8 mandates "Re-measure ROM budget the same way … before and after any future visual-evolution package ships, to keep the budget-tracking discipline … consistent across all future content additions." Eight packages have landed since (`IP-1080`/`1090`/`1100`/`1110`/`9030`/`8010`/`8020`/`1120`) with no re-measurement. **Audit measured it: 4293/32768 used (13.1%), 28475 free** — 874 bytes of drift. The stale figure is cited in `R104` §7, `ADR-0002`, `GDS-00`, `ADS-101`, `ADS-102`, **`ADS-106`** (authored this session), `docs/roadmap/04-release-roadmap.md`, and `strategic-assumptions-register.md` (A5). **Every conclusion drawn from it survives** — 28475 free is still ample and no bank-switching re-trigger fires — so this is staleness, not a wrong decision. | Low (no decision changes; but a tracked metric with named re-triggers is not being tracked, same shape as `BL-0077`'s "nothing checks this") | `03-architecture-design-synthesis` (owns `ADR-0002`/`GDS-00`/the `ADS`s) for the figure refresh; consider folding the measurement into the G5 gate alongside the ROM/test gates so it self-maintains | `SCHEDULED` — rides `BL-0086`'s correction pass (which already reopens `ADS-106`'s budget paragraph). | SCHEDULED |
| BL-0093 | 2026-08-07, run #118 (full-repo audit) | doc-defect | **`BL-0066` was closed while its own disposition records an unfinished half — and that half is still undone.** `BL-0066`'s Status is `DONE`, but its disposition text reads: "`DONE` (FS half) … **GDS-03's own patch-point text remains open** and rides the next `03` pass." Verified still open: `docs/architecture/03-architecture.md:20` reads "`build_rom.py` — master build: imports all modules, lays out ROM sections, **patches pointers**" — describing the patch-point mechanism `IP-8010` deleted (`BL-0064`). What `build_rom.py` actually does is call `rom.resolve()` (line 92), `gbc_lib.py`'s **label-fixup** second pass — a different mechanism with a different name. `GDS-09` §3 ("The patch-point contract is vestigial — a finding") explicitly flags `GDS-03` as still carrying it. **The integrity issue is the closure**: a `DONE` row hides live work from every future triage sweep, which is exactly what the backlog exists to prevent. | Low-Medium (one small doc edit; but the closure pattern is the real finding — severity is about the precedent, not the sentence) | `03-architecture-design-synthesis` for `GDS-03` line 20; `00-pipeline-manager` for the closure convention (an entry with a recorded unfinished half must stay open or be split into a successor entry) | `SCHEDULED` — reopen as this entry (successor to `BL-0066`'s second half); rides the next `03` pass. | SCHEDULED |
| BL-0094 | 2026-08-07, run #118 (full-repo audit) | doc-defect | **`Claude.md` carries the same stale patch-point phrasing as `GDS-03`, and was never in any entry's scope.** `Claude.md:27`: "`build_rom.py` — master build: imports all modules, lays out ROM sections, **patches pointers**" — identical to `GDS-03:20` (`BL-0093`). `BL-0064`/`BL-0066` enumerated three affected document families (GDS-03, the GDS-09 ladder row, six `FS-1xx` *Interfaces Used* fields) and fixed two; `Claude.md` was in none of them, despite being the project's highest-traffic developer quick-reference. | Low (doc-accuracy) | `08-code-implementation` (`Claude.md` is in its documentation-update scope) or the same `03` pass that fixes `BL-0093`, whichever lands first | `SCHEDULED` — rides `BL-0093`'s fix, or the next package whose Documentation Updates touch `Claude.md`. | SCHEDULED |
| BL-0095 | 2026-08-07, run #119 (release-state review) | doc-defect | **`docs/feature-planning/` holds 1 of the 5 deliverables its owning skill mandates, and `00-pipeline-manager` reconciles against one of the missing four every run.** `05-feature-decomposition` requires `01-release-plan.md`, `02-epic-catalog.md`, `03-feature-catalog.md`, `04-feature-dependency-graph.md`, `05-feature-review.md`. On disk: only `01-feature-catalog.md` (the catalog, numbered `01-` rather than the mandated `03-`) plus `INDEX.md`. **`00-pipeline-manager`'s Step 1 names `docs/feature-planning/01-release-plan.md` as an authoritative ledger to reconcile the Position block against** — that file has never existed, so that reconciliation input has silently no-opped on every manager run to date, this session's included. `INDEX.md` compounds it: it lists the file under a *third* name (`00-release-plan.md`) marked `⛔ Planned`, the same misleading-debt-vs-deliberate-deviation framing `BL-0068` already had to correct once for `docs/requirements/INDEX.md`. **Materially, the release state is not lost** — it lives in `docs/roadmap/04-release-roadmap.md` (R0-R13) and the epic/dependency/review content is genuinely absent rather than misplaced. The question is whether that is a deliberate deviation (as the requirements dir's missing files were adjudicated to be) or real debt. | Medium (the pipeline's own driver reads a ledger that isn't there; a documented reconciliation input is inert, and nothing would have surfaced that) | `05-feature-decomposition` (adjudicate: author the missing deliverables, or record them as deliberate deviations and renumber/relabel the index the way `BL-0068` did for requirements), then the `00-pipeline-manager` skill definition if the reconciliation list needs repointing | **Confirmed exhaustively 2026-08-07 (run #120): the file has never existed anywhere.** Searched all 6 refs (2 local, 4 remote incl. both `worktree-agent-*` branches) at tip, plus every commit ever made on any ref (`git log --all --diff-filter=A`), plus all reachable objects and stashes. `docs/feature-planning/` has held exactly two paths in its entire history: `01-feature-catalog.md` and `INDEX.md`. No `release-plan`, `epic-catalog`, `feature-dependency-graph` or `feature-review` file was ever added on any branch. **This materially strengthens the deliberate-deviation reading**: `docs/roadmap/` independently covers most of the same ground under different ownership — `04-release-roadmap.md` (release sequencing, R0-R13) for the release plan, `02-capability-map.md` (CAP-xx) as the epic analogue, `03-capability-dependency-graph.md` for dependency structure. Only `05-feature-review.md` has **no analogue anywhere** (the catalog mentions "Feature Review" once, inline, rather than carrying one). So the honest scope of the gap is narrower than first filed: it is one genuinely absent deliverable plus a naming/ownership mismatch, not four missing documents. **Substantive half CLOSED 2026-08-07** — `docs/feature-planning/01-release-plan.md` authored (origin → releasable v1.0; all 13 `FEAT` and all 49 open `BL` assigned; `00-pipeline-manager`'s Step 1 reconciliation input is now real). `02-epic-catalog.md` and `04-feature-dependency-graph.md` **adjudicated as deliberate deviations** citing their `docs/roadmap/` substitutes, recorded in `docs/feature-planning/INDEX.md` the way `BL-0068` handled `docs/requirements/INDEX.md`. **Residual, still owed:** `05-feature-review.md` — a genuine gap with no analogue anywhere; recommended before R8 adds `FEAT-1130`. | SCHEDULED |
| BL-0096 | 2026-08-07, run #119 (release-state review) | doc-defect | **`docs/roadmap/04-release-roadmap.md`'s R6 and R7 entries carry no Status line at all**, while R0/R1/R2/R3/R4/R4.5/R5 each do. Both have moved since the roadmap was written: **R6 (Song-Form) is shipped and GO-confirmed** — `Claude.md`'s Known Good Behavior header records "full R1-R6 + `IP-1090` + `IP-1110` GO confirmed by the user 2026-07-31" — and **R7 (Emotional/Energy) is `COMPLETE`** via `IP-1120` (this session), awaiting fresh-session verification. A reader consulting the roadmap for release state sees R5 shipped and nothing after it. This is the third stale-roadmap finding (with `BL-0083`'s R9 prerequisite note and `BL-0082`'s `ROADMAP.md` stage-07 row), which together indicate the roadmap is not being updated as releases land — the underlying pattern is that no stage owns writing to it (every stage treats `docs/roadmap/` as a read-only cross-cutting input). | Low-Medium individually; **Medium as a class** with `BL-0082`/`BL-0083` — the release-state view of the project is the one most likely to be read by someone orienting quickly, and it is the least maintained | `11-release-readiness` is the natural owner (it already flips baseline records on GO and would be touching release state anyway); alternatively the ownership gap itself needs deciding, since no stage currently writes `docs/roadmap/` | `SCHEDULED` — bundle with `BL-0082`/`BL-0083` into one roadmap-reconciliation pass; recommend `11-release-readiness` take ownership of the roadmap's status lines. | SCHEDULED |
| BL-0097 | 2026-08-07, run #121 (`05-feature-decomposition`, release plan §10 finding 1) | finding | ~~**`09-content-review` has never run — not once, on any branch, in the project's entire history**~~ **(RESOLVED 2026-08-14 — it has now run; see the update at the end of this cell.)** As originally filed: searched all refs and every commit; only the skill file itself existed, no artifact was ever produced. Meanwhile `music_engine.py` carries **15** constants explicitly marked first-guess / not-tuned-by-ear per `BL-0005`'s convention: tempo steps, scale semitone sets, Euclidean density patterns, dissonance/stale/overload thresholds, `STYLE_TABLE` rows, `SONG_TABLE` phase targets, motif variants, `VALENCE_TABLE`. **This project is a music generator whose every musical constant has been chosen by reasoning and never evaluated by ear.** 142/142 tests prove the engine does what it was specified to do; they cannot prove it sounds good, and no stage has yet asked. `docs/roadmap/09-release-exit-criteria.md` already *requires* a listening pass "for any release touching musical character (R5, R6, R8, R9)" — **that criterion has never been satisfied for R5, R6 or R8**, so three shipped releases are, strictly, out of compliance with the project's own exit bar. Motivates the proposed **R12.5 — Content & Musical Quality Pass** ([`01-release-plan.md` §2.5](../feature-planning/01-release-plan.md)). **PARTIALLY DISCHARGED 2026-08-14 — the review half is done, the retuning half is not.** The first-ever `09-content-review` pass ran across all 17 `VERIFIED` packages ([content-review-full-baseline.md](../reviews/content-review-full-baseline.md), commit `c267b52`), producing 5 findings, no Critical/High, with `R224`'s per-parameter question set. **What that changes:** the "never run" claim above is no longer true, and the exit-criteria listening-pass requirement now has a real artifact behind it for R5/R6/R8's scope. **What it does not change:** the constants are still untuned — the review *evaluated* them, it did not *retune* them (that is out of `09-content-review`'s read-only scope by design), so R12.5 is still owed and this entry stays `SCHEDULED`. The review's own findings are now filed as `BL-0102` (default preset enters the bad zone at boot, Medium-High), `BL-0103` (`DENSITY_IDX` non-monotonic, Medium), `BL-0104` (bad-zone oscillation uncharacterized, Low-Medium) and `BL-0105` (no acoustic-verification capability, Low) — those four are the concrete shape R12.5's scope should now take, replacing this entry's original guess at it. | **Medium-High** (no correctness defect and nothing is broken; but it is the largest quality risk in the project and the one the test suite is structurally blind to — a v1.0 could satisfy every requirement and still be a poor listening experience, which is the actual product) | `00-pipeline-manager` to schedule `09-content-review` across the shipped feature set; the roadmap owner to adopt R12.5; blocked in practice by `BL-0089` (`08-content-authoring`'s write scope must be real before retuning can execute) | `SCHEDULED` — R12.5 per the release plan, **now scoped by real evidence rather than anticipation**: the recommended "first `09-content-review` pass before then" was taken 2026-08-14, and its findings (`BL-0102`-`BL-0105`) are what R12.5 should be planned against. This entry stays open for the retuning half only. | SCHEDULED |
| BL-0098 | 2026-08-07, run #121 (`05-feature-decomposition`, release plan §10 findings 2/4) | doc-defect | **Two roadmap corrections the release plan identified but cannot make itself** (`docs/roadmap/` is read-only to every pipeline stage): (1) **R11 (Persistence) should be marked struck, not pending.** Its own entry states "**R4.5's decision must have been GO** … If R4.5 was NO-GO, this release is **struck from the roadmap entirely, not deferred indefinitely** — an explicit no is a valid outcome." `ADR-0002` decided NO-GO on 2026-07-25; R11 still reads as pending work. (2) **R12.5 — Content & Musical Quality Pass needs adoption** into the roadmap's own sequence (see `BL-0097`); the release plan proposes it using the half-numbered-checkpoint precedent R4.5 already established. Bundle with the standing roadmap-staleness cluster (`BL-0082`, `BL-0083`, `BL-0096`) — four findings now trace to one cause: **no pipeline stage owns writing `docs/roadmap/`**. | Low-Medium each; **Medium as a class** — the release-state view is the most-read orientation surface and the least maintained | Recommend `11-release-readiness` formally take ownership of `docs/roadmap/`'s status lines and release set, since it already flips baseline records on GO | `SCHEDULED` — one roadmap-reconciliation pass covering `BL-0082`/`BL-0083`/`BL-0096`/`BL-0098` together. | SCHEDULED |
| BL-0099 | 2026-08-07, run #123 (`03-architecture-design-synthesis`, `ADS-107` §8 Risks) | finding | **`ADS-107` (Genre Blending, roadmap R8) supersedes `FR-1240`'s instant-apply guarantee, and named one concrete downstream casualty this pass could see but not fix**: `test_rom.py`'s existing `T15` suite asserts style application lands on `TEMPO_IDX`/`DENSITY_IDX`/`DUTY_BIAS` the **same frame** Start is pressed — that assertion becomes false once `IP-1130` (R8's implementation package) ships the blend, since those three fields now land after 4 discrete steps, not instantly (`SCALE_IDX` keeps the same-frame guarantee). `ADS-107` itself flags this in its own Risks section so `07-implementation-planning` doesn't discover it mid-package, but no backlog entry existed to carry it forward once this session ends. | Low-Medium (a known, already-scoped test-update need, not a surprise defect — but real work `IP-1130`'s own Files to Create/Modify must name explicitly) | `07-implementation-planning` (name `test_rom.py`'s `T15` same-frame checks in `IP-1130`'s Files to Create/Modify, updated to assert the 4-step landing instead) | `SCHEDULED` — rides `IP-1130`'s own planning pass, per `01-release-plan.md` §2.2's R8 pipeline chain. | SCHEDULED |
| BL-0100 | 2026-08-07, run #125 (`05-feature-decomposition`, Feature Review) | doc-defect | **`NFR-1000`/`NFR-1030` are baselined but owned by no `FEAT-xxxx` row** — a full-catalog Feature Review (this project's genuinely-missing `05-feature-review.md` deliverable, run standalone as a health check while `FEAT-1130` was being added) found both unassigned. `NFR-1000` (ROM builds to a fixed size with a valid header) doesn't cleanly belong to any single listener-visible feature; `NFR-1030` (preset/threshold constants are first-guess placeholders) is the same shape. Both are **satisfied in practice** — `test_rom.py`'s `T1` suite checks `NFR-1000` directly, and `BL-0005` already tracks `NFR-1030`'s tuning debt — so this is catalog bookkeeping, not unmet behavior. | Low (no functional gap; a traceability-completeness item) | `07-implementation-planning` or a future `05` pass: add `NFR-1000` to `FEAT-1050`'s traced-requirement column (same infrastructure category as its existing `NFR-1010`/`NFR-1020`) and `NFR-1030` to `FEAT-1000`'s (the natural owner of the preset tables it describes) | `SCHEDULED` — rides the next pass that touches `docs/feature-planning/01-feature-catalog.md`'s FR/NFR-traced column. | SCHEDULED |
| BL-0101 | 2026-08-07, run #125 (`05-feature-decomposition`, catalog cross-check) | doc-defect | **`FEAT-1120`'s own catalog row states a stale trigger-site count.** It reads "3 input-step routines, the song-form tick, and boot/Select-reset" for `AROUSAL`/`VALENCE`'s recompute sites — `FS-112`/`IP-1120` later corrected this to **4** input-step routines (`TEMPO_IDX` has two writers, D-pad Up *and* Down, not one), 6 sites total. Noticed while adding `FEAT-1130` immediately below it in the same file; flagged rather than silently fixed, since `05-feature-decomposition`'s own write scope is the catalog's *organization*, not re-verifying a shipped feature's implementation-grade detail after the fact. | Low (doc-accuracy only — the shipped code and `FS-112`/`IP-1120` themselves have always carried the correct count; only this one summary row lagged) | `05-feature-decomposition` (a one-line correction to `FEAT-1120`'s own summary text) or `08-code-implementation`'s documentation-update scope if it next touches this file | `SCHEDULED` — rides the next pass that touches `FEAT-1120`'s own row, or `IP-1120`'s eventual `09-package-verification`, whichever comes first. | SCHEDULED |
| BL-0102 | 2026-08-14, intake: file `09-content-review`'s findings (F1) | finding | **The shipped "known-good" default preset reproducibly enters the dissonant bad zone almost immediately at boot** — 5/5 independent fresh boots showed `BAD_ZONE_FLAGS`' combined bit already set at the first sampled post-boot-settle frame, `DISSONANCE_SCORE` in the high-20s/low-30s against `DISSONANCE_THRESHOLD=20`. Autonomous recovery (`IP-0007`) does work and does pull it back, but the practical consequence is that the **red bad-zone palette is what a first-time listener sees first, almost every time**, directly undercutting the "calm default" design intent (`GDS-03` §5's known-good preset). Source: [content-review-full-baseline.md](../reviews/content-review-full-baseline.md) F1, surfaced by `R224`'s bad-zone-recovery manipulation-check question. **Named caveat, carried forward honestly rather than concluded past:** the review flags that PyBoy's deterministic `DIV`-seeded LFSRs may produce the same early trajectory on every headless boot, so this may partly be a harness-determinism artifact rather than pure threshold miscalibration — confirming whether it reproduces across varied seeds/real hardware is the *first* step, before any retuning of `DISSONANCE_THRESHOLD`/`LFSR_SEED_*`/`PRESET_*`. Distinct from `BL-0005` (which records that these constants are untuned placeholders in general); this is the first *measured, reproducible listener-visible consequence* of that. | **Medium-High** (the review's own assessment, recorded unchanged — no crash, no correctness defect, and recovery works; but it is the single most listener-visible finding of the project's first content review, and it contradicts a stated design intent) | `02-research-game-design` (confirm seed/hardware variance first, then ground whether the threshold or the preset's own starting interval relationships are miscalibrated), handing to `07-implementation-planning`/`08-code-implementation` only once the cause is settled | `SCHEDULED` — rides the next `02-research-game-design` pass alongside `BL-0104` (same evidence run, "decide before retuning" ordering); feeds R12.5's retuning scope once settled. Not urgent enough to preempt the standing `09-package-verification` queue (`IP-9030`/`IP-8010`/`IP-8020`/`IP-1120`), which this run's next step addresses first. | NEW |
| BL-0103 | 2026-08-14, intake: file `09-content-review`'s findings (F2) | finding | **`DENSITY_IDX` does not monotonically increase onset rate — the nominal densest setting produces *fewer* measured onset events than several sparser ones.** Measured onset-event counts (NR52 bit3 rising edges, 600-frame window, all other state at boot default) across `DENSITY_IDX=0..7`: `10, 14, 19, 38, 28, 35, 27, 13`, against `DENSITY_K=[2,3,4,5,6,8,10,12]`. Indices 0-2 track `k` closely; index 3 overshoots (38 vs. ≈23 expected); indices 6-7 undershoot badly (27 vs. ≈47; **13 vs. ≈56**). Musically: `DENSITY_IDX`'s higher settings do not reliably read as "busier", which is the entire point of the control. Source: [content-review-full-baseline.md](../reviews/content-review-full-baseline.md) F2, surfaced by `R224`'s density manipulation-check question. **Root cause not isolated in this pass**, and the review names two competing explanations rather than guessing between them: (a) `IP-0007`'s autonomous note-timer-doubling overload throttle (index 6 showed 154/600 overload frames) — but index 7 showed **zero** overload frames, so this cannot be the whole story; (b) `NR52`-bit-continuity undercounting, where two onsets landing close enough together never let the channel's envelope/DAC drop back to inactive between them, merging two real onsets into one observed event — more likely at high `k`, exactly where the undercount is largest. | **Medium** (the review's own assessment — a real, measured contradiction of the control's design intent, and exactly the routing-actionable kind of finding a content review exists to produce: it lands on a named mechanism, not a vague "sounds off") | `02-research-tooling-and-testing` first — add register-write-level (not `NR52`-active-level) onset instrumentation to conclusively separate explanation (a) from (b); only then does the retuning question (`DENSITY_K`'s upper values vs. `OVERLOAD_THRESHOLD` vs. the noise channel's envelope/length settings) route to `07`/`08-code-implementation`. Filed upstream deliberately: retuning before the measurement is trustworthy would be tuning against an instrument that may itself be miscounting. | `SCHEDULED` — rides the next `02-research-tooling-and-testing` pass, alongside `BL-0105` (same tier, adjacent instrumentation scope); feeds R12.5. Behind the verification queue in this run's ordering. | NEW |
| BL-0104 | 2026-08-14, intake: file `09-content-review`'s findings (F3) | design-question | **Bad-zone entry/recovery cycles quickly and repeatedly under ordinary default no-input play** — max 120 consecutive combined-bad-zone frames observed in a 4000-frame no-input run, re-entering and re-exiting at a period on the order of a few hundred frames. Distinct from `BL-0102` (which is about the *first* entry, at boot): this is steady-state behavior, persisting long after the initial recovery. Source: [content-review-full-baseline.md](../reviews/content-review-full-baseline.md) F3, same underlying evidence run as F1. **This is filed as a question, not a defect, deliberately** — a self-correcting oscillation may well be a desirable "living" quality rather than a fault, but the project has never characterized or decided on it either way, and any future retuning pass (`BL-0102`, `BL-0005`, R12.5) would change it incidentally without anyone having chosen to. | **Low-Medium** (informational; no functional risk, and it may turn out to be intended — the cost of leaving it open is that a retuning pass silently changes an uncharacterized behavior) | `02-research-game-design` — decide and document (in `R204`, or an `ADS`) whether this oscillation frequency is intended texture or an unintended consequence of `DISSONANCE_THRESHOLD`'s calibration, **before** `BL-0102`'s retuning conversation reaches a decision | `SCHEDULED` — rides the same `02-research-game-design` pass as `BL-0102`. | NEW |
| BL-0105 | 2026-08-14, intake: file `09-content-review`'s findings (F4) | research-gap | **Octave/register and mode/scale *acoustic* correctness cannot currently be confirmed by any harness this project has** — `NR13`/`NR14` (pulse frequency) are write-only and unreadable in PyBoy, a pre-existing, long-documented limitation (`test_rom.py`'s `t3_generation_live` docstring, `R108`, `VR-0001`). Every verification and review pass to date, this one included, can therefore only confirm octave/scale *mechanism* correctness via the WRAM mirror (the right values land, the right degree sets are reached) — never the resulting pitch or perceived mood. Source: [content-review-full-baseline.md](../reviews/content-review-full-baseline.md) F4; `R224`'s own register and mode manipulation-check questions are the first artifacts that *cannot be fully answered* because of it, which is what promotes a standing limitation into a filed gap. Shares its root cause with `BL-0027` (portamento's missing dynamic test) but asks a different question: `BL-0027` wants a test for one behavior, this asks whether the **capability** exists at all. Concrete ask: evaluate whether PyBoy's `sound_emulated=True` APU state can yield raw audio samples (the review did not attempt extraction), and if so whether adding sample capture to `run-driftune`'s toolkit is worth it. | **Low** (the review's own assessment — a disclosed, pre-existing testing-capability gap, not a new defect; nothing is broken, but it bounds what every future listening pass can conclude) | `02-research-tooling-and-testing` | `SCHEDULED` — rides the same `02-research-tooling-and-testing` pass as `BL-0103`. | NEW |
| BL-0106 | 2026-08-09, `VR-1130` (fresh-session re-verification pass, F1) | finding | **A general, pre-existing, ~30-frame-periodic engine-wide characteristic** — independent `pyboy.hook_register` instruction-level tracing shows `blend_tick` is called `{0-then-2}` times per harness `tick()` on a clean 30-frame period, present even during pure idle running with zero blend activity (`BLEND_STEP` pinned at 4 the whole time). No functional requirement is violated (`FR-1480`'s exact-landing guarantee holds in every case independently tested) — but `IP-1130`'s own remediation commit message, `music_engine.py`'s docstrings/comments, and the Master Build Plan's `IP-1130` row all attribute the effect narrowly to "the combined `begin_blend`+`blend_tick` cost on the Start-press frame itself," a claim independent instrumentation falsifies as too narrow: the phenomenon is present with zero blend activity and is plausibly the same underlying effect as the already-disclosed "near-exhausted VBlank budget" (`R101` §8.5) / `T9.3` self-healing race. | **Medium** (`VR-1130`'s own assessment — no functional defect, but a confirmed-fact disclosure that was asserted without falsifying against a broader sample, the same "claimed-and-not-fully-falsified" pattern `BL-0069` already required correcting once) | `02-research-gbc-hardware`/`00-intake` for the underlying ~30-frame periodicity itself (engine-wide, not `IP-1130`-scoped); `07-implementation-planning`/doc-correction to narrow the inaccurate press-frame-specific claims in `music_engine.py`'s comments and the Master Build Plan's `IP-1130` row | `SCHEDULED` — the doc-correction half now rides `IP-9040` (task 8c) directly, named in its own Implementation Tasks; the periodicity investigation is a separate, lower-urgency research thread, not blocking any release gate. | IN PIPELINE |
| BL-0107 | 2026-08-09, `VR-1130` (F2) | doc-defect | **`test_rom.py`'s `T17.6` test-change carries a factually incorrect justification.** The change's comment/commit message states the old "song-form value wins the collision" assertion is "no longer true now that `blend_tick`... runs after `song_tick`" — independently falsified by rebuilding the pre-remediation commit (`7c9ccb2`) directly: that call order (`badzone_tick → song_tick → blend_tick`) was already present before `IP-1130`'s remediation, not introduced by it. The test change's *result* is correct (independently re-confirmed) — only the stated reasoning is wrong; the more likely actual cause is that the pre-fix defect made `blend_tick`'s overwrite coincidentally read back close to `song_tick`'s value for the specific pairing `T17.6` exercises. | Low-Medium (`VR-1130`'s own assessment — not a masked bug, the assertion itself is correct; a misattributed justification only) | `07-implementation-planning`/doc-correction — amend the `T17.6` comment and the commit-message-derived Master Build Plan prose | `SCHEDULED` — investigated during `IP-9040`'s own authoring (task 8d): the current `T17.6` comment already states the accurate account, the false phrasing `VR-1130` quoted is not present in the tree — confirmed already resolved, no edit needed; `IP-9040` records this explicitly rather than silently closing it. | IN PIPELINE |
| BL-0108 | 2026-08-09, `VR-1130` (F3) | doc-defect | **`NFR-1220`'s own requirement body text still reads "The 4 new blend-state WRAM bytes"** (`01-functional-requirements.md:88`), stale since the F1 remediation added 3 more (`BLEND_DELTA_TEMPO`/`DENSITY`/`DUTY`, `0xC074`-`0xC076`), now 7 total. The RTM's own forward-traceability note three lines below already correctly says "now 7 bytes... grew from 4" — an internal inconsistency within the same document, not a missed update entirely. The bound itself (bounded WRAM, ample headroom) still holds regardless of the exact count. | Low (`VR-1130`'s own assessment — one-line staleness, no functional impact) | `07-implementation-planning` — one-line correction to `NFR-1220`'s body text | `SCHEDULED` — now rides `IP-9040` (task 8a) directly, named in its own Implementation Tasks/Documentation Updates. | IN PIPELINE |
| BL-0109 | 2026-08-07, `VR-1130` prior (`RETURNED`) pass F2, re-confirmed still open by the 2026-08-09 re-verification pass (F4) | doc-defect | **`IP-1130`'s own package doc Risks field still reads "`N=16` frames is a first-guess placeholder"** (`docs/implementation/packages/IP-1130-genre-blending.md:17`) with no note of the as-shipped `N=4` deviation — independently re-confirmed still present verbatim after the F1 remediation commit (which did not touch this file). The Master Build Plan, `Claude.md`, and `GDS-07` all three already disclose `N=4` accurately — this is the one place that still lags. Never previously backlogged despite being logged twice now in `VR-1130`'s own findings tables (originally its prior-pass F2). | Low-Medium (same doc-coherence pattern as `BL-0025`/`BL-0026`, no functional impact) | `07-implementation-planning` — one-line correction to the package doc's Risks field | `SCHEDULED` — now rides `IP-9040` (task 8b) directly, named in its own Implementation Tasks/Documentation Updates. | IN PIPELINE |
| BL-0110 | 2026-08-14, `10-integration-review` (R7 verification tranche, F1) | doc-defect | **`GDS-03` §1's module-layout table omits `wram_constants.py`** — the architecture document's canonical "one file, one job" inventory still lists only the original six repo-root modules; `wram_constants.py` has existed in the tree since `IP-8020` shipped (2026-07-31), over two weeks stale at review time. `Claude.md`'s own module overview already lists it correctly — this gap is specific to `GDS-03`. No functional impact. | Low-Medium (documentation-coherence only, same pattern as `BL-0018`) | `03-architecture-design-synthesis` | `SCHEDULED` — a one-line row addition the next time `GDS-03` §1 is touched for any reason; not urgent enough to warrant its own dedicated pass. | NEW |
| BL-0111 | 2026-08-14, `10-integration-review` (R7 verification tranche, F2, headline finding) | bug | **`AROUSAL`/`VALENCE` (`IP-1120`) go silently stale on every genre-blend-triggered write to `TEMPO_IDX`/`DENSITY_IDX`/`SCALE_IDX`, violating `FR-1410`'s "recomputed on every write... within one frame" contract.** `IP-1130` (Genre Blending, shipped and independently `VERIFIED` *after* `IP-1120`, entirely outside either package's own review scope) writes all three of `mood_update`'s trigger inputs from `_emit_begin_blend`/`_emit_blend_tick` without ever calling `mood_update` — exactly the "7th write path" risk `IP-1120`'s own Risks field named in advance. Empirically confirmed live: after a Start press begins a blend, `AROUSAL` stays pinned at its pre-press value through the entire blend, never once matching `TEMPO_IDX+DENSITY_IDX`; `VALENCE` goes stale the instant `SCALE_IDX` changes and stays stale until an unrelated trigger site (Up/Down/A/B, a song-form transition, or Select) happens to fire. No player-visible consequence today — neither field has a real consumer yet, both are groundwork for roadmap R9 — which is exactly why no single-package verification caught it: `VR-1120` predates `IP-1130` entirely, and `IP-1130`'s own Requirements Covered never names `AROUSAL`/`VALENCE`, so `VR-1130`'s scope never looked there. Only visible from `10-integration-review`'s cross-package vantage point. | **Medium-High** (the review's own assessment — a baselined requirement is violated by a shipped, `VERIFIED` interaction; no current harm, but it will silently corrupt roadmap R9's first real consumer of this data unless caught first) | `07-implementation-planning` — author a small remediation package adding a `mood_update` call to `_emit_begin_blend` (covers the immediate `SCALE_IDX` case) and to `_emit_blend_tick`'s active-blend path (covers the gradual `TEMPO_IDX`/`DENSITY_IDX` case); needs its own `07`→`08`→`09` loop and G3 before it may build | `SCHEDULED` — recommend addressing before roadmap R9 begins (R9 is `AROUSAL`/`VALENCE`'s first real consumer and is the point this defect would first become player-visible); not release-blocking today since R7 has no pending GO call and no shipped feature reads these fields yet. Medium-High, not Critical/High, so this disposition is within the manager's own authority per the severity-honesty rule — not routed to `NEEDS-USER`. **`IP-9040` built under the new pre-authorized conformance-remediation path (G3 granted, both bases cited) but `BLOCKED` 2026-08-17 on a VBlank-budget regression (`BL-0112`)** — needs `07-implementation-planning` (v2) to re-scope with a cheaper approach before any further build attempt. | IN PIPELINE |
| BL-0112 | 2026-08-17, `08-code-implementation` (`IP-9040` build attempt, Blocking Report) | finding | **Wiring `mood_update` into `IP-1130`'s genre-blend write path (per `IP-9040`'s own design) regresses the VBlank budget on active-blend frames.** Both `CALL('mood_update')` insertions were implemented exactly as the package specified (after catching and correcting a register-clobbering bug in the originally-specified `_emit_begin_blend` placement — `mood_update` clobbers `A`/`B`/`C`/`HL`, and the routine's row-traversal code immediately after the specified insertion point still needed those registers; moved the call to the routine's tail instead, same frame-level semantics). Independent `VIS_ENTRY_LY` measurement on an active-blend frame confirmed the package's own named central risk: `VIS_ENTRY_LY` read `0`-`1` (outside the required 144-153 VBlank range) on both a plain single blend and the restarted-blend scenario, most visibly caught by `T22.7`'s own new check (`AROUSAL` off-by-one after a mid-blend restart settles). `mood_update`'s own added ~dozen instructions push `_emit_blend_tick`'s already-budget-sensitive per-active-blend-frame path (previously brought back within margin only by `VR-1130` F1's `BLEND_DELTA_*` precompute fix) back over budget. | **Medium** (no shipped regression — all changes were reverted before this was committed, nothing partial landed; but it blocks `BL-0111`'s own remediation and confirms the genre-blending write path's per-frame budget margin is now genuinely thin) | `07-implementation-planning` — re-scope `IP-9040` (v2) with a cheaper approach: candidates named in the package doc's own Blocking Report (settle/press-frame-only recompute instead of every active-blend frame; a purpose-built inline recompute reusing already-loaded registers instead of a full `CALL`/`RET`; isolating each call site's own cost, since the failing measurement bundled both together) | `SCHEDULED` — rides the next `07-implementation-planning` pass on `IP-9040` (v2), which is also this run's own recommended next step. Not `NEEDS-USER`: no scope/requirement question, a pure engineering re-scoping the pipeline can do itself. | NEW |

`NEEDS-USER` — nothing here is a decision only the user can make right now; BL-0007 is the closest
candidate but is explicitly deferred rather than asked, since it's genuinely low-stakes and the
vision doc already names it as revisable later.
