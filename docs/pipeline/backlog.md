# Pipeline Backlog

| ID | Filed | Type | Summary | Sev/Pri | Entry stage | Disposition | Status |
|---|---|---|---|---|---|---|---|
| BL-0001 | 2026-07-21, run #1 | research-gap | GDS-02 (System Context), GDS-04 (Domain Model), GDS-05/06 (FR/NFR at the GDS-ladder level — superseded in ordering by the direct `04-requirements-engineering` pass already done, but the ladder levels themselves are still unauthored), GDS-08 (Presentation Architecture/visualizer), GDS-09 (Interface Spec), GDS-10 (RTM level) remain `⛔ Planned`. | Medium | 03 | `SCHEDULED` — GDS-08 rides with `IP-0006` (visualizer); GDS-09 rides with whichever package first needs a formal interface contract (likely `IP-0002`); GDS-02/04/10 are lower-priority, scheduled opportunistically alongside those. | NEW |
| BL-0004 | 2026-07-21, run #1 | gate | `IP-0001` was built and self-tested in the same session/agent that authored it, not verified independently. Per this project's own adopted convention (mirroring the reference project's "verification ideally in a fresh session" practice), it is `COMPLETE` but not `VERIFIED`. | Medium | 09 | `DONE` — run #5: the user explicitly authorized "accept single session limitations just this once" to reach an MVP. `09-package-verification` re-ran on `IP-0001` this session with that exception recorded ([VR-0001](../implementation/verification/VR-0001-skeleton-and-single-channel-generation.md)) — VERIFIED. This is a one-time exception, not a standing waiver of the independence rule for future packages. | DONE |
| BL-0005 | 2026-07-21, run #1 | design-question | Preset table values (tempo BPM steps, octave root Hz values, scale semitone sets, Euclidean density k/n pairs — the last not yet implemented) and bad-zone thresholds (dissonance/stale/overload) are first-guess placeholders (GDS-03 SS3/SS4 say so explicitly), not yet tuned by ear. **Update, run #3:** the dissonance weight table specifically is now literature-grounded (Helmholtz roughness ordering), not just plausible-by-taste — see [R204 SS5](encyclopedia/R204-bad-zone-detection-heuristics.md); the *tempo/octave/scale/density* preset values are still untuned placeholders. | Medium | 08 (content-authoring) / 09 (content-review) | `DEFERRED` — revisit trigger: once `IP-0002`/`IP-0003` land (pulse B/wave/noise all present) and there's enough sound surface for a real listening pass to be meaningful; tuning single-channel pulse A alone isn't representative. R204's grounded table is the starting point for that pass, not a substitute for it. | NEW |
| BL-0006 | 2026-07-21, run #1 | feature | No FS-xxx (Feature Specification, stage 06) has been authored yet for any of FEAT-1000...1050 — the pipeline moved from the feature catalog straight to implementation planning for `IP-0001`'s narrow slice rather than writing full 20-field specs first. | Medium | 06 | `SCHEDULED` — **run #5 update**: the user authorized proceeding straight to an MVP this session; abbreviated (not full 20-field) FS notes were written per-package instead of formal FS-1xx documents, tracked as `BL-0012`. Full FS-1xx authoring is deferred, not abandoned — see `BL-0012`. | NEW |
| BL-0007 | 2026-07-21, run #1 | design-question | Working title "Driftune" is explicitly open to revision (MSTR-001 SS1 flags it as such) — no user confirmation has been sought yet. | Low | 01 | `DEFERRED` — revisit trigger: surface it to the user the next time `01-vision` is naturally revisited (e.g. alongside BL-0001's GDS-08 visualizer work, where a name might matter more concretely), rather than interrupting this session's flow for a naming call alone. | NEW |
| BL-0008 | 2026-07-21, run #3 | finding | [R207](encyclopedia/R207-gb-chiptune-channel-idioms.md) (GB-era chiptune channel-usage idioms) found that GDS-03's current plan would give the wave channel the same melodic scale-walk treatment as pulse B, undifferentiated — real GB chiptune convention (LSDJ/GB Studio) treats the wave channel as a distinct bass/timbre voice, not a second identical lead. | Medium | 06 (feature specification, `IP-0002`'s eventual FS) | `DONE` — run #5: `IP-0002` implemented the wave channel with a distinct octave anchor (one index lower, floored at 0) and half note-rate, per R207 SS5's concrete recommendation. Confirmed working via `test_rom.py` T6 (independent CUR_DEGREE_WV walk). | DONE |
| BL-0009 | 2026-07-21, run #3 | doc-defect | Run #1's research pass authored three monolithic per-tier files (`R100`/`R200`/`R300-*.md` directly under `docs/research/`) before reconciling against the adapted `02-research-*` skills' own expected structure (`docs/research/encyclopedia/R1xx`/`R2xx`/`R3xx`, one topic per file, 7-section shape, inline citations). | Medium | 02 | `DONE` — run #3 split the load-bearing content into 10 new encyclopedia topics (R107-R110, R201-R207 except R203 was new not split, R301, R305) with real citations (Helmholtz/psychoacoustic sources, Toussaint's Euclidean rhythms paper, GB chiptune convention sources, PyBoy's own docs, generative-music history), superseded the three original files in place (retained, not deleted), and updated `docs/research/INDEX.md` to the new structure. | DONE |
| BL-0010 | 2026-07-21, run #4 | research-gap | [R211](encyclopedia/R211-melodic-harmonic-generation-techniques.md) SS3 and [R212](encyclopedia/R212-form-tension-and-macro-musical-parameters.md) both independently confirm the same real gap from different angles: no SM83-tractable technique was surfaced this pass for genuine phrase/motif development or composed song-structure/cadence/key-modulation — Driftune currently generates texture and variation, not composed musical form. R214's L-system idea (SS5) is the most promising *specific* candidate, not yet evaluated in depth. **2026-07-22 update:** [R220](encyclopedia/R220-style-evolution-and-song-form-structure.md) (MSTR-001 §9 thread) found a cheap, concrete partial answer for the *song-form-structure* half of this gap — horizontal-resequencing/vertical-layering technique, a state machine driving Driftune's existing tempo/density/scale parameters through envelopes, no new composition needed. The *motif-recurrence* half (L-systems, R214 SS5) remains open, deliberately not solved by R220 (a materially harder problem) — this entry now tracks only that narrower remainder. | Medium | 02 (song-form half now has a concrete R220 answer ready for `03-architecture-design-synthesis` if picked up; motif-recurrence half still needs deeper research before scheduling) | `DEFERRED` — revisit trigger: when the project roadmap wants recognizable musical "sections" (R220 unblocks this now) or motif recurrence specifically (still needs the deeper L-system pass). Not blocking any current package. | NEW |
| BL-0011 | 2026-07-21, run #4 | feature | Four concrete, cited generation-technique upgrade candidates surfaced by the 115-topic research pass: (1) arpeggio-as-polyphony on pulse channels ([R216](encyclopedia/R216-sound-design-techniques.md) SS5); (2) `DIV`-register-seeded boot LFSR instead of a fixed seed, for a genuinely different sequence each power-on ([R213](encyclopedia/R213-prng-seed-management.md) SS5); (3) session-length-adaptive drift, Spore/"The Shuffler" precedent ([R218](encyclopedia/R218-inspiration-and-history-survey.md) SS5); (4) bytebeat-style procedurally-generated Wave RAM tables instead of only picking among precomputed shapes ([R218](encyclopedia/R218-inspiration-and-history-survey.md) SS5). | Low-Medium (post-v1 polish, none blocking the Foundation bucket) | 00 (each should be individually triaged via `00-intake` when picked up, not bulk-authorized here) | **Run #6 update:** item (2) `DONE` — `IP-0007` implemented `DIV`-based reseeding on boot/Select (driven by the user's own reset/randomize directive, not a separate intake). **2026-07-22 update:** item (1) (arpeggio) superseded by `BL-0024` (user-directed R216 implementation push, broader scope covering arpeggio + vibrato + portamento + duty-cycle) — tracked there now, not duplicated here. Items (3), (4) remain `DEFERRED` — revisit trigger: a post-`BL-0024` intake pass. | NEW |
| BL-0012 | 2026-07-21, run #5 | doc-defect | The user directed "iterate the pipeline through to an MVP ROM, accept single-session limitations just this once" — `07-implementation-planning`'s packages for `IP-0002`-`IP-0006` were authored with abbreviated (not full 20-field) FS-equivalent notes inline on each `IP-xxxx` doc, rather than separate formal `06-feature-specification` `FS-1xx` documents, to move at the pace the user's directive required. | Medium | 06 | `SCHEDULED` — **run #6 update**: `IP-0007` (autonomous recovery) continued under the same abbreviated-notes convention. A future session should author the proper `FS-100`...`FS-105` documents retroactively from the shipped `IP-0002`-`IP-0007` packages (an as-built-style backfill, same pattern the reference project used for its own bootstrap), closing this alongside `BL-0006`. | NEW |
| BL-0013 | 2026-07-21, run #5 (scope extended run #9, VR-0004) | doc-defect | `IP-0004`'s implementation deviates from two of GDS-03/R204's original proposals: (1) stale/repetition detection is period-1 only, not the period-1-or-2 GDS-03 SS4b described, and doesn't use GDS-07's reserved ring-buffer fields; (2) dissonance scoring uses a 7-entry interval-class table (inversions folded) rather than R204 SS5's literal 12-entry proposal. Both are documented, deliberate MVP simplifications, not oversights. Run #9 (`VR-0004`) extended this to `FR-1090`, which also still names the unbuilt ring buffer. **Run #19 update (`03-architecture-design-synthesis`):** the architecture half is done — GDS-03 §4a/§4b and GDS-07 §3/§4 now both carry dated reconciliation notes describing the shipped design and explicitly marking the fuller versions as a named v2 upgrade path with no fired trigger. `R204` SS5 (`02-research-game-design`) and `FR-1090`'s text (`04-requirements-engineering`) remain outstanding — out of this skill's write-scope, named for their owners rather than touched directly. | Low-Medium (no functional defect — the simplifications are sound and tested; this is a doc-coherence gap) | 03 (architecture, done) / 02 (research, still owed) / 04 (requirements, still owed) | `SCHEDULED` — R204 SS5 and `FR-1090`'s wording still need the same reconciliation GDS-03/GDS-07 just received; ride either owning skill's next natural touch of those documents. | IN PIPELINE |
| BL-0014 | 2026-07-21, run #5 | doc-defect | `IP-0005`'s own package doc flags a light verification gap: pulse B/wave/noise's Select-reset coverage is implicitly exercised (via T6/T7's drift patterns) but not independently asserted post-Select the way T5/T8 do for pulse A and bad-zone state. No evidence of an actual defect — `init_engine`'s code was read and audited as covering every relevant field. | Low | 09 (a future verification pass, or a small `test_rom.py` addition) | `DONE` — run #10 (`VR-0005`): independently drifted pulse B/wave/noise state, pressed Select, confirmed `CUR_DEGREE_PB`/`CUR_DEGREE_WV`/`NOISE_STEP_IDX`/timers all reset correctly on the exact reset frame (the per-channel `STALE_COUNT_*` variance observed is explained by `IP-0007`'s independent randomized reseeding, not a defect). | DONE |
| BL-0015 | 2026-07-21, run #6 | finding | A rare (~0.1% of frames), self-healing one-frame transient was found where `BAD_ZONE_FLAGS` bit3 (COMBINED) can read one frame behind bits0-2 during `IP-0007`'s testing — e.g. bit0 set while bit3 momentarily still clear, corrected the very next frame. Static review of `_emit_badzone_tick` (`music_engine.py`) shows the combined-bit computation is logically correct and unconditional every call; the exact root cause (plausibly a PyBoy `tick()`-boundary/CPU-execution-granularity artifact rather than a logic bug) was not chased down further given the severity — it affects one frame of the visualizer's palette choice only; the autonomous recovery logic itself reads bits0-2 directly, not bit3, so it is unaffected. | Low (cosmetic, self-healing, no behavioral impact found) | 09 (a future verification/investigation pass) | `DEFERRED` — revisit trigger: if a future deeper investigation into PyBoy tick()/interrupt-timing semantics is undertaken for other reasons, fold this in; not worth a dedicated session on its own given the severity. | NEW |
| BL-0016 | 2026-07-21, run #11 (`VR-0006`) | doc-defect | `FR-1120` requires the visualizer to "represent tempo, per-channel activity, and bad-zone status" — the shipped `visuals.py` (`IP-0006`) implements only per-channel activity and bad-zone status; `TEMPO_IDX`/tempo is never read anywhere in the file (confirmed by grep, zero matches). The package doc's own "Explicit non-scope" section already names "tempo-synced motion" as an unbuilt, unscheduled follow-up, so this is a knowingly-scoped-down MVP simplification, not an oversight — but `FR-1120` itself still describes the full three-signal version. Same pattern as `BL-0013`/`FR-1090` (doc says more than the shipped simplification), one layer up at the requirements text this time for a different feature. No functional defect. | Low-Medium (doc-coherence only, no functional impact) | 04 (requirements) / 03 (architecture, GDS-08 still pending per `BL-0001`) | `SCHEDULED` — reword `FR-1120` to scope tempo representation as an unbuilt v1.1+ follow-up (or build it) the next time `04-requirements-engineering` touches the FR document, alongside `03-architecture-design-synthesis`'s still-pending GDS-08 authoring (`BL-0001`). | NEW |
| BL-0017 | 2026-07-21, run #12 (`VR-0007`) | bug | `OVERLOAD_THRESHOLD=20` (`music_engine.py`, `ONSET_WINDOW_FRAMES=32`) is mathematically unreachable: independently computed the engine's own onset-rate ceiling at the *maximum* tempo (`TEMPO_TABLE[7]=20`) and *maximum* density (`DENSITY_K[7]=12`) simultaneously — `≈8.8` onsets per 32-frame window, well under the threshold of `20`. Empirically confirmed: `BAD_ZONE_FLAGS` bit2 (OVERLOAD) has never been observed set anywhere across this session's testing (`VR-0004`'s live drive, T8's 2000-frame runs, T10's 8000-frame stress evidence, and a dedicated 6000-frame max-density drive in `VR-0007`). This means `FR-1100` (OVERLOAD detection) is code-complete but permanently unreachable, and `IP-0007`'s own overload-driven reload-doubling recovery logic is consequently dead code that can never execute under any valid tempo/density combination — not merely untuned at the default (which is all `BL-0005` currently says), but structurally unreachable everywhere. No test currently asserts OVERLOAD triggering, so this was undetected until `VR-0007`'s non-default-parameter live drive. The dissonant/stuck recovery paths (the ones most likely to matter perceptually) are independently confirmed working and unaffected. | **Medium-High** (a documented requirement/feature is silently non-functional under all reachable engine states — real functional gap, not cosmetic; no crash/data-corruption risk) | 07 (implementation planning — needs an actual remediation package, e.g. `OVERLOAD_THRESHOLD` recalibrated to roughly `5`-`6` given the computed `~8.8` ceiling, or `ONSET_WINDOW_FRAMES` widened) / 08 (code-implementation, to build the recalibration) | `SCHEDULED` — author a small remediation Implementation Package (via `07-implementation-planning`, entering as a code fix, not a doc-only change like `BL-0013`/`BL-0016`) to recalibrate the overload threshold/window so bit2 becomes reachable under realistic play, then re-verify via `09-package-verification`. Should be weighed before or alongside `10-integration-review`, since it's a real functional gap in a feature the release plan already counts as delivered. | NEW |
| BL-0018 | 2026-07-21, run #13 (`10-integration-review`, Foundation bucket) | doc-defect | Six WRAM addresses live in the shipped ROM — `LFSR_STATE_PB`/`LFSR_STATE_WV` (`0xC017`/`0xC018`, added by `IP-0002`), `NOISE_STEP_IDX` (`0xC019`, `IP-0003`), `SEMI_PA`/`SEMI_PB`/`SEMI_WV` (`0xC01A`-`0xC01C`, `IP-0004`) — appear nowhere in `docs/architecture/07-data-model.md` (GDS-07), added incrementally across three packages, none of which updated the architecture doc. No collision with GDS-07's own reserved-but-unused `HIST_HEAD_*`/`HIST_*` range (`0xC013`-`0xC015`, `0xC020`-`0xC037`, already tracked by `BL-0013` from the opposite direction — documented-but-unused, not undocumented-but-used). `memory.md`'s own WRAM quick-reference already lists all six, so the gap is specific to the formal architecture document. No functional defect — this is a single-package-verification blind spot: no individual VR checks GDS-07 completeness against the whole tree, only integration review has that vantage point. | Low-Medium (doc-coherence only, no functional impact) | 03 (architecture) | `DONE` — run #19 (`03-architecture-design-synthesis`): all six addresses now documented in GDS-07 §3, each tagged with the package that added it and this entry's ID. | DONE |
| BL-0019 | 2026-07-21, run #13 (`10-integration-review`, Foundation bucket) | bug | `CHMIX_IDX` ("channel-mix," the Start-button-controlled parameter, `0xC004`) is correctly stepped by input and correctly reset to its preset — but has **no consumer anywhere in the shipped code**. Grepped the entire tree: no channel's generation or PSG register writes are ever gated/muted/conditioned on `CHMIX_IDX`'s value, and no channel-mix preset table (analogous to `TEMPO_TABLE`/`DENSITY_K`) exists at all. All four channels always play regardless of what Start does. `IP-0001`'s own package doc explicitly scoped this as a promise ("`DENSITY_IDX`/`CHMIX_IDX` are wired... but have no consumer until `IP-0002`/`0003`") — `IP-0003` kept the `DENSITY_IDX` half of that promise but **no package from `IP-0002` through `IP-0007` ever wired `CHMIX_IDX`**, and none of the seven independent per-package `VR-000x` verifications caught the gap (each audited only its own package's scope). `FR-1000`/`FR-1010` (traced by `FEAT-1000`, shown fully delivered on the Master Build Plan/Feature Catalog) both presume this gate functions. Already informally flagged in `Claude.md`/`memory.md` ("channel-mix wired but not yet consumed") but never formally routed through the pipeline — zero prior backlog entries reference it, despite `Claude.md`'s own "Known Issues" section claiming the backlog is the live, non-stale list. | **High** (a fully-specified, requirements-traced, player-facing control is completely non-functional, silently dropped across five packages' worth of development; no crash/corruption risk, but a real, user-visible gap between documented and actual behavior) | 07 (implementation planning — author a remediation package wiring `CHMIX_IDX` to a real channel-activity-mask table and gating each channel's generation/register writes on it, per GDS-03's original design intent) / 08 (code-implementation) / 09 (re-verification) | `SCHEDULED` — should be remediated and re-verified before `11-release-readiness` GO is considered for this bucket, per the integration review's own recommendation; needs a fresh `07`→`08`→`09` loop like any other code-scoped finding, with `08`'s own G3 authorization gate applying when reached (not yet attempted). | NEW |
| BL-0020 | 2026-07-22, intake (user-filed) | feature | User's ask, as stated: "implement multiple procgen music schemes which can be used either solo or in combinations according to research guidance." Distinct from `BL-0010` (the phrase/song-form composition gap) and `BL-0011` (a bundle of specific *technique* upgrades — arpeggio, adaptive drift, bytebeat wave tables) — this is a request for a **meta-capability**: more than one selectable generation *scheme* (algorithm/approach to producing notes, not just a preset-table value), usable alone or blended/layered in combination, grounded in the research already gathered across `docs/research/encyclopedia/R2xx-*` (e.g. R211 melodic/harmonic generation techniques, R212 form/tension/macro-musical parameters, R214 L-systems, R216 sound design, R202 Euclidean rhythms, R218 inspiration/history survey). The current engine has exactly one generation scheme (LFSR-driven scale-constrained walk per channel, `_emit_channel_gen`) with no concept of alternate/selectable/combinable schemes anywhere in the requirements baseline, architecture, or code — this has no home in the approved FR/GDS baseline today. | Medium (user-stated feature request, no urgency given) | 03 (architecture-design-synthesis) — routed one level upstream of a plain requirements addition because "solo vs. combination" implies real design decisions (how schemes coexist per-channel or per-mix, what a "combination" mixing rule even means on 4 fixed PSG channels, WRAM/preset-space impact) that should be resolved architecturally before `04-requirements-engineering` drafts FRs against them, per intake's own "pick the more upstream stage when unsure" rule | **Run #19 update (`03-architecture-design-synthesis`):** architecture design complete — [ADS-100](../architecture/ADS-100-combinable-generation-schemes.md) proposes one concrete design (a second "Scheme E" — Euclidean-onset + fixed-motif — selectable per pitched channel alongside the existing "Scheme W" walk, riding the not-yet-shipped `CHMIX_IDX` preset space rather than a new control, per [ADR-0001](../architecture/adr/ADR-0001-scheme-selection-rides-chmix-preset-space.md)). Contingent on `IP-9010` shipping first (the design reuses its planned mask-byte layout). | `SCHEDULED` — rides `04-requirements-engineering` next, to formalize ADS-100 §5's candidate FRs; implementation is contingent on `IP-9010` (`BL-0019`) shipping first, per `ADR-0001`. | IN PIPELINE |
| BL-0021 | 2026-07-22, run #16 (`R208`) | finding | `R208` (palette/color design research) computed approximate perceived luminance for `visuals.py`'s two "on"-tile colors: `CALM_PALETTE` idx3 ≈ 21.7, `BAD_PALETTE` idx3 ≈ 15.9 (0-31 scale) — a real but narrow (~27%) luminance gap. The shipped calm(blue-green)/bad(red) semantic correctly follows the near-universal warning-color convention (R208 §3, cited), and isn't relying on hue alone — but red-green color vision deficiency (affecting a meaningful fraction of viewers, R208 §3 cited) means the gap should be wider to read clearly at a glance, or a second non-color signal (e.g. a distinct tile shape, not just a palette swap) should distinguish the states. Nothing in R205, GDS-03, or the `IP-0006` package doc considered accessibility when the visualizer shipped; `VR-0006` verified hue correctness, not luminance-only legibility. No functional defect — a design-quality gap. | Low-Medium (accessibility/design-quality, no functional impact; real but not urgent) | 08-content-authoring (a small palette-tuning package, or add a shape-based secondary signal) — grounded, doesn't need further architecture work first | `SCHEDULED` — fold into whatever next touches `visuals.py`'s palette (a future visualizer package, or GDS-08's eventual authoring per `BL-0001`); not urgent enough to warrant its own dedicated package ahead of higher-priority work (`BL-0019`, `BL-0017`). | NEW |
| BL-0022 | 2026-07-22, run #17 (`R304`) | doc-defect | `test_rom.py` T1 asserts the header checksum (`0x14D`, the boot-gating one) but never the global checksum (`0x14E`-`0x14F`) — a latent test-coverage gap, not a functional risk, since the global checksum has no boot-time consequence on real hardware/emulators (`R304` §3, cited). A future regression in `set_header`'s emission logic that broke only the global checksum would go undetected. | Low (test-coverage completeness only; zero functional/boot-time risk) | 08-code-implementation (a one-line `test_rom.py` addition, T1.6) | `DEFERRED` — revisit trigger: the next time `test_rom.py`'s T1 suite is touched for another reason; not worth a dedicated package given the near-zero risk. | NEW |
| BL-0023 | 2026-07-22, run #17 (`R306`) | doc-defect | No `requirements.txt`/`pyproject.toml` exists anywhere in the repo to document the `pyboy` dependency `test_rom.py` requires. Confirmed as a real, recurring friction point, not a theoretical gap: every one of this session's fresh-session `09-package-verification` runs needed an unplanned `pip install pyboy` before the suite could run at all (first hit in run #7, `ModuleNotFoundError: No module named 'pyboy'`). `R306` recommends a minimal manifest pinning the verified version (`pyboy==2.7.0`, per `R301`). | Low-Medium (pure friction, no functional risk — every run has successfully self-recovered by installing on the fly) | 08-refactoring (or a small direct doc/tooling addition — not a behavior change) | `SCHEDULED` — add `requirements.txt` (`pyboy==2.7.0`) the next time any package touches the build/test toolchain, or as a standalone near-zero-risk addition whenever convenient; not urgent enough to interrupt higher-priority work. | NEW |
| BL-0028 | 2026-07-25, run #33 (`10-integration-review`, R216 tranche) | doc-defect | `ROADMAP.md`'s rows 04 (Requirements), 06 (Feature Specification), 07 (Implementation Planning), 08 (Implementation), and 09 (Verification) have been stale since run #19 — all five still describe only the original 7-package Foundation-bucket scope, with no mention of `FR-1130`-`FR-1170`, `FEAT-1060`, `FS-106`, or `IP-1060`/`IP-1061` despite that work having been authored and independently verified across runs #20-24 and #33. No functional impact (every stage's own Step 1 reconciliation reads the authoritative per-directory indexes, not this flat summary) — the review's [full report](../reviews/integration-review-r216-sound-design.md) confirms the underlying facts are all correctly recorded elsewhere; this is pure transcription drift on the top-level quick-glance summary. | Medium | 00 (route each stale row to its owning stage skill's next natural touch) | `SCHEDULED` — next natural touch of each row (04/06/07/08/09), or a single consolidated reconciliation pass since no new judgment is needed, just transcription from the already-correct indexes. | NEW |
| BL-0027 | 2026-07-23, run #33 (`VR-1061`) | doc-defect | `IP-1061`'s shipped portamento mechanism is a 2-frame "retrigger at the old pitch, then jump to the new target on the next `arp_tick` call" — not the N-frame linear-interpolation glide (dedicated glide-remaining counter + interpolated-frequency scratch pair) both `IP-1061`'s own package doc and `FS-106`'s Acceptance Criteria (3) describe. The reduction was disclosed honestly in the shipping commit (`268458e`) as a WRAM-budget tradeoff, but the package doc/`FS-106` text were never reworded to match, and `test_rom.py` T11 has zero dynamic assertions on the mechanism itself (write-only `NR13`/`NR14` frequency registers make a direct assertion impractical, the same limitation `R108`/`VR-0001` already established) — a future refactor breaking the `arp_tick`-before-`gen_tick` ordering (the sole mechanism producing this behavior) would pass the full suite undetected. `FR-1150`'s literal wording is still satisfied (transition spans >1 frame, doesn't jump directly on the onset frame) — not a functional defect, a design-doc-vs-shipped gap plus a real test-coverage blind spot. | **Medium** (real, not cosmetic — the gap between documented and actual design is larger than the project's usual `BL-0013`/`BL-0016` doc-coherence pattern, and unlike those, this one has zero test coverage of the as-shipped behavior) | 07 (reconcile `IP-1061`'s package doc to the as-shipped mechanism) / 06 (reword `FS-106`'s Acceptance Criteria (3), or explicitly accept the narrower behavior) | `SCHEDULED` — next natural touch of `IP-1061`'s package doc or `FS-106` (same doc-coherence sweep as `BL-0013`/`BL-0016`/`BL-0025`/`BL-0026`); a test-coverage improvement (e.g. a WRAM shadow-mirror of the last-written frequency, purely for test visibility) is a separate `08-code-implementation` follow-up, not required to close this entry. | NEW |
| BL-0025 | 2026-07-23, run #33 (`VR-1060`) | doc-defect | `IP-1060`'s package doc describes `ARPEGGIO_OFFSETS` as "3 entries" (e.g. `[0, 2, 4]`); the shipped table is 4 entries (`[0, 2, 4, 2]`), a deliberate period-4 up/down shape (avoids a mod-3 counter on hardware with no division instruction). Functionally sound and tested — the package doc's prose just doesn't match the as-shipped table. Same character as `BL-0013`/`BL-0016`. | Low | 07 | `DEFERRED` — revisit trigger: next time `IP-1060`'s package doc or a general doc-coherence sweep (alongside `BL-0013`/`BL-0016`) is touched. | NEW |
| BL-0026 | 2026-07-23, run #33 (`VR-1060`) | doc-defect | `FS-106`'s Title/Purpose field frames the feature as covering "the three pitched channels (pulse A, pulse B, wave)"; `IP-1060` explicitly scoped arpeggio+duty-cycle to pulse A/B only (a documented, deliberate call — wave keeps its plain bass role). `FS-106`'s top-level framing text was never reworded to reflect the narrower shipped scope. | Low | 06 | `DEFERRED` — revisit trigger: next time `FS-106` is touched, or the same doc-coherence sweep as `BL-0025`. | NEW |
| BL-0024 | 2026-07-22, intake (user-directed) | feature | User's ask, as stated: "Iterating the pipeline skill run through to implantation the concepts in R216 — Sound Design Techniques: Timbre, Arpeggio, Vibrato, Portamento & Percussion Synthesis. Iterate until they are all in a committed and pushed ROM." Four concrete techniques from [R216](encyclopedia/R216-sound-design-techniques.md), none yet implemented: (1) arpeggio-as-polyphony (cycle a pulse channel's frequency rapidly among 2-3 notes of an implied chord — R216's own "single highest-value technique to add"); (2) vibrato (periodic small pitch modulation, a phase counter + small table); (3) portamento (per-frame frequency glide from old note to new note over N frames, replacing the current instant-onset model); (4) duty-cycle variation (`NR11`/`NR21` currently fixed at 50%, R216 recommends steerable or degree-linked). R216's 5th technique, percussion synthesis, is effectively already satisfied by the shipped `IP-0003` noise implementation (`NR42=0xF2`, fast-decay envelope matching R216's own recommendation) — to be confirmed, not re-implemented, during requirements/spec work. **Folds in and supersedes `BL-0011` item (1)** (arpeggio, open since run #4) — that item is superseded by this broader, explicitly-scoped entry, not duplicated. | High (explicit user directive: "iterate until... committed and pushed") | 04 (requirements-engineering) — no home in the current FR/GDS baseline (the engine's current model is instant-onset, single-note-per-channel; arpeggio/vibrato/portamento are all new per-note-modulation behavior), routed one level upstream of feature-spec per intake's own "no home in the baseline" rule `IN PIPELINE` — manager weighed the filing request itself as G3 authorization (per intake's own note) and carried the item straight through: `04-requirements-engineering` (FR-1130-FR-1170, NFR-1040/1050), `05-feature-decomposition` (FEAT-1060), `06-feature-specification` (FS-106), `07-implementation-planning` (IP-1060/IP-1061, both recorded G3-authorized on this basis), `08-code-implementation` (both built, tested, committed — IP-1060 arpeggio+duty-cycle, IP-1061 vibrato+portamento, 65/65 `test_rom.py` checks, two clean stress runs). Percussion synthesis (R216's 5th technique) confirmed already satisfied by shipped `IP-0003`, no new work needed. **Run #33 update:** both packages now independently `VERIFIED` — `IP-1060` ([VR-1060](../implementation/verification/VR-1060-arpeggio-and-duty-cycle.md), two Low findings `BL-0025`/`BL-0026`) and `IP-1061` ([VR-1061](../implementation/verification/VR-1061-vibrato-and-portamento.md), one Medium finding `BL-0027`). This completes the `BL-0024`/`FS-106`/R216 tranche through `09-package-verification`; both packages are now eligible for `10-integration-review` alongside whatever other `VERIFIED` work is in scope for the next tranche review. | IN PIPELINE |

**Triage note (run #13, 2026-07-21):** `10-integration-review` ran on the full 7-package Foundation
tranche (the standing gate — all packages independently `VERIFIED` — was satisfied). Two new
entries filed: `BL-0019` (High — `CHMIX_IDX` has no consumer anywhere, a cross-package dead-end no
single-package VR could see) and `BL-0018` (Low-Medium — six WRAM addresses undocumented in
GDS-07). Neither `DEFERRED` — both `SCHEDULED` with a concrete next step, consistent with
severity-honesty for `BL-0019`'s High rating. `BL-0017` re-surfaced by the review, not duplicated.
Per the review's own verdict, `11-release-readiness` is not being sought for this bucket until
`BL-0019` is remediated and re-verified.

**Triage note (run #12, 2026-07-21):** Same session as runs #7-#11 (still independent).
`09-package-verification` ran on `IP-0007` — `VERIFIED`
([VR-0007](../implementation/verification/VR-0007-autonomous-recovery-and-randomize.md)) —
**completing independent verification of the full 7-package tranche.** New entry `BL-0017` filed
(Medium-High: `OVERLOAD_THRESHOLD` mathematically unreachable, a real functional gap not just
doc-coherence) — dispositioned `SCHEDULED` (a remediation package, not `DEFERRED`), consistent
with severity-honesty: a Medium-High finding is addressed via a concrete next step, not shelved.
Not raised as `NEEDS-USER` — the remediation path is clear and will naturally hit its own G3 gate
when `08-code-implementation` is reached, so no decision is needed from the user *now*.

**Triage note (run #11, 2026-07-21):** Same session as runs #7-#10 (still independent).
`09-package-verification` ran on `IP-0006` — `VERIFIED`
([VR-0006](../implementation/verification/VR-0006-minimal-visualizer.md)). New entry `BL-0016`
filed (`FR-1120` overstates shipped tempo-representation scope) — dispositioned `SCHEDULED`
alongside `BL-0013`/`BL-0001`'s existing GDS-08/reconciliation work, not `NEEDS-USER` (it's a
routine doc-coherence fix, not a decision).

**Triage note (run #10, 2026-07-21):** Same session as runs #7-#9 (still independent).
`09-package-verification` ran on `IP-0005` — `VERIFIED`
([VR-0005](../implementation/verification/VR-0005-full-reset-scope.md)). `BL-0014` closed `DONE`
this run — the exact gap it named (pulse B/wave/noise Select-reset never independently asserted)
was closed by this VR's own live drive.

**Triage note (run #9, 2026-07-21):** Same session as runs #7-#8 (still independent).
`09-package-verification` ran on `IP-0004` — `VERIFIED`
([VR-0004](../implementation/verification/VR-0004-bad-zone-detection.md)). One new finding
(`FR-1090` still names the unbuilt ring buffer) folded into the existing `BL-0013` entry rather
than filed as a new ID, since it's the same underlying doc-coherence gap one layer higher.

**Triage note (run #8, 2026-07-21):** Same session as run #7 (still independent — no
implementation work done in this session). `09-package-verification` ran on `IP-0003` —
`VERIFIED` ([VR-0003](../implementation/verification/VR-0003-noise-channel-and-density.md)); no
new findings. No backlog disposition changes required.

**Triage note (run #7, 2026-07-21):** Fresh session. `09-package-verification` ran on `IP-0002` —
`VERIFIED` ([VR-0002](../implementation/verification/VR-0002-pulse-b-and-wave-channel.md)); no new
findings surfaced (`BL-0008`, already `DONE`, independently re-confirmed). No backlog entries
required disposition changes this run — all standing entries' triggers remain unfired.

**Triage note (run #6, 2026-07-21):** User directed: "If you are already able to detect bad zones,
avoid them or navigate naturally out of them. The Select button is only for the user to
reset/randomize if they want to." Implemented `IP-0007` (autonomous avoidance/recovery + Select
reset-and-randomize via `DIV`-seeded LFSRs), amending MSTR-001 C5 to v1.1 and GDS-01/GDS-03 to
match. Fixed two `JR`-out-of-range assembler errors (converted to `JP_NZ`) that surfaced once the
recovery logic lengthened the per-channel routines. Revised several `test_rom.py` assertions
(T5.5, T8.1/2/4/6/7/7c) that had implicitly assumed the now-superseded fixed-seed behavior,
documenting why in each case; added new suite T10 confirming autonomous entry into *and* recovery
from a bad zone with no Select. 60/60 checks pass; an 8000+ frame stress run confirmed both.
`BL-0011` item 2 (`DIV`-seeding) closed as a direct consequence. One new low-severity finding
filed (`BL-0015`, a self-healing display artifact, investigated but not fully root-caused given
its severity). `BL-0012`/`BL-0014` updated to include `IP-0007` in their existing scope.

**Triage note (run #5, 2026-07-21):** User authorized "accept single session limitations just this
once" and directed iterating the pipeline through to an MVP ROM. `BL-0004` (verification gate)
resolved `DONE` this run — `IP-0001` VERIFIED. Implemented and self-tested `IP-0002`-`IP-0006`
(pulse B/wave, noise/density, bad-zone detection, full reset, minimal visualizer) — 56/56
`test_rom.py` checks, 6000+ frame stress run clean. `BL-0006` updated to reflect abbreviated FS
notes (`BL-0012`, scheduled backfill). `BL-0008` closed `DONE` (wave bass role implemented,
confirmed by test). Two new doc-coherence gaps filed (`BL-0013`, `BL-0014`), both low-severity,
neither blocking. This is a deliberate, user-authorized pace exception for this run only — not a
precedent for skipping `06-feature-specification`/independent verification on future increments.

**Triage note (run #4, 2026-07-21):** User supplied a 115-topic, 10-phase research wishlist;
authored 17 new encyclopedia topics (R111, R113-R115, R209-R218, R307-R309) covering the
load-bearing majority of it, deduped against R107-R110/R201-R207/R301/R305 already authored, and
explicitly mapped the remainder (SM83 instruction set, PPU/palette/OAM topics, tile encoding,
toolchain portability, ROM validation split, palette design) to existing `⛔ Planned` rows with
the same "no gap yet" reasoning as before — nothing was silently dropped, see
`docs/research/INDEX.md`. Surfaced `BL-0010` (research-gap, phrase/form generation) and `BL-0011`
(four `feature`-type upgrade candidates) this run, both `DEFERRED` with named triggers, neither
blocking. `BL-0008`'s ride (`IP-0002`'s FS) is unaffected.

**Triage note (run #2, 2026-07-21):** `BL-0002`/`BL-0003` were `DONE` and relocated verbatim to
`docs/pipeline/backlog-archive.md`. `BL-0004` is due this run (rides `09-package-verification` on
`IP-0001`) — flipped to `IN PIPELINE`. `BL-0005`/`BL-0007`'s deferral triggers have not fired yet
(no `IP-0002`/`IP-0003`, no `01-vision` revisit in progress) — left `DEFERRED`. `BL-0001`/`BL-0006`
remain `SCHEDULED` for their named rides, not yet due. Still no `NEEDS-USER` entries.

**Triage note (run #1):** every entry above got an explicit disposition at filing time (no
separate later triage pass needed yet, since this is the project's first run). None are
`NEEDS-USER` — nothing here is a decision only the user can make right now; BL-0007 is the closest
candidate but is explicitly deferred rather than asked, since it's genuinely low-stakes and the
vision doc already names it as revisable later.
